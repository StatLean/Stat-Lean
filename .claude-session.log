srun: job 32996646 queued and waiting for resources
srun: job 32996646 has been allocated resources
The commit is now clean (artifacts back to untracked, as at session start). Everything is finalized. Here is the report.

## Report

### Build status (per module)
| Module | Status |
|---|---|
| `ForMathlib.CondDistribDensity` | ✅ Build completed, **0 sorries** |
| `Equivariance.LocationExamples` | ✅ Build completed, 1 sorry |
| `Equivariance.Pitman` | ✅ Build completed, 2 sorries |

### Per-file sorry counts
- **CondDistribDensity.lean — 0** (was 3). All closed:
  - `map_fst_withDensity_prod` — Tonelli via `← Set.prod_univ` + `← Measure.restrict_prod_eq_prod_univ` + `lintegral_prod`.
  - `lintegral_withDensity_prod` — `lintegral_withDensity_eq_lintegral_mul` then `lintegral_prod`.
  - `condDistrib_withDensity_prod_ae_eq` — built the candidate kernel `Kernel.withDensity (Kernel.const S volume) q`, proved `IsFiniteKernel` (mass `≤ 1` via `ENNReal.inv_mul_le_one`), verified the defining compProd identity with `Measure.ext_of_lintegral`, and concluded by `condDistrib_ae_eq_of_measure_eq_compProd`. Needed two extra **Mathlib-only** imports (`Kernel.WithDensity`, `Kernel.Basic`).
- **LocationExamples.lean — 1.** Closed `locRisk_mean_eq_inv` (the `E₀[X̄²]=1/(m+1)` moment calculation). Remaining: `isLocMRE_mean_gaussian` (genuinely blocked by Pitman below).
- **Pitman.lean — 2** (unchanged): `pitmanEstimator_eq_sub_condMean` and the pre-existing private helper `integrable_lastCoord_orbitCondKernel`.

### `#print axioms` (all clean — only `propext, Classical.choice, Quot.sound`)
- `condDistrib_withDensity_prod_ae_eq` ✅ (also `map_fst_withDensity_prod`, `lintegral_withDensity_prod`, `locRisk_mean_eq_inv`)
- `pitmanEstimator_isLocMRE` — **not axiom-clean**: still transitively depends on the two Pitman `sorry`s, so I could not run this check meaningfully yet.

### What remains and why (honest)
Priority 1 (CondDistribDensity — the stated blocker) is fully done and axiom-clean. Priority 3's `locRisk_mean_eq_inv` is done.

The **Pitman shear change-of-variables** (`pitmanEstimator_eq_sub_condMean`) is **not** closed. The upstream obstacle is gone (CondDistribDensity is now a real, axiom-clean lemma), but the remaining content is the det-1 shear geometry itself: pushing `locationBase f` along `Φ x = (diffs x, xₙ)` to a product-with-density law requires assembling `volume_preserving_transvectionStruct` (the shear) with `MeasurableEquiv.piFinSuccAbove` (the snoc reindexing), then a `condDistrib` transport step, then the Bochner translation substitution. This is a large self-contained measure-geometry development (≈several hundred lines, ~6 hard sub-lemmas). I judged it beyond reliable, axiom-clean completion in this session rather than risk a broken file, so I left the two Pitman sorries in place and refreshed the (proof-body) comment to record the corrected route. Consequently `isLocMRE_mean_gaussian` (which routes through Pitman + Basu) also remains sorried.

### No false statements found
All statements I engaged with are true as stated; no counterexamples. I did **not** touch `Defs.lean`, the frozen docstrings, or `risk_le_gaussian_of_unitVariance` / `locRisk_isLocMRE_gaussian_eq` (already closed — the latter now rests on the newly-proven `locRisk_mean_eq_inv` but still on `isLocMRE_mean_gaussian`'s sorry via `locRisk_isLocMRE_gaussian_eq`).

Commits: `8463822` (CondDistribDensity), `b3b565f` (locRisk_mean_eq_inv), `7b60f67` (Pitman comment). Session artifacts left untracked as at start.
