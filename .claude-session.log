srun: job 33258414 queued and waiting for resources
srun: job 33258414 has been allocated resources
Done. Both sorries in `PolyaUniformCDF.lean` are closed and axiom-clean.

## Report

**Final `lake build` status:** ✅ Green — `Built StatLean.HypothesisTesting.ForMathlib.PolyaUniformCDF`, 0 errors, **0 warnings** (no `sorry` warnings).

**Remaining sorry count:** 0 in this file.

**`#print axioms`:** both expect-only clean —
- `mem_Icc_of_monotone_of_tendsto` → `[propext, Classical.choice, Quot.sound]`
- `tendstoUniformly_of_monotone_of_continuous` → `[propext, Classical.choice, Quot.sound]`

**No statements were false** — both are true as stated and fully proven.

### What I did (edited only the one target file, Mathlib-only imports)

1. **`mem_Icc_of_monotone_of_tendsto`** — limit sandwich: `le_of_tendsto h0 ((eventually_le_atBot x).mono …)` and `ge_of_tendsto h1 ((eventually_ge_atTop x).mono …)`.

2. **Pólya (`tendstoUniformly_of_monotone_of_continuous`)** — standard three-ε argument with `δ = ε/2`:
   - Derived `Monotone F` from pointwise convergence (`le_of_tendsto_of_tendsto'`).
   - Padded the tail thresholds into a genuine **compact core** `[a,b]`; applied **Heine–Cantor** (`isCompact_Icc.uniformContinuousOn_of_continuous`) to get uniform continuity of `F` there.
   - Built a **uniform x-mesh** of width `h < η` and located each `x` in its cell via `i = ⌊(x−a)/h⌋₊`.
   - Combined **finite-point** pointwise convergence (`Finset.eventually_all` + `Metric.tendsto_nhds`) with **monotone sandwiching** of both `Fₙ` and `F`, closing each of left-tail / right-tail / interior with `abs_sub_lt_iff` + `linarith`.

Added imports: `Mathlib.Order.Filter.Finite`, `Mathlib.Topology.UniformSpace.HeineCantor`, `Mathlib.Algebra.Order.Floor.Ring`.

Committed as two commits on `ht/polya` (lemma 1, then Pólya). The 3 downstream bootstrap theorems that inherited `sorryAx` through these are now automatically axiom-clean.
