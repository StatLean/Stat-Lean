srun: job 32953894 queued and waiting for resources
srun: job 32953894 has been allocated resources
All work is committed. Here is the final report.

## Summary

Closed **21 of 23** sorries across the four files. Three files are fully green (0 sorries); `LocationMRE` retains two documented analytic-core debts.

### Final `lake build` status (all green, 0 errors)
| Module | Status | Sorries |
|---|---|---|
| `LocationStructure` | ✅ builds | **0** |
| `ConditionalRiskEngine` | ✅ builds | **0** |
| `LocationMRE` | ✅ builds | **2** (existence corollaries) |
| `RiskUnbiased` | ✅ builds | **0** |

### `#print axioms` (both requested)
```
'isLocMRE_of_conditional_min' depends on axioms: [propext, Classical.choice, Quot.sound]
'locRisk_const'              depends on axioms: [propext, Classical.choice, Quot.sound]
```
Both are axiom-clean — no `sorryAx`. (The engine `isLocMRE_of_conditional_min` does **not** transitively depend on the lifted convex sorry.)

### What was closed
- **LocationStructure (10/10):** `measurable_diffs`, `diffs_snoc`, the two `IsLocEquivariant` closure lemmas, `locRisk_const` (change of variables `x ↦ x + ξ·𝟙` via `lintegral_map` + equivariance), and all four structure/factorization theorems (Doob–Dynkin replaced by the explicit `Fin.snoc · 0` section + the `x = snoc(diffs x) 0 + (x last)·𝟙` translate identity).
- **ConditionalRiskEngine (3/3):** the disintegration `lintegral_eq_lintegral_condDistrib` (via `compProd_map_condDistrib` + `Measure.lintegral_compProd` + `lintegral_map`), `measurable_integral_orbitCondKernel` (`StronglyMeasurable.integral_kernel`), and the engine `lintegral_le_of_condMinimizer` (disintegrate both sides, then `lintegral_mono_ae` with `hmin` at `w := v z`).
- **LocationMRE (2/4):** `isLocMRE_of_conditional_min` (engine instantiation with `F w x = δ₀ x − w`, `Z = diffs`) and `isLocMRE_sq_of_condMean` (squared error, via a private `ℝ≥0∞`-level "mean minimizes MSE" helper handling the non-`L²` case by both sides `= ∞`). `exists_isLocMRE_of_convex` is **closed in its body**, routed through the lifted private analytic lemma.
- **RiskUnbiased (6/6):** `IsEquivariant.smul`, the general `isRiskUnbiased_of_equivariant_min` (transitivity + loss invariance + commutativity), both location risk-unbiasedness lemmas (Thm 1.27 via MRE minimality on `δ − a`, no convexity/evenness added), `locEquivariant_sub_bias`, and `integral_eq_zero_of_isLocMRE_sq` (derives `MemLp δ 2` from finite risk `hfin`, then bias–variance).

### Remaining debt (`LocationMRE`)
The two existence corollaries rest on **two genuinely independent analytic cores**, so they cannot fit the one-sorry-per-file budget honestly:

1. **`exists_measurable_condMinimizer_convex`** (lifted `private` sorry, the one sanctioned per-file debt): supplies the measurable fibrewise minimizer for a convex non-monotone loss by discharging the four `exists_measurable_argmin` hypotheses. The hard one is **continuity of the `ℝ≥0∞`-valued conditional risk in the shift `w`** (a convex `ℝ≥0∞` objective can jump finite→∞ at its finiteness boundary). Precise TODO in-file.
2. **`exists_isLocMRE_of_bounded_loss`** (raw sorry, exceeds the one-per-file target — reported honestly rather than laundered): for a *merely measurable* bounded loss, continuity of `c ↦ ∫⁻ ofReal(ρ(x₀−c)) ∂(locationBase f)` comes from **translation-continuity of the convolution** (using `hcont`, density continuity) + DCT — a different fact that cannot be folded into the convex lemma. The reduction (single-observation ⇒ constant `v(diffs x)`; minimizer via `Continuous.exists_forall_le'`) is spelled out in the TODO.

I did **not** weaken any statement or invent hypotheses; no statement was found false.

Note: two pre-existing untracked artifacts (`.claude-session.log`, `.fanout-prompt.md`) were left untouched.
