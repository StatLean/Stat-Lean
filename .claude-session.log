srun: job 33072250 queued and waiting for resources
srun: job 33072250 has been allocated resources
All four modules build with **0 errors**. Here is the report.

## Report: Invariance pass 4

### The real blocker was an instance error, not just sorries
The previous session committed `map_maximalInvariant_eq_of_orbit` and the three `UMPInvariantFinite` theorems with `[MeasurableSMul G 𝓧]` added — **but never actually built** (it "stalled on a build poll"). `MeasurableSMul G 𝓧` presupposes `[MeasurableSpace G]`, which was absent from the section variables of both files, so every such declaration **failed to synthesize `MeasurableSpace G`** — a hard compile error, not a warning. I added `[MeasurableSpace G]` to the `Shrinkage` section of `MaximalInvariant` and the main variable line of `UMPInvariantFinite`. This is pure scaffolding (no mathematical content — `MeasurableSMul` is ill-formed without it) and completes the laptop session's clear intent.

### Final `lake build` status
| Module | Status | sorry count |
|---|---|---|
| `ForMathlib.GroupAverageMeasure` | ✅ green | 0 |
| `Invariance.MaximalInvariant` | ✅ green | 3 |
| `Invariance.UMPInvariantFinite` | ✅ green | 3 |
| `Invariance.EquivariantConfidence` | ✅ green | 1 |

### `#print axioms` (both requested + the newly-closed one)
- `condExp_eq_groupAverage` → `[propext, Classical.choice, Quot.sound]` ✅
- `isInvariantTest_iff_factors` → `[propext, Classical.choice, Quot.sound]` ✅
- `map_maximalInvariant_eq_of_orbit` → `[propext, Classical.choice, Quot.sound]` ✅ (newly closed)

### What I closed
- **`map_maximalInvariant_eq_of_orbit`** — closed. Beyond the `[MeasurableSpace G]` fix, the committed proof body had a residual type mismatch (`congr 1` yields the goal in the flipped/`∘`-form `M x = (M ∘ (g•·)) x`); replaced `hMinv g x` with `(hMinv g x).symm`.

### Statements left open, with the precise missing hypothesis

**`MaximalInvariant` (3) — frozen signatures, laptop to apply:**
- `InducesOn.mul` (135): missing `Measurable ⇑g'`. The composite pushforward `(Pθ).map ⇑(g'*g) = ((Pθ).map ⇑g).map ⇑g'` needs it; only `hg : Measurable ⇑g` is supplied. (Existing note accurate; content discharged inline in `PreservesFamily.mul` where `g'` is bimeasurable.)
- `InducesOn.inv` (185): missing `Measurable ⇑g`. Inverting the pushforward needs both directions; only `Measurable ⇑g.symm` is supplied. (Existing note accurate.)
- `isInvariantTest_iff_factors_measurable` ⇒ (269): needs a standard-Borel hypothesis on `𝓘`, `𝓨` (measurable uniformization / Jankov–von Neumann). Bare `MeasurableEmbedding (M,Y)` gives an analytic, not measurable, projection. **Prompt confirms: do not attempt.**

**`UMPInvariantFinite` (3) — under-hypothesized beyond `MeasurableSMul`; I rewrote the stale "needs MeasurableSMul" TODOs to the correct diagnoses:**
- `orbitAverage_eq_avg_translated_density` (101): missing **`[SigmaFinite μ]`** (or `[∀ θ, IsProbabilityMeasure (P θ)]`). With `MeasurableSMul` the change of variables now gives equality of the *measures* `μ.withDensity(ofReal∘p(g•θ)) = μ.withDensity(fun x => ofReal(pθ(g⁻¹•x)))`; extracting a.e. equality of the density *functions* needs density uniqueness (`withDensity_eq_iff_of_sigmaFinite`, or `withDensity_eq_iff` via finite mass). Neither is in the signature; without one it genuinely fails on a non-σ-finite `μ`.
- `isUMPInvariant_of_orbitAverage_ratio` (144): **FALSE as stated** — missing **`hμ : ∀ g : G, μ.map (g • ·) = μ)`** (invariance of the dominating measure). The optimality bridge `∫ ψ·orbitAverage(pθᵢ) dμ = power P ψ θᵢ` and the fact that the orbit-averaged densities integrate to 1 both rest on `∫ f(g•x) dμ = ∫ f dμ`. Counterexample without `hμ`: `G=ℤ/2` acting on `ℝ` by reflection (trivial induced action), every `Pθ` reflection-symmetric (so `IsInvariantModel` holds), `[SigmaFinite μ]`/`[IsProbabilityMeasure]` all satisfied — but with an asymmetric `μ` (e.g. `N(1,10)`) the sample-side orbit-average ratio is *not* the true likelihood ratio, so the test is invariant but not UMP. **Smoking gun:** the theorem's own proof-sketch comment already invoked "`hμ`-invariance of `μ`" for a hypothesis that was never in the signature.
- `exists_isUMPInvariant_of_finite_transitive` (176): same missing **`hμ`**. Its two *other* previously-listed blockers are now gone: `MeasurableSMul` is present, and `NeymanPearson.Lemma.exists_mostPowerful` supplies the exact-size test (and `npTest` of invariant orbit-averages is invariant via `isInvariantTest_orbitAverage`). Once `hμ` is added, it closes by forming the orbit-averaged probability measures at the representatives, applying `exists_mostPowerful`, and finishing through the same bridge as the previous theorem.

**`EquivariantConfidence` (1):** `isUMAEquivariant_of_isUMPInvariant` (192) — left as-is; waits on `power_acceptanceTest`/`isCriticalFn_acceptanceTest` in `Tests/Confidence` plus a measurable-slice restriction on competitors. **Prompt confirms: do not attempt.**

### Net
The `MeasurableSMul` fix was **necessary but not sufficient**. It was correct for the measure-level `map_maximalInvariant_eq_of_orbit` (now closed) but the three density/power-level `UMPInvariantFinite` statements need a further hypothesis each — `[SigmaFinite μ]` for the density-uniqueness bridge, and `hμ` (dominating-measure invariance) for the two headline theorems, the latter being provably false without it. These are frozen-signature gaps for the laptop session to apply, exactly analogous to the `condExp_eq_groupAverage` fix.

Two commits landed (MaximalInvariant, UMPInvariantFinite). The untracked `.claude-session.log` / `.fanout-prompt.md` are session artifacts I left alone.
