# Close the sorries in HypothesisTesting/GoodnessOfFit/ChiSquaredMultinomial.lean (ONE file)

**RUN EVERY BUILD IN THE FOREGROUND. Never background a build, never `srun`/`sbatch` from inside, never `sleep`/poll, never schedule a wakeup. Plain `lake build StatLean.HypothesisTesting.GoodnessOfFit.ChiSquaredMultinomial` and WAIT for it. Backgrounding a build wastes the whole session — do not.**

Lean 4 / Mathlib proof engineer on `StatLean`. Pin `v4.29.1`. (The repo `CLAUDE.md` is gitignored and NOT in this worktree — rules below. Project rules: never `lake update`; `sorry` is planned debt tied to a named lemma; never launder unproven content into hypotheses; lines ≤ 100 chars.)

You are ON the cluster in a fan-out worktree on branch `ht/chisq-multinomial-2`. This is the Pearson chi-squared goodness-of-fit limit (TSH Thm 16.3.1).

## Scope — edit ONLY this one file
`StatLean/HypothesisTesting/GoodnessOfFit/ChiSquaredMultinomial.lean`. Touch nothing else — NOT any imported file. You MAY add `import Mathlib.*`, import closed project modules, and add `private` helpers. Public signatures/docstrings FROZEN.

## The 4 sorries (commit after each compiles)
- `pearsonQ_weakConverges_chiSquared` (~131) — under H₀, Pearson's `Q ⇝ χ²_k`.
- `pearsonQ_weakConverges_noncentral` (~165) — under local alternatives `π + h/√n`, `Q ⇝ noncentral χ²_k(λ)` with `λ = multinomialNoncentrality π h`.
- `pearsonQ_consistent` (~197) — against a fixed alternative `p ≠ π`, the level-α test has power → 1.
- `pearsonQ_local_power_nondegenerate` (~203/235) — the local power is a nondegenerate (>α) limit.

## Proof architecture — two PROVEN bricks do the heavy lifting (black-box them)
1. **Multivariate iid CLT** — `StatLean.AsymptoticStatistics.ForMathlib.MultivariateCLT.tendstoInDistribution_multivariate_clt` (imported, 0-sorry). The standardized multinomial count vector `(N − nπ)/√n` is a normalized sum of iid categorical indicator vectors, so it converges in distribution to `N(0, Σ)` with `Σ = diag(π) − π πᵀ`.
2. **Gaussian → chi-squared bridge** — `multivariateGaussian_map_inner_inv_eq_chiSquared` / `_noncentralChiSquared` (imported from `ForMathlib.NoncentralChiSquared`, just proven axiom-clean): `N(μ,S)` pushed through `z ↦ ⟪z, S⁻¹ z⟫` is (noncentral) `χ²_k`.

**The one real subtlety — the degenerate covariance.** `Σ = diag(π) − ππᵀ` is SINGULAR (rank `k`, not `k+1`: the counts sum to `n`, so the last coordinate is redundant). The bridge needs a PosDef matrix. Reduce to the non-degenerate `k`-dimensional marginal: **drop the last category** (work with the first `k` of the `k+1` counts). The reduced covariance `Σ_k = diag(π₀..π_{k-1}) − π' π'ᵀ` (π' the first k) IS PosDef, and Pearson's `Q = Σ_{j=0}^{k} (Nⱼ−nπⱼ)²/(nπⱼ)` equals the reduced quadratic form `⟪Z_k, Σ_k⁻¹ Z_k⟫` (standard identity — the `1/πⱼ` weights reconstruct `Σ_k⁻¹` via Sherman–Morrison). Establish that algebraic identity as a `private` lemma, then CLT + bridge + continuous-mapping (`Continuous.tendstoInDistribution`-style) give the result. `multinomialNoncentrality` is the same quadratic form applied to the drift `h`.

If a genuine gap remains (e.g. the Sherman–Morrison inverse identity is heavier than expected), lift it to ONE named `private` lemma with a precise `-- TODO:` and report it — never fake a proof or weaken a statement.

## Discipline
- Commit after each lemma compiles.
- **If any statement is actually false, STOP, leave it sorried, report the counterexample.** Honest refusal is desired.
- `#print axioms pearsonQ_weakConverges_chiSquared` (and the others) — expect only `propext, Classical.choice, Quot.sound`. Any `sorryAx` means a dep is still sorried — report which.

## Report at the end
Final `lake build` status, remaining sorry count, which CLOSED vs LIFTED, the `#print axioms` outputs, and any statement you believe false (with counterexample).