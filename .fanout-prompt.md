# hdp-orlicz-bernstein — Theorem 2.9.1, ψ₁-norm Bernstein inequality [HDP §2.9]

You are a Lean 4 proof-closure session on branch `conc/hdp-orlicz-bernstein` of the StatLean project (Lean v4.29.1, pinned Mathlib `5e932f97` pre-built under `.lake/packages/mathlib`). You are inside an srun allocation on FAS-RC: run plain `lake build <Target>`; NEVER `lake update`; never touch `lakefile.lean` / `lake-manifest.json` / `lean-toolchain`. Iterate until GREEN and COMMIT BEFORE ENDING (do not end your turn while a build is still running — wait for it, then commit).

CONTEXT: this worktree has no CLAUDE.md/notes/tools (gitignored). Everything you need is in this prompt + the stub files. Mathlib search: `grep -rn "<name>" .lake/packages/mathlib/Mathlib/` and in-proof `exact?` / `apply?` / `rw?`. Upstream StatLean lemmas in OTHER files are `sorry`-backed statements — cite them as black boxes; only YOUR file must become sorry-free. `Orlicz/Attainment.lean` + `Orlicz/Triangle.lean` are REAL on this branch.

## Touch-set (edit ONLY this file)
`StatLean/ConcentrationInequalities/Orlicz/Bernstein.lean`
Do NOT edit any other file. Do NOT change any statement, hypothesis, constant, or tag. If a statement looks unprovable AS STATED, stop and report precisely why — do not weaken it.

## Build gate
`lake build StatLean.ConcentrationInequalities.Orlicz.Bernstein` — exit 0.

## The goal — close the file's sorries (read it for exact statements)
1. `measure_sum_ge_le_of_subExponentialNorm_le` — one-sided engine: exponent `min (t²/(16·∑Kᵢ²)) (t/(4·max Kᵢ))`.
2. `bernstein_subexponential` — Theorem 2.9.1 two-sided.
3. `bernstein_subexponential_weighted` — Corollary 2.9.2 (BONUS; the file's fallback).

## Proof roadmap (HDP p. 47)
- Ingredient: the restricted MGF bound from `Orlicz/SubExponentialMGF.lean` (sorry-backed citable — read its exact statement: `mgf_le_of_lintegral_exp_abs_le_two`-shaped, `E exp(λX) ≤ exp(4λ²K²)` for `|λ| ≤ 1/(2K)`, plus its norm-facing wrapper). Convert `hnorm i : subExponentialNorm (X i) μ ≤ K i` via the ψ₁-attainment (`lintegral_exp_abs_le_two_of_subExponentialNorm_le`, Attainment.lean, REAL).
- Chernoff: `μ{t ≤ ∑X} ≤ exp(−λt)·mgf(∑X, λ)` — Mathlib `ProbabilityTheory.measure_ge_le_exp_mul_mgf` (pin-verified; check exact form: takes `0 ≤ λ` + integrability of `exp(λ·∑X)`).
- Independence: `mgf (∑ i, X i) μ λ = ∏ i, mgf (X i) μ λ` — grep `iIndepFun.mgf_sum` (pin: `ProbabilityTheory.iIndepFun.mgf_sum` — check exact hypotheses: measurability family).
- Combine: for `|λ| ≤ 1/(2·max K)`, `mgf(∑X, λ) ≤ ∏ exp(4λ²Kᵢ²) = exp(4λ²∑Kᵢ²)`; bound `exp(−λt + 4λ²∑Kᵢ²)` at the optimizer `λ* = min (t/(8∑Kᵢ²)) (1/(2·max K))`:
  - Case `t/(8∑Kᵢ²) ≤ 1/(2 max K)`: `λ* = t/(8∑Kᵢ²)`, exponent `= −t²/(16∑Kᵢ²)` — the quadratic regime.
  - Else: `λ* = 1/(2 max K)`, exponent `≤ −t/(4 max K) + t/(8 max K)`... compute: `−t/(2maxK) + 4·(∑Kᵢ²)/(4maxK²)`; with `∑Kᵢ² < t·maxK/4` (case condition): `≤ −t/(2maxK) + t/(4maxK) = −t/(4maxK)` — the linear regime.
  - `min`-form conclusion via `le_min_iff`/case analysis; `Real.exp` monotone; `Finset.sup` facts: `K i ≤ Finset.univ.sup K` (`Finset.le_sup`), positivity of the sup from `hK` + `hn`.
- Integrability side conditions for Chernoff/mgf_sum: from the restricted-MGF integrability exports of SubExponentialMGF.lean (read what it provides; else derive via `integrable_exp_mul_of…`-shaped lemmas there).
- Two-sided: apply the engine to `X` and to `−X` (ψ₁ norm is |·|-invariant: `orliczNorm` depends on `|X|` — a `orliczNorm_neg` lemma exists in `Orlicz/Basic.lean` stubs, citable), `{t ≤ |∑X|} ⊆ {t ≤ ∑X} ∪ {t ≤ −∑X}`, `measure_union_le`, `ENNReal.ofReal_add`-collapse to the factor 2.
- Weighted (BONUS/fallback): `subExponentialNorm (aᵢ·Xᵢ) = |aᵢ|·‖Xᵢ‖` via `orliczNorm_const_mul` (Basic.lean, citable) then the engine at `Kᵢ = |aᵢ|·K`; `∑(aᵢK)² = K²‖a‖₂²`, `max |aᵢ|K = K·‖a‖∞` bookkeeping with `Finset.sup`/`nnnorm`.

## Fallback (graded)
If exactly one lemma resists, it must be `bernstein_subexponential_weighted` (Cor 2.9.2, the bonus; leave its single `sorry`). Theorem 2.9.1 and its engine MUST close.

## Constraints
- New Mathlib imports fine; StatLean imports only from `StatLean.ConcentrationInequalities.*`.
- `private` named helpers allowed; NO sorried `have`s, no `axiom`/`admit`, no new hypotheses.
- Keep all tags/docstrings intact.
- Commit: `git add StatLean/ConcentrationInequalities/Orlicz/Bernstein.lean && git commit -m "conc/hdp-orlicz-bernstein: close Thm 2.9.1"`.

## DONE means
`lake build StatLean.ConcentrationInequalities.Orlicz.Bernstein` exits 0 — WAIT for the build to finish before ending; at most the designated fallback sorry remains; report closed sorries + key Mathlib lemmas.