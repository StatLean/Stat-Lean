# hdp-empirical-symmetrization — Exercise 8.11, empirical-process symmetrization [HDP §8.3.6]

You are a Lean 4 proof-closure session on branch `conc/hdp-empirical-symmetrization` of the StatLean project (Lean v4.29.1, pinned Mathlib `5e932f97` pre-built under `.lake/packages/mathlib`). You are inside an srun allocation on FAS-RC: run plain `lake build <Target>`; NEVER `lake update`; never touch `lakefile.lean` / `lake-manifest.json` / `lean-toolchain`. Iterate until GREEN and COMMIT BEFORE ENDING (wait for the final build, then commit).

CONTEXT: no CLAUDE.md/notes/tools in this worktree. Mathlib search: `grep -rn "<name>" .lake/packages/mathlib/Mathlib/` + `exact?`/`apply?`. Upstream StatLean lemmas may be sorry-backed — cite as black boxes. REAL on this branch: `Symmetrization/Symmetrization.lean` (Lem 6.3.2 both directions, canonical-product + packaged forms), `Symmetrization/Contraction.lean`, `Symmetrization/Gaussian.lean`, Orlicz bridges.

## Touch-set (edit ONLY this file)
`StatLean/ConcentrationInequalities/Symmetrization/Empirical.lean`
No statement/hypothesis/constant/tag changes. If unprovable AS STATED, stop and report.

## Build gate
`lake build StatLean.ConcentrationInequalities.Symmetrization.Empirical` — exit 0.

## The goal
Close the file's sorries (read it): `empirical_symmetrization` (finite `[Fintype ι]` core — MUST close) and `empirical_symmetrization_countable` (`[Countable ι]` lift — this item's designated fallback).

## Proof roadmap (HDP p. 255, Ex 8.11 — "modify the proof of Lemma 6.3.2"; one-sided, constant 2)
- The classes are `F : ι → α → ℝ` with `|F k x| ≤ 1`, data `X : Fin n → Ω → α` iid with `μ.map (X i) = P`. The target compares `∫ ⨆ k |n⁻¹∑ F k (X i ω) − ∫ F k dP|` against `2·∫∫ ⨆ k |n⁻¹∑ εᵢ F k (X i)|` over `μ.prod (signVec n)`.
- Follow Lem 6.3.2's UPPER-bound proof shape (in `Symmetrization/Symmetrization.lean` — REAL, read its proof for the established idioms): introduce the independent copy via the canonical product (Fubini `MeasureTheory.integral_prod`/`integral_integral_swap`), Jensen (`norm_integral_le_integral_norm` on the inner integral, adapted to the `⨆ k |·|` composite — the sup of absolute deviations is a norm-like convex functional: use `Real.iSup`-monotonicity + `abs_integral_le`-style pointwise, or the established `ciSup` lemmas from the 6.3.2 proof), sign-flip equidistribution (`measurePreserving_condSwap` / `signVec_map_mul_pm` from `SignFlip.lean`/`Rademacher.lean`), triangle to drop the copy (factor 2).
- Law transfer: `∫ F k dP = ∫ F k (X i ω) dμ(ω)` via `hX_law i : μ.map (X i) = P` + `MeasureTheory.integral_map` (measurability from `hF_meas`/`hX_meas`).
- Finite-`ι` sup: `⨆ k` over `[Fintype ι] [Nonempty ι]` is a `ciSup` with `BddAbove` from `|·| ≤ 2` (`abs` bound via `hF_bdd` + probability measures); measurability of `ω ↦ ⨆ k …` by `Measurable.iSup` (finite).
- Countable lift (fallback): monotone exhaustion by finite sub-index-sets + MCT/dominated convergence with dominant `2` — mirror `VC/LawOfLargeNumbersCountable.integral_biSup_le_of_forall_finset`'s statement shape (sorry-backed citable) or prove directly.

## Fallback (graded)
Only `empirical_symmetrization_countable`. The `[Fintype ι]` core MUST close (the VC cluster consumes it).

## Constraints
No `axiom`/`admit`; no new hypotheses; keep tags; `private` named helpers OK.
Commit: `git add StatLean/ConcentrationInequalities/Symmetrization/Empirical.lean && git commit -m "conc/hdp-empirical-symmetrization: close Ex 8.11"`.

## DONE means
Target builds exit 0 (WAIT for it); ≤ 1 designated fallback sorry; report closures + key lemmas.