# hdp-bundle-chain-tail2 — SubGaussianIncrements + GammaTwo + DudleyConsumers + FinsetMaximal [HDP §8.1/§8.5 support]
You are a Lean 4 proof-closure session on branch conc/hdp-bundle-chain-tail2 of the StatLean project (Lean v4.29.1, pinned Mathlib 5e932f97 pre-built under .lake/packages/mathlib). Inside an srun allocation: plain `lake build <Target>`; NEVER `lake update`; never touch lakefile/manifest/toolchain. Iterate until GREEN and COMMIT BEFORE ENDING (wait for the final build, then commit).
CONTEXT: no CLAUDE.md/notes/tools here. Mathlib search: grep over .lake/packages/mathlib + exact?/apply?. REAL on this branch: ALL Orlicz files, ALL Symmetrization, Chaining/{DiscreteDudley,Dudley,DudleyTail,GenericChaining,EntropySum,EntropyIntegrand,DyadicNets,PsiTwoMaximal,TailToExpectation}, Maximal/* (batch-1). Statements FROZEN — everything real was proven against them.
## Touch-set (ONLY these four; one at a time, building each)
1. StatLean/ConcentrationInequalities/Chaining/FinsetMaximal.lean — gate `...Chaining.FinsetMaximal`. Finset-indexed adapters of Maximal/FiniteMaximal (REAL, batch-1): reindex via s.equivFin / Finset.equivFin; the sup'↔biSup helper (biSup_finset_eq_sup' — if this name is already proven in GenericChaining.lean's private section on this branch, just prove it here per the stub statement).
2. StatLean/ConcentrationInequalities/Chaining/SubGaussianIncrements.lean — gate `...Chaining.SubGaussianIncrements`. Bridge repackaging: mono_set/mono_const (gauge-set monotonicity — trivial from the def); measure_abs_sub_ge_le via B1 (REAL; convert edist/nndist: edist_dist, ENNReal.ofReal coercions, K·dist positivity per hypotheses); hasSubgaussianMGF_sub via B2 (REAL; hmean is on the INCREMENT); isSubGaussian_sub repackaging (HasSubgaussianMGF.congr + sub_zero under the increment-mean); ae_eq_zero_of_subGaussianNorm_eq_zero = thin cite of orliczNorm_eq_zero_iff (REAL, Attainment) at ψ = psiTwo (Generators facts REAL); sub_ae_eq_zero (dist 0 ⇒ norm 0 by the increments def + the zero lemma).
3. StatLean/ConcentrationInequalities/Chaining/GammaTwo.lean — gate `...Chaining.GammaTwo`. AdmissibleSequence API: nonempty_admissibleSequence (constant singleton sequence — card bounds 1 ≤ 2^(2^k) by Nat.one_le_two_pow), gammaTwo_le (iInf_le), gammaTwo_lt_top_of_finite (eventually-in-T sequence: exhaust T by its own Finset — infDist 0 once seq k = T.toFinset; geometric tail bound with tsum_geometric), exists_eventually_dist_zero, ofReal_infDist/diam bounds (metric algebra: Metric.infDist_le_dist_of_mem, diam bounds).
4. StatLean/ConcentrationInequalities/Chaining/DudleyConsumers.lean — gate `...Chaining.DudleyConsumers`. Exp-branch: integral_sqrt_div_le (∫₀^D √(C/ε) = 2√(CD): integral_rpow / intervalIntegral power rule — mirror TailToExpectation/TailToNorm improper-integral technique, REAL); dudleyIntegral_le_of_cov_le_exp_div (integrand majorization via sqrtLogCov_le_sqrt_log_of_le [EntropyIntegrand REAL] + setIntegral_mono_on + log(exp(C/ε)) = C/ε); dudley_abs_of_cov_le_exp_div (plug dudley_inequality_abs [REAL] + the evaluated integral: 40·2 = 80 per frozen docstrings).
## Fallbacks (≤ ONE per file, docstring-designated only).
## Constraints
No axiom/admit; no new hypotheses; keep tags; private helpers OK.
Commit: git add StatLean/ConcentrationInequalities/Chaining/ && git commit -m "conc/hdp-bundle-chain-tail2: close chaining support tail"
## DONE means
All four targets exit 0 (WAIT); ≤ 4 designated fallbacks; report per-file closures.