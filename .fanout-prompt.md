# Close the 2 sorries in HypothesisTesting/ForMathlib/PolyaUniformCDF.lean (ONE file, Mathlib-only)

**RUN EVERY BUILD IN THE FOREGROUND. Never background a build, never `srun`/`sbatch` from inside, never `sleep`/poll, never schedule a wakeup. Plain `lake build StatLean.HypothesisTesting.ForMathlib.PolyaUniformCDF` and WAIT. Backgrounding a build wastes the whole session — do not.**

Lean 4 / Mathlib proof engineer on `StatLean`. Pin `v4.29.1`. (The repo `CLAUDE.md` is gitignored and NOT here — rules below. Project rules: never `lake update`; `sorry` is planned debt; never launder unproven content into hypotheses; lines ≤ 100 chars.)

You are ON the cluster on branch `ht/polya`. This is **Pólya's uniform-convergence theorem** for CDFs — a self-contained real-analysis brick.

## Scope — edit ONLY this one file
`StatLean/HypothesisTesting/ForMathlib/PolyaUniformCDF.lean`. **Keep imports Mathlib-only.** You MAY add `import Mathlib.*` and `private` helpers. Public signatures/docstrings FROZEN. This is high value: 3 bootstrap theorems currently carry `sorryAx` inherited from these 2 sorries — closing them axiom-cleans those downstream headliners automatically.

## The 2 sorries
1. **`mem_Icc_of_monotone_of_tendsto`** (~50) — a monotone `f : ℝ → ℝ` with the stated pointwise limits has its values in the corresponding `Icc`. Straightforward: monotonicity + `le_of_tendsto`/`ge_of_tendsto` sandwich each value between the limits (`Monotone` gives `f x ≤ f y` for `x ≤ y`; push to the limit).
2. **`tendstoUniformly_of_monotone_of_continuous`** (~63) — **Pólya**: if each `Fn` is monotone, `Fn → F` pointwise, and `F` is continuous with finite limits at `±∞` (read the exact hypotheses in the signature — likely `F` continuous + `Tendsto F atBot/atTop` to the CDF endpoints, or `F` a genuine CDF), then `TendstoUniformly Fn F atTop`.

**Proof of Pólya (standard ε-grid / three-ε argument).** Fix `ε > 0`.
- Using continuity of `F` and its finite limits at `±∞`, pick a finite grid `x₀ < x₁ < … < x_m` (plus the two tails `(-∞,x₀]`, `[x_m,∞)`) such that `F` varies by `< ε` across each cell — i.e. `F(x_{i+1}) − F(x_i) < ε`, and `F(x₀) − F(−∞) < ε`, `F(+∞) − F(x_m) < ε`. (Intermediate value / the limits give this.)
- Pointwise convergence at the **finitely many** grid points gives `N` with `|Fn(x_i) − F(x_i)| < ε` for all `n ≥ N` and every grid point `i` (finite `max` of the per-point thresholds).
- For any `x` and `n ≥ N`: `x` lies in some cell `[x_i, x_{i+1}]`; **monotonicity** of both `Fn` and `F` sandwiches `Fn(x)` and `F(x)` between the grid values, and the cell-width bound + grid-point closeness combine to `|Fn(x) − F(x)| < 2ε` (three-ε). Hence uniform.

Mathlib helpers to look for (verify with `exact?`/`loogle`): `Metric.tendstoUniformly_iff`, `Monotone`/`Monotone.le_of_lt`, `le_of_tendsto_of_tendsto`, `Filter.Eventually` finite-intersection for the grid, and IVT/`Continuous` surjectivity for building the grid. Build the finite grid as a `Finset` or an explicit `n`-point list; a uniform partition of the range `[F(−∞), F(+∞)]` pulled back through `F` is cleanest.

If a genuine gap remains, lift it to ONE named `private` lemma with a precise `-- TODO:` and report it — never fake a proof or weaken a statement.

## Discipline
- Commit after each lemma compiles.
- **If a statement is actually false, STOP and report the counterexample.** Honest refusal is desired.
- `#print axioms tendstoUniformly_of_monotone_of_continuous` and `mem_Icc_of_monotone_of_tendsto` — expect only `propext, Classical.choice, Quot.sound`.

## Report at the end
Final `lake build` status, remaining sorry count, `#print axioms` outputs, any statement you believe false (with counterexample).