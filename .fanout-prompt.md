# hdp-bundle-sym-support — Rademacher + SignFlip + NormAddMeanZero + IndepTransport [HDP §6.3 support]
You are a Lean 4 proof-closure session on branch conc/hdp-bundle-sym-support of the StatLean project (Lean v4.29.1, pinned Mathlib 5e932f97 pre-built under .lake/packages/mathlib). Inside an srun allocation: plain `lake build <Target>`; NEVER `lake update`; never touch lakefile/manifest/toolchain. Iterate until GREEN and COMMIT BEFORE ENDING (wait for the final build, then commit).
CONTEXT: no CLAUDE.md/notes/tools here. Mathlib search: `grep -rn "<name>" .lake/packages/mathlib/Mathlib/` + exact?/apply?. REAL on this branch: Symmetrization.lean (6.3.2), Contraction, Gaussian, Empirical — all PROVEN AGAINST the statements you are closing. Do NOT change any statement.
## Touch-set (edit ONLY these four; close one file at a time, building each)
1. StatLean/ConcentrationInequalities/Symmetrization/Rademacher.lean — gate `...Symmetrization.Rademacher`. radLaw = two-Dirac mixture: IsProbabilityMeasure (measure_add, dirac_apply, smul-measure algebra); integrals via integral_add + integral_smul_measure + integral_dirac; radLaw_map_neg via Measure.map_add/map_smul/map_dirac; signVec marginals via measurePreserving_pi/Measure.pi lemmas; iIndepFun_eval_signVec via iIndepFun_pi (pin-verified); signVec_map_mul_pm via Measure.pi_map_pi (pin) + radLaw_map_mul_pm.
2. StatLean/ConcentrationInequalities/ForMathlib/IndepTransport.lean — gate `...ForMathlib.IndepTransport`. Product-integral transport: MeasureTheory.integral_map + MeasurePreserving.integral_comp + Measure.prod/pi manipulation per the stub statements.
3. StatLean/ConcentrationInequalities/Symmetrization/SignFlip.lean — gate `...Symmetrization.SignFlip`. The conditional coordinate swap is measure-preserving: product-measure factor-wise (MeasurePreserving.prod, Measure.pi invariance under coordinate maps — the swap acts per sign coordinate: reduce to radLaw two-point symmetry).
4. StatLean/ConcentrationInequalities/Symmetrization/NormAddMeanZero.lean — gate `...Symmetrization.NormAddMeanZero`. Eq (6.13): E‖Y‖ ≤ E‖Y+Z‖ for independent mean-zero Z on the product: for fixed y, ‖y‖ = ‖∫(y + Z)dν‖ ≤ ∫‖y + Z‖dν (integral_const + integral_add with mean-zero + norm_integral_le_integral_norm), then integrate in y (Fubini integral_prod).
## Fallbacks (graded, ≤ ONE per file, docstring-designated only): signVec_map_mul_pm / integral_prod_eq_integral_pi_prod / measurePreserving_condSwap / integral_norm_le_integral_norm_add_prod.
## Constraints
No axiom/admit; no new hypotheses; keep tags; private helpers OK. Statements FROZEN.
Commit: git add StatLean/ConcentrationInequalities/Symmetrization/ StatLean/ConcentrationInequalities/ForMathlib/IndepTransport.lean && git commit -m "conc/hdp-bundle-sym-support: close 6.3.2 support layer"
## DONE means
All four targets build exit 0 (WAIT); ≤ 4 designated fallback sorries total; report per-file closures.