# Close the sorries in PointEstimation/ExponentialFamily/{Smoothness,SteinIdentity}.lean and InformationInequality/{ExpFamilyInfo,Multiparameter,Attainment}.lean

Lean 4 / Mathlib proof engineer on `StatLean`. Pin `v4.29.1`. (Note: the repo `CLAUDE.md` is gitignored and is NOT present in this worktree — everything you need is below. Project rules that matter here: never `lake update`; `sorry` is planned debt tied to a named lemma; do not launder unproven content into hypotheses.)

You are ON the cluster. Iterate with plain foreground `lake build <module>`, in the order: `ExponentialFamily.Smoothness`, `ExponentialFamily.SteinIdentity`, `InformationInequality.ExpFamilyInfo`, `.Multiparameter`, `.Attainment`. **Never** background a build, never nest `srun`/`sbatch`, never poll with `until pgrep`.

## Hard constraints

- **Only edit** those five files. Touch nothing else — no `Defs.lean` (all frozen, laptop-only).
- **Signatures, hypothesis tags, docstrings FROZEN.** You may add `import Mathlib.*`, import already-closed project modules, and add `private` helpers. Lines ≤ 100 characters.
- Goal: **0 sorries, 0 errors**, with the sanctioned exceptions noted below. Escape hatch elsewhere: at most one lifted `private` sorry per file with a precise `-- TODO:`; report each.
- **Do not weaken any statement.** If one looks false, STOP, leave it sorried, report the counterexample.
- Commit after each lemma compiles.
- After green: `#print axioms fisherInfo_expFamily`, `#print axioms multiparameter_cramer_rao` — expect only `propext, Classical.choice, Quot.sound`.

## Sanctioned deferrals (do not fight these)

- `Smoothness.analyticOnNhd_integral_exp_inner` — full s-dimensional **joint** analyticity, marked DEFERRAL-ELIGIBLE in its docstring. The continuity / `HasFDerivAt` / all-order-partials statements in the same file cover every downstream consumer; close **those**. Leave the joint-analyticity one sorried with a `-- TODO:` if it resists.
- `Attainment.expFamily_of_cramer_rao_attained` (the ⇒ direction) carries extra pre-agreed regularity (joint measurability, a.e. C¹ in θ, continuity of `I` and `g'`). Attempt it; if the functional-equation step resists, it is the intended escape-hatch candidate for that file. `cramer_rao_attained_of_affine` (⇐) is unconditional — close it regardless.

## Already closed, treat as black boxes (import them)

- `ExponentialFamily.{Basic,MGF,NaturalStatistic}` — including `integral_exp_inner_P` (the mgf identity, with its `E.base ≠ 0` nondegeneracy), `integral_stat_P`/`variance_stat_P` (mean/variance = A′/A″), `map_stat_P`, and the `natSet`/`logPartition`/`cgf` bridge lemmas.
- `InformationInequality.{Basic,CramerRao,Additivity}` — `integral_score_eq_zero`, `fisherInfo_eq_variance_score`, `fisherInfo_reparam`, the bridge to the vdV-area bilinear `fisherInformation`, `cramer_rao`, `cramer_rao_of_deriv`, `fisherInfo_prod`, `fisherInfo_pi`.
- `ForMathlib.IntegrationByPartsReal.{integral_deriv_eq_zero_of_tendsto_zero, integral_eq_zero_of_hasDerivAt_of_tendsto_zero}` — closed; this is what `SteinIdentity` should consume.

## Notes on specific targets

- **`Smoothness`**: `continuousOn_integral_exp_inner` and `hasFDerivAt_integral_exp_inner` are dominated-convergence / parametric-integral arguments on the interior of the `f`-weighted natural set — `hasFDerivAt_integral_of_dominated_loc_of_lip` is the intended brick; the local envelope comes from the `2^s` sign-vector domination `exp(c·Σ|Tᵢ|) ≤ Σ_ε exp⟪c·ε, T⟫`, each term integrable on an interior ball. The derivative is stated in Riesz form (`innerSL ℝ (∫ …)`) — that is the honest "integral of the differentiated integrand".
- **`SteinIdentity`** (1-D): integration by parts against the carrier. The boundary hypothesis is decay of `g · e^{⟪η,T⟫} · h` at ±∞ and the integrability of the LHS integrand is explicit — both are in the signature; use them. Route: apply the closed `integral_eq_zero_of_hasDerivAt_of_tendsto_zero` to `G = g · e^{ηx} · h` and expand `G'` by the product rule.
- **`ExpFamilyInfo`**: `fisherInfo_expFamily` is `I(η) = A''(η)`, i.e. the second derivative of the log-partition — route through the closed `variance_stat_P` plus `fisherInfo_eq_variance_score`, having identified the score of the canonical family as `T − A'(η)`. `infoMatrix_expFamily` is the covariance-matrix version; entries are `cov_η(Tᵢ,Tⱼ)`.
- **`Multiparameter`**: `covariance_matrix_inequality` (Thm 6.1) is the matrix Cauchy–Schwarz — plug `a := C⁻¹ *ᵥ γ` into the scalar covariance inequality applied to `δ` and `⟪a, ψ⟫`; positive-definiteness of `C` (hypothesis) gives the quotient. `multiparameter_cramer_rao` (Thm 6.6) is the instance with `C = infoMatrix` and `γ = ∇g`, discharged coordinatewise from the frozen per-coordinate `HasDerivAt`/diff-under-integral hypotheses.

## Report

Final `lake build` status per module, per-file sorry counts, both `#print axioms` outputs, which (if any) of the two sanctioned deferrals you left open, and any statement you believe is false (with the counterexample).