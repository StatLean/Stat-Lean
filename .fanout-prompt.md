# Close the sorries in HypothesisTesting/NeymanPearson/Generalized.lean (ONE file)

**RUN EVERY BUILD IN THE FOREGROUND. Never background a build, never `srun`/`sbatch` from inside, never `sleep`/poll, never schedule a wakeup, never say "the build is running in the background". Plain `lake build StatLean.HypothesisTesting.NeymanPearson.Generalized` and WAIT for it to print. A previous session lost its entire run by backgrounding a build — do not repeat that.**

Lean 4 / Mathlib proof engineer on `StatLean`. Pin `v4.29.1`. (Repo `CLAUDE.md` is gitignored and NOT here — rules below. Project rules: never `lake update`; `sorry` is planned debt; never launder unproven content into hypotheses; lines ≤ 100 chars.)

You are ON the cluster on branch `ht/np-gen`. This is the **generalized Neyman–Pearson fundamental lemma** — high leverage: it unblocks two-sided UMP (MLR) and UMPU (multiparameter exponential families) downstream.

## Scope — edit ONLY this one file
`StatLean/HypothesisTesting/NeymanPearson/Generalized.lean`. Touch nothing else. You MAY add `import Mathlib.*`, import closed project modules, and add `private` helpers. Public signatures/docstrings FROZEN.

## Already proven — reuse, do NOT re-derive
`isMax_of_multiplier_form` and `isMax_le_of_multiplier_form_nonneg` (the **sufficiency** direction: a test with the multiplier shape maximizes over the class) are closed. The remaining sorries are all the **existence / convex-geometry** side.

## The 4 sorries — work in THIS order (easy first, commit after each compiles)
1. **`convex_isClosed_momentSet`** (~322) — `Convex ℝ (momentSet μ f) ∧ IsClosed (momentSet μ f)` where `momentSet μ f = {(∫φf₁dμ,…,∫φf_mdμ) : φ critical}`.
   - **Convexity is easy**: critical functions (`IsCriticalFn`, values in `[0,1]`) form a convex set and `φ ↦ ∫φfᵢdμ` is affine — take `t•φ₁+(1−t)•φ₂` and push linearity of the integral through.
   - **Closedness is the hard half**: the standard route is weak-* compactness of the unit ball of `L^∞(μ)` (Banach–Alaoglu) — a bounded net of critical functions has a weak-* convergent subnet whose limit is still critical, so `momentSet` is even *compact*. If Mathlib's Alaoglu (`WeakDual.isCompact_closedBall` / `WeakDual.isCompact_polar`) doesn't transfer cleanly at this pin, **lift ONLY the closedness half** to a named `private` lemma with a precise `-- TODO:` (say exactly which Mathlib brick is missing) and keep convexity closed. Report it.
2. **`exists_multipliers_of_max`** (~403) — given the max is attained and `c ∈ interior (momentSet …)`, produce the multipliers. This is the **supporting-hyperplane** step: the maximizer sits on the boundary of the (convex, closed) moment set in the extended direction, so a separating/supporting functional exists — Mathlib: `geometric_hahn_banach_open`/`_point_closed`, or `Convex.exists_supporting_hyperplane`-style results (search with `loogle`/`exact?`). Read off `k` from that functional; the interiority hypothesis is what makes the multiplier finite.
3. **`exists_test_with_prescribed_sizes`** (~430) — a critical test attaining prescribed sizes exists, i.e. the prescribed point lies in `momentSet`. Use convexity + the endpoints `φ≡0`, `φ≡1` and an intermediate-value/connectedness argument on the moment set.
4. **`exists_test_max_integral_of_constraints`** (~118) — existence of the maximizer over the (nonempty, by `hne`) constraint class. This needs the compactness from step 1; if you lifted closedness there, this one inherits the debt — say so explicitly rather than hiding it.

## Discipline
- Commit after each lemma compiles.
- **If a statement is actually false, STOP, leave it sorried, report the counterexample.** Honest refusal is desired.
- For each closed theorem: `#print axioms <name>` — expect only `propext, Classical.choice, Quot.sound`. Report any `sorryAx` and which lifted lemma it traces to.

## Report at the end
Final `lake build` status, remaining sorry count, which CLOSED vs LIFTED (with the exact missing Mathlib brick for each lift), `#print axioms` outputs, and any statement you believe false (with counterexample).