# hdp-emp-approx — Exercise 8.9 approximation lemma (3/m cone-envelope) [HDP §8.2]

You are a Lean 4 proof-closure session on branch `conc/hdp-emp-approx` of the StatLean project (Lean v4.29.1, pinned Mathlib `5e932f97` pre-built under `.lake/packages/mathlib`). Inside an srun allocation: plain `lake build <Target>`; NEVER `lake update`; never touch lakefile/manifest/toolchain. Iterate until GREEN and COMMIT BEFORE ENDING (wait for the final build, then commit).

CONTEXT: no CLAUDE.md/notes/tools here. Mathlib search: `grep -rn "<name>" .lake/packages/mathlib/Mathlib/` + `exact?`/`apply?`. Upstream StatLean lemmas may be sorry-backed — cite as black boxes.

## Touch-set (edit ONLY this file)
`StatLean/ConcentrationInequalities/EmpiricalProcess/LipschitzApprox.lean`
No statement/hypothesis/constant/tag changes. If unprovable AS STATED, stop and report.

## Build gate
`lake build StatLean.ConcentrationInequalities.EmpiricalProcess.LipschitzApprox` — exit 0.

## The goal
Close the file's sorries (read it): the snap-path construction lemmas and `exists_mem_lipschitzNet_dist_le` (every `f` in the 1-Lipschitz `[0,1]`-valued class on `unitInterval` is within `L∞`-distance `3/m` of the cone-envelope net — this item's designated fallback per the docstring; the construction lemmas must close).

## Proof roadmap (HDP Exercise 8.9; the file + `LipschitzNet.lean` docstrings record the design)
- Defs live in `EmpiricalProcess/LipschitzNet.lean` (final bodies; read them): `gridKnot m j = j/m`, `gridPath` (±1-step ℤ-paths), `gridValue m k = clamp(k/m)`, `coneFun` (finite sup of 1-Lipschitz cones, clamped to [0,1]), `lipschitzNet m` (the finite net), `zero_mem_lipschitzNet`.
- Snap values: `k_j := ⌊m·f(j/m)⌋₊` (`Nat.floor`); 1-Lipschitzness of `f` gives `|f((j+1)/m) − f(j/m)| ≤ 1/m` hence the snapped integers step by −1/0/+1 (`Nat.floor` algebra: `Nat.floor_add_one`-family, work in ℤ per the stub's `snapPath` to dodge ℕ-subtraction).
- Envelope bounds: at each knot, `|gridValue m (k_j) − f(j/m)| ≤ 1/m` (floor error); between knots, 1-Lipschitzness of both `f` and the cone envelope bleeds another `≤ 2/m`; total `‖f − coneFun …‖_∞ ≤ 3/m` — pointwise on `x ∈ [j/m, (j+1)/m]` via triangle through the knot (`ContinuousMap.dist_le` converts sup-distance to pointwise; `unitInterval` coercion algebra: `Set.Icc` membership, `div_le_div` bounds).
- Membership: the constructed cone envelope IS a net member (`lipschitzNet` membership by exhibiting `(k₀, step)` parameters — read the net def's membership lemma exports).

## Fallback (graded)
Only `exists_mem_lipschitzNet_dist_le` (the 3/m assembly); the floor/path construction lemmas MUST close.

## Constraints
No `axiom`/`admit`; no new hypotheses; keep tags; `private` named helpers OK.
Commit: `git add StatLean/ConcentrationInequalities/EmpiricalProcess/LipschitzApprox.lean && git commit -m "conc/hdp-emp-approx: close Ex 8.9 approximation"`.

## DONE means
Target builds exit 0 (WAIT for it); ≤ 1 designated fallback sorry; report closures + key lemmas.