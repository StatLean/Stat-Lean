# hdp-bundle-final attempt 2 — finish LawOfLargeNumbersCountable + GlivenkoCantelli + Generalization [HDP §8.3.6/§8.4]
You are a Lean 4 proof-repair/continuation session on branch conc/hdp-bundle-final of the StatLean project (Lean v4.29.1, pinned Mathlib 5e932f97 pre-built under .lake/packages/mathlib). Inside an srun allocation: plain `lake build <Target>`; NEVER `lake update`; never touch lakefile/manifest/toolchain. Iterate until GREEN and COMMIT BEFORE ENDING (wait for the final build, then commit). Budget your time: three files in ~3 hours — if a file threatens to consume more than half your remaining time, take its designated fallback and MOVE ON.
SITUATION: the previous session closed VC/Examples + VC/EntropyIntegral + Symmetrization/SymmetrizationIndep (all green) but ENDED mid-flight: VC/LawOfLargeNumbersCountable.lean has a compilation ERROR at line 136:21 (Application type mismatch) plus remaining sorries; VC/GlivenkoCantelli.lean and VC/Generalization.lean are untouched stubs (4 sorries each).
CONTEXT: no CLAUDE.md/notes/tools here. Mathlib search: grep over .lake/packages/mathlib + exact?/apply?. REAL on this branch: everything else (all Orlicz/Symmetrization/Chaining/EmpiricalProcess + VC/{Defs,SauerShelah,DimensionReduction,HalfSpaces,CoveringByVC,LawOfLargeNumbers,Examples,EntropyIntegral} + ForMathlib incl. SupRatApprox). Statements FROZEN.
## Touch-set (ONLY these three, in this order)
1. StatLean/ConcentrationInequalities/VC/LawOfLargeNumbersCountable.lean — gate `...VC.LawOfLargeNumbersCountable`. FIRST reproduce and fix the 136:21 type mismatch (read the previous session's partial proof around lines 120–150), then close any remaining sorries: integral_biSup_le_of_forall_finset (countable exhaustion + MCT with dominant 1 — mirror EmpiricalProcess/LipschitzLLN's MCT step, REAL on this branch) and vc_lln_countable (instantiate vc_lln_finset [REAL] via vcDim_mono).
2. StatLean/ConcentrationInequalities/VC/GlivenkoCantelli.lean — gate `...VC.GlivenkoCantelli`. vcDim_iicClass = 1; the two right-continuity lemmas; iSup_iic_diff_eq_iSup_rat via ForMathlib/SupRatApprox (REAL); glivenko_cantelli assembly (5400·√1). Designated fallback: iSup_iic_diff_eq_iSup_rat.
3. StatLean/ConcentrationInequalities/VC/Generalization.lean — gate `...VC.Generalization`. shatters_symmDiff_image (labeling bijection via symmDiff algebra), vcDim_symmDiff_image, excess_risk_le_two_mul_sup (Eq 8.40 chain via hERM + le_ciSup with BddAbove from bounds ≤ 1), vc_generalization (integrate + vc_lln_countable on the image class; 10800 = 2·5400). Designated fallback: vcDim_symmDiff_image.
## Constraints
No axiom/admit; no new hypotheses; keep tags; private helpers OK; do NOT touch the three already-green files.
Commit: git add StatLean/ConcentrationInequalities/ && git commit -m "conc/hdp-bundle-final: attempt 2 — countable lift + GC + generalization"
## DONE means
All three targets exit 0 (WAIT for the final build); ≤ 1 designated fallback per file; report per-file closures.