# dinf-gamma: faithful general-T generic chaining (Thm 8.5.2)

TOUCH-SET (only these files):
- StatLean/ConcentrationInequalities/Chaining/GammaTwo.lean (1 sorry:
  `sqrt_two_pow_mul_infDist_le_toReal_gammaFunctional`)
- StatLean/ConcentrationInequalities/Chaining/GenericChaining.lean (4 sorries:
  general `generic_chaining_tail`, `generic_chaining_of_admissible`,
  `generic_chaining`, `generic_chaining_real` in the "Faithful general
  forms" section)

The finite templates `*_of_finite` (same file, complete proofs) are your
blueprints. The general forms need NO covering numbers and NO finiteness:
admissible sequences of T carry the geometry; γ₂/gammaFunctional are honest
ℝ≥0∞.

## Roadmaps

1. `sqrt_two_pow_mul_infDist_le_toReal_gammaFunctional` (GammaTwo): the
   single series term sits under the tsum:
   `ofReal (√2^k · infDist t (A.seq k)) ≤ ∑' k, ofReal (…) ≤ gammaFunctional A`
   (`ENNReal.le_tsum k`, then `le_biSup`-in-ℝ≥0∞ at t ∈ T — ℝ≥0∞ biSup
   junk is ⊥ = 0, harmless: `le_iSup₂`-family). Then
   `ENNReal.ofReal_le_iff_le_toReal hA` (LHS nonneg via
   `Real.sqrt`-pow · `Metric.infDist_nonneg`) lands the real inequality.
   Compare the inline `hterm` block inside
   `AdmissibleSequence.exists_eventually_dist_zero` (this file) — same
   skeleton, minus its finiteness detour.
2. `generic_chaining_tail` (general): blueprint =
   `generic_chaining_tail_of_finite`. Same per-level pair events over
   `PS k := (A.seq (k+1)) ×ˢ (A.seq k)` with the same thresholds and the
   same geometric union bound (unchanged: they live on the sequence, not on
   T's cardinality). Two changes:
   (a) the good-event bound is over the finite subset F: for t ∈ F,
   truncate the walk at level m — `hgood`'s telescoping (template) runs to
   level m and leaves a residual `|X t ω − X (netProj (A.seq m) t) ω|`;
   (b) kill the residual in the TAIL: for fixed δ > 0, the residual event
   `{max_{t∈F} |X t − X_{π_m t}| > δ}` has probability
   `≤ 2|F|·exp(−δ²/(K·r_m)²)` with `r_m := (gammaFunctional A).toReal · (√2)^{−m}`
   — bound the per-t ψ₂ norm by `K · d(t, A.seq m)` (`hinc` at
   `netProj (A.seq m) t ∈ T` via `A.subset_carrier` + `netProj_mem`) and
   `d(t, A.seq m) = infDist`? NOTE `netProj` picks the closest net point:
   `dist t (netProj N t) ≤ dist t a` for the infDist-approximate a — get
   `dist t (netProj (A.seq m) t) ≤ 2 · infDist …` (or ≤ any r with a net
   point within r: `Metric.infDist_lt_iff` + `dist_netProj_le`), then the
   new GammaTwo lemma bounds it by `2·G·(√2)^{−m} → 0`. Use
   `tail_abs_max_le` (PsiTwoMaximal) for the residual event (same device as
   `residual_tail_le` in SubsetChaining.lean — you may NOT edit that file,
   but you may imitate it; if `residual_tail_le`'s statement fits your net
   (radius `2G(√2)^{−m}`, net `A.seq m` ⊆ T), consume it directly).
   Then: event independent of m ⇒ take m → ∞ (inf of the RHS), then δ ↓ 0
   by measure continuity from below (events `{thr + δ_m < …}` increase to
   `{thr < …}`). The template's threshold arithmetic
   `(12 + 4u)·K·G` already contains the slack the of_finite proof used for
   its chain-end — re-derive the inequality chain with the extra `+2δ`
   absorbed the same way the of_finite version absorbed its exact-hit
   chain-end (adjust constants inside the proof only; the FROZEN statement
   constant (12+4u) is what you must land).
3. `generic_chaining_of_admissible` (general): blueprint = of_finite
   version: `by_cases hA : gammaFunctional A = ⊤` (trivial: RHS ⊤ unless
   K = 0 — for K = 0 all increments vanish a.e. and the LHS integral is
   ofReal of `∫ max_F |X−X_{t₀}|` = 0 via the a.e.-zero argument in the
   template's K = 0 branch); else integrate the general tail via the
   machinery in Chaining/TailToExpectation.lean exactly as the template
   does (its `htail → expectation` block transfers with F.sup' carriers —
   the integrand is a finite max, integrable per the ψ₂ per-member
   integrability mined from `integrable_biSup_abs`'s proof or
   `integrable_sup'_finset`).
4. `generic_chaining` (general): mirror the of_finite assembly:
   `hred`: `∫ max_F X ≤ ∫ max_F |X − X_{t₀}|` (pointwise
   `Finset.sup'_le` + `le_abs_self` + anchor `t₀ := hne.some`, integrate,
   `hmean t₀` kills `∫ X_{t₀}`; integrability: `integrable_sup'_finset`
   from hint for the LHS, abs-side as in item 3);
   then `le_iInf` over admissible sequences with
   `ENNReal.mul_iInf` (template's `hpush` block verbatim;
   `Nonempty (AdmissibleSequence T)` from `nonempty_admissibleSequence hne`).
5. `generic_chaining_real`: from the general `generic_chaining` +
   `ENNReal.ofReal_le_iff_le_toReal` with
   `htop : 20 * K * gammaTwo T ≠ ⊤` from hγ (template's real-display proof
   transfers verbatim, minus `gammaTwo_lt_top_of_finite`).

FALLBACK (single named sorry): `generic_chaining_tail`. Items 1, 3, 4, 5
MUST close (they only need the tail's STATEMENT, which is frozen).

DONE: at most the fallback sorry remains across the two files; module +
area builds error-free.
## Common rules (all dinf sessions)

You are a Lean 4 (v4.29.1, Mathlib pinned) proof-closure session for the StatLean
library, working in a git worktree of the Stat-Lean repo on your own branch. The
statements you must prove are ALREADY STUBBED with `sorry` — they are FROZEN
CONTRACTS. You may NOT change any statement, hypothesis, constant, definition, or
docstring of a stubbed declaration; you may NOT touch files outside your touch-set;
you may NOT use `axiom`, `admit`, or add new hypotheses. If you become convinced a
frozen statement is FALSE or unprovable as stated, leave its `sorry` in place and
write a precise blocker report (what corner breaks, with a counterexample sketch) in
your final commit message — do NOT weaken or alter the statement.

Build discipline: iterate with `lake build StatLean.ConcentrationInequalities.<YourModule>`
(fast); before finishing run `lake build StatLean.ConcentrationInequalities` — it must
compile with NO errors (sorry warnings from files outside your touch-set are expected;
your own files must have 0 sorries except, if truly stuck, your single designated
fallback below). Commit early and often with clear messages; your session auto-commits
and pushes at close.

Mathlib search: you have no network tools. Use `exact?`/`apply?`/`rw?` in-proof, and
`grep -rn "<pattern>" .lake/packages/mathlib/Mathlib/` for names. The repo's own
lemmas: grep under StatLean/.

House gotchas: real `⨆ i ∈ s, f i` over a Finset carries junk `sSup ∅ = 0` from
out-of-set indices — the junk-free finite max is `Finset.sup'`; convert with
`biSup_finset_eq_sup'` (needs the family nonneg, e.g. `fun _ _ => abs_nonneg _`) or
`biSup_finset_eq_sup'_of_sup'_nonneg` (needs `0 ≤ sup'`), and `le_biSup_finset` for
the unconditional member bound (all in Chaining/FinsetMaximal.lean). `simp only` does
not β-reduce after `rw [hSdef]`-style set-unfolds reliably — `dsimp only` first when
rewriting under `(fun x => ...) y` redexes. `Finset.sup'_cons` carries the tail's
Nonempty only on its RHS — supply it explicitly. Never write the two-character
sequence `-` `/` inside a docstring. Keep lines ≤ 100 chars.