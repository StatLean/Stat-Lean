# hdp-debt-closure — close the 7 named Batch-10 debts [HDP, tranche 2]
You are a Lean 4 debt-closure session on branch conc/hdp-debt-closure of the StatLean project (Lean v4.29.1, pinned Mathlib 5e932f97 pre-built under .lake/packages/mathlib). Inside an srun allocation: plain `lake build <Target>`; NEVER `lake update`; never touch lakefile/manifest/toolchain. Iterate until GREEN and COMMIT BEFORE ENDING (wait for the final build, then commit). Budget: 7 debts in ~3.5h — spend at most ~30 min per debt before moving on; closing 5 of 7 beats stalling on one.
CONTEXT: no CLAUDE.md/notes/tools here. Mathlib search: grep over .lake/packages/mathlib + exact?/apply?. EVERYTHING in StatLean/ConcentrationInequalities is REAL except exactly these 7 sorries. Statements FROZEN. Only proof bodies may change. Every helper you need that was previously sorry-backed is NOW PROVEN — re-read the relevant files.
## The 7 debts (each: file — lemma — the recorded blocker, now resolved)
1. ForMathlib/ExpTaylorBounds.lean — exp_le_one_add_add_sq_half_of_nonpos (Real.exp x ≤ 1 + x + x²/2 for x ≤ 0): elementary — e.g. h(x) = 1 + x + x²/2 − eˣ has h(0) = 0 and h'(x) = 1 + x − eˣ ≤ 0 for... careful: for x ≤ 0, 1 + x ≤ eˣ (Real.add_one_le_exp) so h' ≤ 0, h decreasing, h(x) ≥ h(0) = 0 for x ≤ 0 ✓. Use MVT/monotonicity (StrictMonoOn via deriv — Mathlib: monotoneOn_of_deriv_nonneg / Real.add_one_le_exp) or a direct convexity trick. Gate `lake build StatLean.ConcentrationInequalities.ForMathlib.ExpTaylorBounds`.
2. Chaining/GammaTwo.lean — AdmissibleSequence.exists_eventually_dist_zero: for finite T, the exhausting admissible sequence eventually contains each t (read the file's own gammaTwo_lt_top_of_finite proof — the same exhaustion device is already built there, REAL). Gate `...Chaining.GammaTwo`.
3. Chaining/DiscreteDudley.lean — discrete_dudley_abs (frozen constant 20): the recorded blocker was the 𝒩-based threshold scale κ' = max{k : 𝒩(T, 2^{-k}) = 1}; DyadicNets is NOW REAL — build the κ'-selection as a PRIVATE lemma here (Int-valued max over a set bounded above because 𝒩 > 1 below the min-positive-distance scale and = 1 above diam — use coveringNumber monotonicity from Maximal/CoveringNumbers + EntropyIntegrand vanishing lemmas, all REAL) and follow the re-anchoring plan in the module docstring (E|X_{t₀'} − X_{t₀}| ≤ √π·K·diam via TailToExpectation REAL; diam·√log2 ≤ 4·dudleySum via the window lemmas REAL). Gate `...Chaining.DiscreteDudley`.
4. Chaining/Dudley.lean — dudley_inequality_countable: countable exhaustion + ∫⁻-MCT (lintegral_iSup for monotone), per-n dudley_inequality_abs (REAL), coveringNumber subset-monotonicity at ε/2 (the 2× loss is in the frozen 80). Mirror the exhaustion device from VC/LawOfLargeNumbersCountable (REAL). Gate `...Chaining.Dudley`.
5. Chaining/GenericChaining.lean — generic_chaining_tail: per-level event bookkeeping per the module docstring (thresholds 2^{k/2}(√(2log2)+1)+u; B1 pair tails via SubGaussianIncrements.measure_abs_sub_ge_le REAL; (a+b+c)² ≥ a²+b²+c² inflation; geometric union bound; the integration consumers generic_chaining_of_admissible are ALREADY REAL and cite this — only the tail event lemma remains). Gate `...Chaining.GenericChaining`.
6. VC/CoveringByVC.lean — card_le_pow_of_entropy_bound: pure-real footnote-4 algebra (from ∀ n ≥ 100ε⁻⁴log N → N ≤ (en/d)^d conclude N ≤ (2/ε)^{21d}; instantiate n = ⌈100ε⁻⁴ log N⌉, isolate via rpow/log monotonicity; the N ≤ A^d√N ⇒ N ≤ A^{2d} squaring step per the docstring). Gate `...VC.CoveringByVC`.
7. VC/LawOfLargeNumbers.lean — rademacher_process_expectation_le: the conditional Dudley glue — ALL inputs now REAL: subGaussianIncrements_inner_signVec (same file), dudley_inequality_abs (40, REAL), the empirical covering bound from VC/CoveringByVC (REAL incl. debt 6 if you closed it — else cite its statement), VC/EntropyIntegral (REAL). Follow the docstring's frozen constant accounting (2700-flavored per-sample bound). Gate `...VC.LawOfLargeNumbers`.
## Constraints
No axiom/admit; no new hypotheses; no statement changes; keep tags; private named helpers OK (new PRIVATE lemmas allowed in the touched files).
Commit after EACH closed debt: git add <file> && git commit -m "conc/hdp-debt-closure: close <lemma>". Any debt you cannot close in its time box: leave its sorry, note why in one sentence in your final report, move on.
## DONE means
Each touched file's target builds exit 0 (WAIT for the last build); report per-debt: closed / left-open + reason.