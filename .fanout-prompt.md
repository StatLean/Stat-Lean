# hdp-gaussian-symmetrization — Lemma 6.6.2, symmetrization with Gaussians [HDP §6.6]

You are a Lean 4 proof-closure session on branch `conc/hdp-gaussian-symmetrization` of the StatLean project (Lean v4.29.1, pinned Mathlib `5e932f97` pre-built under `.lake/packages/mathlib`). You are inside an srun allocation on FAS-RC: run plain `lake build <Target>`; NEVER `lake update`; never touch `lakefile.lean` / `lake-manifest.json` / `lean-toolchain`. Iterate until GREEN.

CONTEXT: this worktree has no CLAUDE.md/notes/tools (gitignored). Everything you need is in this prompt + the stub files. Mathlib search: `grep -rn "<name>" .lake/packages/mathlib/Mathlib/` and in-proof `exact?` / `apply?` / `rw?`. Upstream StatLean lemmas in OTHER files are `sorry`-backed statements — cite them as black boxes; only YOUR file must become sorry-free. (Note: `Symmetrization/Symmetrization.lean` and `Symmetrization/Contraction.lean` may already carry REAL proofs on this branch if their sessions merged first — either way, cite their statements.)

## Touch-set (edit ONLY this file)
`StatLean/ConcentrationInequalities/Symmetrization/Gaussian.lean`
Do NOT edit any other file. Do NOT change any statement, hypothesis, constant, or tag. If a statement looks unprovable AS STATED, stop and report precisely why — do not weaken it.

## Build gate
`lake build StatLean.ConcentrationInequalities.Symmetrization.Gaussian` — exit 0.

## The goal — close the file's sorries (read it for exact statements)
Per the stub: `gaussian_symmetrization_upper_sqrt` (E‖∑Xᵢ‖ ≤ √(2π)·E‖∑gᵢXᵢ‖ mixed form), `gaussian_symmetrization_upper` (headline constant 3), `gaussian_symmetrization_lower` (2√(2log 2N) shape; fallback — see below), `gaussian_symmetrization_lower_log` (4√(log N), `hN : 2 ≤ N`).

READ FIRST (toolbox; sorry-backed citable): `Symmetrization/Symmetrization.lean` (Lem 6.3.2 upper/lower, canonical + packaged), `Symmetrization/Contraction.lean` (Thm 6.6.1 + normalized core), `Symmetrization/SymmetricLaw.lean` (`IsSymmetricLaw`, gaussian symmetry, sign-mixing lemmas `map_signVec_mul_pi`-style), `Symmetrization/GaussianVector.lean` (`gaussVec`, coordinate API, `iIndepFun_eval_gaussVec`), `Symmetrization/GaussianMax.lean` (`integral_ciSup_abs_gaussVec_le` ≤ √(2 log 2N), `pi_norm_eq_ciSup_abs`), `ForMathlib/GaussianAbsMoment.lean` (`integral_abs_id_gaussianReal` = √(2/π)).

## Proof roadmap (HDP p. 185, proof of Lemma 6.6.2)
- UPPER (`≤ √(2π)`, then 3): starting from Rademacher symmetrization (Lem 6.3.2 upper, factor 2): `E‖∑Xᵢ‖ ≤ 2·E‖∑εᵢXᵢ‖`. Insert Gaussian magnitudes: with `g ~ gaussVec` independent, `E|gᵢ| = √(2/π)` so `εᵢ = εᵢ·E|gᵢ|·√(π/2)`; conditionally on ε and X, Jensen pulls `E|gᵢ|` inside the norm: `E‖∑εᵢXᵢ‖ = √(π/2)·E‖∑εᵢ·E|gᵢ|·Xᵢ‖ ≤ √(π/2)·E‖∑εᵢ|gᵢ|Xᵢ‖` (`norm_integral_le_integral_norm` fibrewise + Fubini over the g-factor; linearity `Finset.sum` × integral swap `integral_finset_sum`). Then `εᵢ|gᵢ| =d gᵢ` (sign-mixing: `SymmetricLaw` exports — the product of an independent Rademacher sign and |g| is distributed as g): `E‖∑εᵢ|gᵢ|Xᵢ‖ = E‖∑gᵢXᵢ‖`. Total: `2·√(π/2) = √(2π) ≤ 3`? NO — √(2π) ≈ 2.507 ≤ 3 ✓ (headline weakening step).
- LOWER (`E‖∑gᵢXᵢ‖ ≤ 2√(2 log 2N)·E‖∑Xᵢ‖`-shaped — read the stub's exact frozen form/direction): condition on g's magnitudes: `E‖∑gᵢXᵢ‖ = E_{|g|}E_ε‖∑εᵢ|gᵢ|Xᵢ‖` (sign-mixing backwards); contraction (Thm 6.6.1) at coefficients `|gᵢ|`: `E_ε‖∑εᵢ|gᵢ|Xᵢ‖ ≤ ‖(|g|)‖_∞·E_ε‖∑εᵢXᵢ‖ = (max|gᵢ|)·E_ε‖∑εᵢXᵢ‖`; integrate the max: `E max|gᵢ| ≤ √(2 log 2N)` (`GaussianMax` export); close with Rademacher symmetrization lower (Lem 6.3.2: `E_ε‖∑εᵢXᵢ‖ ≤ 2E‖∑Xᵢ‖`). Constant: `2·√(2 log 2N)`.
- `_lower_log` display: for `N ≥ 2`, `√(2 log 2N) ≤ 2√(log N)` (log 2N = log2 + log N ≤ 2 log N since log N ≥ log 2): pure `Real.log` algebra (`Real.log_mul`, `Real.log_le_log`, `Real.sqrt_le_sqrt` + `nlinarith`).
- All statements live on canonical product measures (read the stub hypotheses EXACTLY — products of `Measure.pi ν`, `signVec n`, `gaussVec n` in a specific order; Fubini = `MeasureTheory.integral_prod` / `integral_integral_swap`; independence is by construction, no `iIndepFun` juggling).

## Fallback (graded)
If exactly one goal resists, it must be `gaussian_symmetrization_lower` (leave its single `sorry`); the upper chain (with √(2π) and 3) and the `_lower_log` display (derived from the fallback statement) must close.

## Constraints
- New Mathlib imports fine; StatLean imports only from `StatLean.ConcentrationInequalities.*`.
- `private` named helpers allowed; NO sorried `have`s, no `axiom`/`admit`, no new hypotheses.
- Keep all tags/docstrings intact.
- Commit: `git add StatLean/ConcentrationInequalities/Symmetrization/Gaussian.lean && git commit -m "conc/hdp-gaussian-symmetrization: close Lem 6.6.2"`.

## DONE means
`lake build StatLean.ConcentrationInequalities.Symmetrization.Gaussian` exits 0; at most the designated fallback sorry remains; report closed sorries + key Mathlib lemmas.