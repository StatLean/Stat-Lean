# Close the sorries in PointEstimation/InformationInequality/{ExpFamilyInfo,DensityRegularity,Attainment}.lean

**RUN EVERY BUILD IN THE FOREGROUND. Never background a build, never `srun`/`sbatch` from inside, never `sleep`/poll, never schedule a wakeup. Plain `lake build <module>` and WAIT. Backgrounding a build wastes the whole session — do not.**

Lean 4 / Mathlib proof engineer on `StatLean`. Pin `v4.29.1`. (The repo `CLAUDE.md` is gitignored and NOT in this worktree — rules below. Project rules: never `lake update`; `sorry` is planned debt tied to a named lemma; never launder unproven content into hypotheses; lines ≤ 100 chars.)

You are ON the cluster in a fan-out worktree on branch `pe/info-close`. This is the Cramér–Rao / Fisher-information chapter.

## Scope — edit ONLY these 3 files
`StatLean/PointEstimation/InformationInequality/{ExpFamilyInfo,DensityRegularity,Attainment}.lean`. Touch nothing else — NOT any `Defs.lean`/`Basic.lean`/`CramerRao.lean` (frozen/closed). You MAY add `import Mathlib.*`, import closed project modules (incl. `ExponentialFamily.Smoothness` and `.MGF`, both now proven — use their differentiation/cumulant results), and add `private` helpers. Public signatures/docstrings FROZEN (except a pre-authorized `[FiniteDimensional …]`/regularity hypothesis if genuinely needed — report it).

## The 3 sorries (commit after each compiles)
1. **`ExpFamilyInfo.lean` `infoMatrix_expFamily`** (~160) — the Fisher **information matrix** of a canonical exponential family equals the **covariance matrix of the natural statistic** `T` (the Hessian of the log-partition function `A`). `fisherInfo_expFamily` (the 1-D scalar version) is already proven — mirror it. Use `ExponentialFamily.MGF` cumulant/covariance identities; if you need second derivatives of `A`, `ExponentialFamily.Smoothness.iteratedDeriv_integral_exp_inner` (now proven) gives them.
2. **`DensityRegularity.lean` `diff_under_integral_core`** (~112, private) — the dominated differentiation-under-the-integral step for a **general** parametric family satisfying `HasDominatedDifferenceQuotient`. Use Mathlib's `hasDerivAt_integral_of_dominated_loc_of_deriv_le` (verify exact name with `exact?`/`loogle`) with the supplied dominating function. This is the engine behind `integral_score_eq_zero_of_density_regular` and `cramer_rao_of_density_regular` above it.
3. **`Attainment.lean` `expFamily_of_cramer_rao_attained`** (~171) — the **equality case**: if the Cramér–Rao bound is attained by an unbiased estimator, the family is a one-parameter exponential family. Route: CR is Cauchy–Schwarz on `⟨score, T−E T⟩`; equality forces `score(θ,x) = a(θ)·(δ(x) − g(θ))` a.e.; integrating in `θ` gives the exponential-family density form. The forward direction `cramer_rao_attained_of_affine` is already proven — reuse its algebra.

If a sorry needs a genuinely missing brick, lift it to ONE named `private` lemma with a precise `-- TODO:` and report it — never fake a proof or weaken a public statement.

## Discipline
- Commit after each lemma compiles.
- **If any statement is actually false, STOP, leave it sorried, report the counterexample.** Honest refusal is desired.
- `#print axioms infoMatrix_expFamily` (and the others) — expect only `propext, Classical.choice, Quot.sound`. Report any `sorryAx`.

## Report at the end
Per-file `lake build` status, remaining sorry count, which CLOSED vs LIFTED, the `#print axioms` outputs, any hypothesis you added, and any statement you believe false (with counterexample).