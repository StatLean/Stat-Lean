# Close the sorries in HypothesisTesting/Randomization/{Asymptotics,SignChange}.lean (2 files)

**RUN EVERY BUILD IN THE FOREGROUND. Never background a build, never `srun`/`sbatch` from inside, never `sleep`/poll, never schedule a wakeup, never say "the build is running in the background". Plain `lake build <module>` and WAIT for it to print. A previous session lost its whole run by backgrounding — do not repeat that.**

Lean 4 / Mathlib proof engineer on `StatLean`. Pin `v4.29.1`. (Repo `CLAUDE.md` is gitignored and NOT here. Project rules: never `lake update`; `sorry` is planned debt; never launder unproven content into hypotheses; lines ≤ 100 chars.)

You are ON the cluster on branch `ht/rand-asym`. This is the **asymptotic theory of randomization/permutation tests**.

## Scope — edit ONLY these 2 files
`StatLean/HypothesisTesting/Randomization/Asymptotics.lean` then `StatLean/HypothesisTesting/Randomization/SignChange.lean`. Nothing else — NOT `Randomization/Defs.lean` (frozen). Build `…Randomization.Asymptotics` first, then `…Randomization.SignChange` (it imports Asymptotics). You MAY add `import Mathlib.*`, import closed project modules, add `private` helpers. Public signatures/docstrings FROZEN.

## Part 1 — `Asymptotics.lean` (3 sorries) — do these FIRST, SignChange depends on them
The file already has **proven** first- and second-moment helpers: `integral_randDist_eq_real_randPairLaw` (~158) and `integral_randDist_sq_eq_real_randPairLaw` (~223), plus `tendsto_real_of_weakConverges_of_null_frontier` (~131). Use them.
1. **`randDist_tendstoInProb_cdf`** (~310) — the random randomization CDF converges in probability to the limiting CDF, pointwise at continuity points. **Second-moment method**: the two helpers give `E[randDist]` and `E[randDist²]` in terms of the *pair* law; under the hypotheses the variance → 0, so Chebyshev gives convergence in probability. This is the engine for the rest.
2. **`randQuantile_tendstoInProb`** (~328) — transfer 1 through the quantile map, using `ForMathlib.QuantileFunction` (imported) and continuity/strict monotonicity of the limit CDF at the quantile.
3. **`weakConverges_randPairLaw_of_randDist_tendstoInProb`** (~348) — the converse packaging: pointwise-in-probability CDF convergence gives weak convergence of the pair law (portmanteau at continuity points).

## Part 2 — `SignChange.lean` (4 sorries) — Thm 17.2.4, sign-change (symmetry) randomization
⭐ **Key available tool:** this file imports `ForMathlib.LindebergCLT`, whose **`weighted_iid_clt` is now CLOSED and axiom-clean** (CLT for `∑ᵢ wₙ,ᵢ Yᵢ` normalised, under weight negligibility). The sign-change randomization distribution is exactly a **±1-weighted sum** of the data, so instantiate `weighted_iid_clt` with the observed data as weights and the Rademacher signs as the i.i.d. sequence.
4. **`weakConverges_randPairLaw_signChange`** (~117/141) — the sign-change randomization law converges weakly to a normal. Route through `weighted_iid_clt` as above; the weight-negligibility hypothesis follows from `max_i xᵢ²/∑ⱼxⱼ² → 0`, which the signature's hypotheses should give you.
5. **`randDist_signChange_tendstoInProb`** (~145/166) — combine 4 with Part 1's `randDist_tendstoInProb_cdf`.
6–7. **`randDist_signChange_tendstoInProb_symmetrized`** (~175/194) and **`randQuantile_signChange_tendstoInProb_symmetrized`** (~199/219) — the symmetrized-population versions; reuse 5 plus Part 1's quantile transfer.

If a genuine gap remains, lift it to ONE named `private` lemma with a precise `-- TODO:` and report it — never fake a proof or weaken a public statement. **If a statement is actually false, STOP and report the counterexample** — honest refusal is desired.

## Discipline
- Commit after each lemma compiles.
- `#print axioms` on each theorem you close — expect only `propext, Classical.choice, Quot.sound`. Report any `sorryAx` and its upstream source.

## Report at the end
Per-file `lake build` status, remaining sorry count per file, which CLOSED vs LIFTED (with the exact obstruction), `#print axioms` outputs, and any statement you believe false (with counterexample).