# bayes3-close-js — finish the James–Stein risk identity + dominance (3D.4) [debt closure]

You are a Lean 4 proof-closure session on branch `bayes3/debt-close` of the StatLean project
(Lean v4.29.1, pinned Mathlib `5e932f97`). Inside an srun allocation: plain `lake build`; NEVER
`lake update`; never touch lakefile/manifest/toolchain/`notes/`/any `*/Defs.lean`/umbrellas.
**NEVER copy code from any external source.** COMMIT AFTER EACH CLOSED THEOREM / MAJOR LEMMA and
`git push cannon HEAD:bayes3/debt-close` so progress is durable. Gate with builds FREQUENTLY:
`lake build StatLean.Bayesian.Hierarchical.Shrinkage`.

## Touch-set (ONLY): `StatLean/Bayesian/Hierarchical/Shrinkage.lean`.

## ⚠️ WHAT IS ALREADY DONE — DO NOT re-prove or modify these (all GREEN, committed):
The file already imports `StatLean.Bayesian.ForMathlib.GaussianDeriv` and contains these proven
`private` bricks — USE them, do not touch:
- `hasDerivAt_jsSlice (c t : ℝ) (h : t^2 + c ≠ 0) : HasDerivAt (fun s => s/(s^2+c)) ((c-t^2)/(t^2+c)^2) t`
- `js_div_sum {p} (x : Fin p→ℝ) : ∑ i, ((∑ j,(x j)^2) - 2*(x i)^2)/(∑ j,(x j)^2)^2 = ((p:ℝ)-2)/(∑ j,(x j)^2)`
- `integrable_bdd_mul_gaussianPDFReal (m)(σ2){h}(hm:Measurable h){C}(hC:∀t,|h t|≤C) : Integrable (fun t => h t * gaussianPDFReal m σ2 t)`
- `integrable_score_gaussianPDFReal (m)(σ2)(hσ:σ2≠0) : Integrable (fun t => (m-t) * gaussianPDFReal m σ2 t)`
- **Brick A `gaussian_stein_1d (m : ℝ) (σ2 : ℝ≥0) (hσ : σ2≠0) {h h' : ℝ→ℝ} (hm : Measurable h)
  (hm' : Measurable h') (hd : ∀ t, HasDerivAt h (h' t) t) {C} (hhC : ∀t,|h t|≤C) (hh'C : ∀t,|h' t|≤C) :
  ∫ t, (t-m)*h t ∂(gaussianReal m σ2) = (σ2:ℝ) * ∫ t, h' t ∂(gaussianReal m σ2)`** — the 1D Gaussian
  Stein identity. This is your workhorse for the cross term.
Also available (deps of the above, 0-sorry in `ForMathlib/GaussianDeriv`): `hasDerivAt_gaussianPDFReal
(θ)(v)(hv)(x) : HasDerivAt (gaussianPDFReal θ v) ((θ-x)/(v:ℝ) * gaussianPDFReal θ v x) x`;
`gaussianPDFReal_nonneg`, `integrable_gaussianPDFReal`, `measurable_gaussianPDFReal`; pinned
`integral_gaussianReal_eq_integral_smul (hv:v≠0) : ∫ f ∂gaussianReal m v = ∫ gaussianPDFReal m v x • f x ∂volume`.

## THE TWO REMAINING `sorry`s (the ONLY goal):
```
jamesStein_risk_difference {p}(hp3 : 3 ≤ p)(θ : Fin p→ℝ)(σ2 : ℝ≥0)(hσ : σ2≠0) :
  ∫ x, ∑ i, (jamesSteinEstimator (σ2:ℝ) x i - θ i)^2 ∂(Measure.pi fun i => gaussianReal (θ i) σ2)
    = (p:ℝ)*σ2 - ((p:ℝ)-2)^2*(σ2:ℝ)^2 * (∫ x, 1/(∑ i,(x i)^2) ∂(Measure.pi fun i => gaussianReal (θ i) σ2))
jamesStein_dominates_mle {p}(hp3)(θ)(σ2)(hσ) : (that JS risk integral) < (p:ℝ)*σ2
```
`jamesSteinEstimator σ2 x i = (1 - ((p:ℝ)-2)*σ2/(∑ j,(x j)^2)) * x i`. Let `N := Measure.pi fun i =>
gaussianReal (θ i) σ2` (a probability measure — `Measure.pi` of Markov ⇒ `IsProbabilityMeasure N`),
`S x := ∑ j,(x j)^2`, `V x i := x i / S x` (so `jamesSteinEstimator = x i - (p-2)σ²·V x i`).

## Route — three remaining bricks.

**Brick B — cross term via coordinate Fubini + Brick A (per coordinate `i`).** Prove:
```
crossTerm (i : Fin p) : ∫ x, (x i - θ i) * (x i / S x) ∂N = (σ2:ℝ) * ∫ x, (S x - 2*(x i)^2)/(S x)^2 ∂N
```
Isolate coordinate `i` by the pinned measure-preserving equiv (obtain `p = n+1` first via
`hp3`; `MeasureTheory.measurePreserving_piFinSuccAbove (fun _ => gaussianReal (θ ·) σ2) i :
MeasurePreserving (MeasurableEquiv.piFinSuccAbove _ i) (Measure.pi μ) ((μ i).prod (Measure.pi fun j =>
μ (i.succAbove j)))`). Push the integral through `(mp).symm.integral_comp'` then `integral_prod`
(Fubini) to get `∫ over other-coords b, ∫ over aᵢ ∼ gaussianReal (θ i) σ2, F ...`. Under the equiv
`e.symm (a,b) = Fin.insertNth i a b` (`MeasurableEquiv.piFinSuccAbove_symm_apply` + `Fin.insertNth`);
`(insertNth i a b) i = a` (`Fin.insertNth_apply_same`). For fixed `b` with `c := ∑_{j≠i} (b j)^2 > 0`
(true for a.e. `b` under the product Gaussian — the singular set `c=0` is a null set), the inner
`aᵢ`-integral is `∫ a, (a - θ i)·(a/(a²+c)) ∂gaussianReal (θ i) σ2`; apply **Brick A** with `h a :=
a/(a²+c)` (bounded by `1/(2√c)`, `HasDerivAt` from `hasDerivAt_jsSlice` with denom `a²+c ≥ c > 0`,
so `h' a = (c-a²)/(a²+c)²` bounded by `1/c`) ⇒ `= σ2·∫ a, (c-a²)/(a²+c)² ∂gaussianReal (θ i) σ2`.
Reassemble Fubini: `= σ2·∫ x, (S x - 2 xᵢ²)/(S x)² ∂N` (since at `x=insertNth i a b`, `S x = a²+c`
and `c - a² = S x - 2 a² = S x - 2 xᵢ²`). If the general Fubini plumbing is heavy, this per-`i`
statement is the whole content — push hard here; it is the mathematical crux alongside Brick D.

**Brick D — `E‖X‖⁻² < ∞` for `p ≥ 3`** (`private`; the analytic crux):
`Integrable (fun x => 1/(S x)) N`. Split `N`-integrably as ball + tail:
- **Tail** `{x : 1 ≤ S x}`: `1/S x ≤ 1` there and `N` is a probability measure ⇒ `IntegrableOn` by
  `MeasureTheory.integrableOn_const`/`.mono'` with the constant-1 bound (needs `AEStronglyMeasurable
  (1/S)` — `S` is a polynomial, `Measurable`).
- **Ball** `{x : S x ≤ 1}`: bound the Gaussian density by its sup. `N = volume.withDensity ρ` with
  `ρ x = ∏ i, gaussianPDFReal (θ i) σ2 (x i)` (`MeasureTheory.volume_pi` + product `withDensity`;
  each factor `≤ (Real.sqrt (2π σ2))⁻¹`, so `ρ ≤ M := (…)^p`). Via `integrable_withDensity_iff_…`,
  reduce to `IntegrableOn (fun x => ρ x · (1/S x)) (ball) volume ≤ M·IntegrableOn (1/S) (ball) volume`.
  The Lebesgue ball fact `IntegrableOn (fun x : Fin p→ℝ => 1/(∑ xᵢ²)) {S≤1} volume`: transfer to
  `EuclideanSpace ℝ (Fin p)` (`EuclideanSpace.volume_preserving_measurableEquiv`, `EuclideanSpace.norm_eq`
  gives `‖x‖² = ∑ xᵢ²`), then use **`MeasureTheory.integrable_fun_norm_addHaar`** with a TRUNCATED
  radial profile `f r := (if r ≤ 1 then r^(-2 : ℝ) else 0)` (the whole-space radial integral diverges;
  the indicator is essential): the iff turns integrability of `fun x => f ‖x‖` into `IntegrableOn
  (fun y => y^(p-1) • f y) (Ioi 0) = IntegrableOn (fun y => y^(p-3)) (Ioc 0 1)`, finite for `p ≥ 3`
  by `intervalIntegrable`/`integrableOn_Ioc_rpow`-style facts (`p - 3 > -1`). Then `1/S x = f ‖x‖`
  on the ball. This brick is the riskiest; if radial/EuclideanSpace transfer walls, isolate it and
  push hardest here.

**Assembly `jamesStein_risk_difference`.** Pointwise `δ_JS(x)ᵢ - θᵢ = (xᵢ - θᵢ) - (p-2)σ²·V x i`. So
`∑ᵢ (δ_JS,ᵢ-θᵢ)² = ∑(xᵢ-θᵢ)² - 2(p-2)σ²·∑(xᵢ-θᵢ)Vᵢ + (p-2)²σ⁴·∑Vᵢ²` pointwise (`Finset.sum_congr`
+ `ring` per term). Integrate by linearity (`integral_add`/`integral_sub`/`integral_finset_sum`;
integrability of each piece: term 1 from Gaussian 2nd moments `variance_id_gaussianReal` per coord;
∑Vᵢ² = 1/S so term 3 = `(p-2)²σ⁴·E‖X‖⁻²` from Brick D; cross term from Brick B + `integrable`):
- `∫ ∑(xᵢ-θᵢ)² dN = ∑ᵢ ∫ (xᵢ-θᵢ)² dN = ∑ᵢ σ² = p σ²` (each coord: `∫(xᵢ-θᵢ)² = Var = σ²`, via
  `integral_comp_eval`/coordinate-marginal `= gaussianReal (θ i) σ2` + `variance_id_gaussianReal`).
- cross term `∑ᵢ ∫ (xᵢ-θᵢ)Vᵢ dN = ∑ᵢ σ²·∫ (S-2xᵢ²)/S² dN` (Brick B) `= σ²·∫ ∑ᵢ(S-2xᵢ²)/S² dN =
  σ²·∫ (p-2)/S dN` (`js_div_sum` pointwise) `= σ²(p-2)·E‖X‖⁻²`.
- `∫ ∑Vᵢ² dN = ∫ (∑xᵢ²)/S² dN = ∫ 1/S dN = E‖X‖⁻²` (`∑ (xᵢ/S)² = S/S² = 1/S`).
Collect: `pσ² - 2(p-2)σ²·σ²(p-2)E‖X‖⁻² + (p-2)²σ⁴·E‖X‖⁻² = pσ² - (p-2)²σ⁴·E‖X‖⁻²`. ∎
**`jamesStein_dominates_mle`.** From the identity, `pσ² - (p-2)²σ⁴·E‖X‖⁻² < pσ²` ⇐ `(p-2)²σ⁴·E‖X‖⁻² > 0`:
`p≥3 ⇒ (p-2)²>0`; `σ2≠0 ⇒ σ⁴>0`; `E‖X‖⁻² = ∫ 1/S dN > 0` because `1/S > 0` on a positive-`N`-measure
set (Brick D gives integrability; positivity from `1/S ≥ 0` and not-a.e.-0). Use `integral_pos_iff`-style.

## Discipline
- USER-INPUT / three-marker docstrings already present on the two theorems — keep them; do NOT add
  hypotheses to the two theorems (that would launder the crux; `E‖X‖⁻²<∞` must be Brick D, proven).
- Gate after EACH brick. Commit + `git push cannon HEAD:bayes3/debt-close` after each green brick.

## Fallback (sanctioned, LAST resort — attempt full close first)
If Brick D (radial/EuclideanSpace) genuinely walls after hard effort within budget, COMMIT Bricks B +
the assembly-modulo-D, and leave the TWO JS theorems `sorry` with Brick D named as the precise blocker
(`Integrable (fun x => 1/∑xᵢ²) N` via ball radial integration). Report exactly which bricks landed.

## Done
Gate exit 0; report each brick (closed / sorry-with-reason) + `#print axioms jamesStein_risk_difference`
and `jamesStein_dominates_mle` (want no `sorryAx`). Target: BOTH JS theorems 0-sorry, axioms clean.