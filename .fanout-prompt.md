# hdp-bundle-gauss-support — GaussianAbsMoment + GaussianVector + SymmetricLaw + GaussianMax [HDP §6.6 support]
You are a Lean 4 proof-closure session on branch conc/hdp-bundle-gauss-support of the StatLean project (Lean v4.29.1, pinned Mathlib 5e932f97 pre-built under .lake/packages/mathlib). Inside an srun allocation: plain `lake build <Target>`; NEVER `lake update`; never touch lakefile/manifest/toolchain. Iterate until GREEN and COMMIT BEFORE ENDING (wait for the final build, then commit).
CONTEXT: no CLAUDE.md/notes/tools here. Mathlib search: `grep -rn "<name>" .lake/packages/mathlib/Mathlib/` + exact?/apply?. REAL on this branch: Symmetrization 6.3.2/6.6.1/6.6.2 + Ex 8.11 + all Orlicz bridges + Maximal/FiniteMaximal (expectation_max_le — batch-1, proven). Statements are FROZEN (real downstream proofs cite them verbatim).
## Touch-set (edit ONLY these four; one file at a time, building each)
1. StatLean/ConcentrationInequalities/ForMathlib/GaussianAbsMoment.lean — gate `...ForMathlib.GaussianAbsMoment`. E|g| = √(2/π) for gaussianReal 0 1: density route (ProbabilityTheory.gaussianReal_apply_eq_integral / gaussianPDF — grep the pin's gaussian density API), split ∫|x| = 2∫_{Ioi 0} x·pdf by symmetry, evaluate via the same integral_Ioi_of_hasDerivAt_of_tendsto pattern as Orlicz/TailToNorm.lean (REAL — mirror it); integrability from moments (memLp_id_gaussianReal-family, grep).
2. StatLean/ConcentrationInequalities/Symmetrization/GaussianVector.lean — gate `...Symmetrization.GaussianVector`. gaussVec coordinate API: marginals (measurePreserving_pi/eval), iIndepFun_eval_gaussVec (iIndepFun_pi), mean/abs-moment transfer (integral over the pi measure via file 1 + MeasurePreserving.integral_comp), hasSubgaussianMGF_eval_gaussVec via mgf_gaussianReal (pin: mgf = exp(μt + σ²t²/2); HasSubgaussianMGF for the standard gaussian with proxy 1 — construct from the definition).
3. StatLean/ConcentrationInequalities/Symmetrization/SymmetricLaw.lean — gate `...Symmetrization.SymmetricLaw`. IsSymmetricLaw closure lemmas: radLaw/gaussian symmetry (map_neg identities — gaussianReal symmetric: grep gaussianReal map neg / gaussianReal_map_neg or derive from density evenness), sign-mixing (ε·|g| =d g: four-piece partition per the stub docstring; Measure.map algebra on products).
4. StatLean/ConcentrationInequalities/Symmetrization/GaussianMax.lean — gate `...Symmetrization.GaussianMax`. E max_i |gᵢ| ≤ √(2 log 2N): apply the EXISTING PROVEN Maximal/FiniteMaximal.expectation_max_le verbatim to the 2N-family (gᵢ, −gᵢ) (each IsSubGaussian with proxy 1 via file 2's MGF bridge + IsSubGaussian defs — read SubGaussian/Defs.lean); pi_norm_eq_ciSup_abs is Pi.norm/Finset.sup algebra.
## Fallbacks (graded, ≤ ONE per file, docstring-designated only): integral_abs_id_gaussianReal / hasSubgaussianMGF_eval_gaussVec / IsSymmetricLaw.map_mul_abs_rad / integral_ciSup_abs_gaussVec_le.
## Constraints
No axiom/admit; no new hypotheses; keep tags; private helpers OK.
Commit: git add StatLean/ConcentrationInequalities/ && git commit -m "conc/hdp-bundle-gauss-support: close gaussian support layer"
## DONE means
All four targets build exit 0 (WAIT); ≤ 4 designated fallbacks total; report per-file closures.