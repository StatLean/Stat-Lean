# Close sorries in HypothesisTesting/LikelihoodMethods/TrinityChiSquared.lean (ONE file)

**RUN EVERY BUILD IN THE FOREGROUND. Never background a build, never `srun`/`sbatch` from inside, never `sleep`/poll, never schedule a wakeup, never say "the build is running in the background". Plain `lake build StatLean.HypothesisTesting.LikelihoodMethods.TrinityChiSquared` and WAIT for it to print. A previous session lost its entire run by backgrounding a build — do not repeat that.**

Lean 4 / Mathlib proof engineer on `StatLean`. Pin `v4.29.1`. (Repo `CLAUDE.md` is gitignored and NOT here — rules below. Project rules: never `lake update`; `sorry` is planned debt; never launder unproven content into hypotheses; lines ≤ 100 chars.)

You are ON the cluster on branch `ht/trinity`. This is the **likelihood trinity** (Wald / Rao-score / LR asymptotic equivalence and their χ² limits).

## Scope — edit ONLY this one file
`StatLean/HypothesisTesting/LikelihoodMethods/TrinityChiSquared.lean`. Touch nothing else — NOT `UniformLAN.lean`, `ScoreUnderAlternatives.lean`, `EstimatorUnderAlternatives.lean` (out of scope; they carry their own open sorries). You MAY add imports, and add `private` helpers. Public signatures/docstrings FROZEN.

## ⭐ START HERE — the file's "sanctioned lifted sorry" is now CLOSEABLE
`private lemma multivariateGaussian_map_quadratic_eq_chiSquared` (~line 201) is marked a sanctioned lifted sorry, with a comment pointing at `stdGaussian` + `MultipleTesting.map_sum_sq_eq_chiSquared`. **That debt is now dischargeable**: the Gaussian→chi-squared bridge was proven and merged into this branch's base. Add

```lean
import StatLean.HypothesisTesting.ForMathlib.NoncentralChiSquared
```

and use **`multivariateGaussian_map_inner_inv_eq_chiSquared`** (0-sorry, axiom-clean: `N(0,S)` pushed through `z ↦ ⟪z, S⁻¹ z⟫` is `chiSquared k`, for `S.PosDef`). Match its quadratic form to this file's `gaussQuadratic`/`waldStatistic` form (the information matrix `J` plays the role of `S⁻¹`, so instantiate with `S = J⁻¹` and rewrite `(J⁻¹)⁻¹ = J` via `Matrix.nonsing_inv_nonsing_inv` under `J.PosDef`). Closing this one lemma is the highest-value action in this session — it feeds every χ² limit below it.

## Then, in order (commit after each compiles)
2. **`wald_tendsto_chiSquared`** (~292) — Wald statistic ⇝ χ²_k. Continuous-mapping on the estimator CLT + the bridge lemma from step 1.
3. **`logLR_tendsto_chiSquared`** (~337) and **`logLR_tendsto_chiSquared_affine`** (~405) — LR statistic ⇝ χ² (and the affine-restriction version, df `= k − m`).
4. **`trinity_asymptotically_equivalent`** (~171) — Wald, score and LR differ by `o_P(1)` under the LAN expansion; conclude all three share the χ² limit.

**Taint warning — check and report.** `UniformLAN`, `ScoreUnderAlternatives`, `EstimatorUnderAlternatives` each carry open sorries. If a theorem you close routes through one of them, its `#print axioms` will show `sorryAx`. That is acceptable and expected — but you MUST report exactly which theorems are axiom-clean and which inherit `sorryAx` and from which upstream lemma. Do NOT weaken a statement to dodge the taint.

If a genuine gap remains, lift it to ONE named `private` lemma with a precise `-- TODO:` and report it — never fake a proof.

## Discipline
- Commit after each lemma compiles.
- **If a statement is actually false, STOP and report the counterexample.** Honest refusal is desired.
- `#print axioms` on every theorem you touch, plus `multivariateGaussian_map_quadratic_eq_chiSquared` (that one MUST come out clean `[propext, Classical.choice, Quot.sound]` once you route it through the bridge).

## Report at the end
Final `lake build` status, remaining sorry count, which CLOSED vs LIFTED, the per-theorem `#print axioms` (clean vs sorryAx + upstream source), and any statement you believe false (with counterexample).