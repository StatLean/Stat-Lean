# hdp-chain-dudley-tail — Eq (8.15) high-probability Dudley (Exercise 8.1) [HDP §8.1]

You are a Lean 4 proof-closure session on branch `conc/hdp-chain-dudley-tail` of the StatLean project (Lean v4.29.1, pinned Mathlib `5e932f97` pre-built under `.lake/packages/mathlib`). You are inside an srun allocation on FAS-RC: run plain `lake build <Target>`; NEVER `lake update`; never touch `lakefile.lean` / `lake-manifest.json` / `lean-toolchain`. Iterate until GREEN and COMMIT BEFORE ENDING (do not end while a build is in flight — wait, then commit).

CONTEXT: no CLAUDE.md/notes/tools in this worktree. Mathlib search: `grep -rn "<name>" .lake/packages/mathlib/Mathlib/` + `exact?`/`apply?`. Upstream StatLean lemmas may be sorry-backed — cite as black boxes. REAL on this branch: `Chaining/DiscreteDudley.lean` (headline), `Chaining/Dudley.lean` (integral forms), `Chaining/GenericChaining.lean` (expectation forms), Orlicz bridges B1/B3/B4.

## Touch-set (edit ONLY this file)
`StatLean/ConcentrationInequalities/Chaining/DudleyTail.lean`
No statement/hypothesis/constant/tag changes. If unprovable AS STATED, stop and report.

## Build gate
`lake build StatLean.ConcentrationInequalities.Chaining.DudleyTail` — exit 0.

## The goal
Close the file's sorries (read it): the three-term per-level tail chaining (`dudley_tail_three_term`-shaped) and the display form `dudley_tail` (Eq 8.15: `sup_{t,s}|X_t − X_s| ≤ C·K·[∫₀^D √log𝒩 + u·D]` w.p. ≥ 1 − 2exp(−u²); the stub's frozen constant and exact event shape are in the docstring — this file's designated fallback is `dudley_tail` per its docstring).

## Proof roadmap (HDP Exercise 8.1; the file's Proof-formalization notes record the designed thresholds)
- Same dyadic chain as `DiscreteDudley` (via `Chaining/DyadicNets.lean` + `EntropySum.lean` window lemmas, sorry-backed citable) but with PER-LEVEL TAIL control instead of expectations: at level `k`, threshold `τ_k = C·K·ε_k·(√(2 log N_k) + √(k − κ') + u)`; B1 pair tails (`SubGaussianIncrements.measure_abs_sub_ge_le`, REAL via bridges) + union over ≤ `N_k²` pairs give per-level failure `≤ 2·N_k²·exp(−(√(2log N_k) + √(k−κ') + u)²)`; the `(a+b+c)² ≥ a² + b² + c²` inflation (nonneg terms; `nlinarith`) splits into `N_k^{-2}·…` — kills the union factor — times `exp(−(k−κ'))` — geometric sum — times `exp(−u²)`; total `∑_k ≤ 2·exp(−u²)` (geometric series bound: `tsum_geometric_le`-flavored / finite window sum).
- On the complement event, sum the thresholds: `∑ ε_k√log N_k ≤ 2∫√log𝒩` (window lemma), `∑ ε_k√(k−κ') ≤ C'·ε_{κ'} ≤ C'·D` (geometric-times-√ sum — elementary: `∑ 2^{-j}√j` converges; the docstring's frozen constant absorbs it), `∑ ε_k·u ≤ 2·ε_{κ'}·u ≤ 2·D·u`.
- Two-sided sup over pairs via the anchor triangle (factor 2), as in `dudley_inequality_abs_pair`.

## Fallback (graded)
Only the decl the file's docstring designates (`dudley_tail`, the display form); `dudley_tail_three_term` MUST close.

## Constraints
No `axiom`/`admit`; no new hypotheses; keep tags; `private` named helpers OK.
Commit: `git add StatLean/ConcentrationInequalities/Chaining/DudleyTail.lean && git commit -m "conc/hdp-chain-dudley-tail: close Eq 8.15"`.

## DONE means
Target builds exit 0 (WAIT for it); ≤ 1 designated fallback sorry; report closures + key lemmas.