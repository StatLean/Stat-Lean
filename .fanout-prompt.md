# hdp-chain-generic — Theorem 8.5.2, generic chaining (γ₂ bound) [HDP §8.5.2]

You are a Lean 4 proof-closure session on branch `conc/hdp-chain-generic` of the StatLean project (Lean v4.29.1, pinned Mathlib `5e932f97` pre-built under `.lake/packages/mathlib`). You are inside an srun allocation on FAS-RC: run plain `lake build <Target>`; NEVER `lake update`; never touch `lakefile.lean` / `lake-manifest.json` / `lean-toolchain`. Iterate until GREEN.

CONTEXT: this worktree has no CLAUDE.md/notes/tools (gitignored). Everything you need is in this prompt + the stub files. Mathlib search: `grep -rn "<name>" .lake/packages/mathlib/Mathlib/` and in-proof `exact?` / `apply?` / `rw?`. Upstream StatLean lemmas in OTHER files are `sorry`-backed statements — cite them as black boxes; only YOUR file must become sorry-free.

## Touch-set (edit ONLY this file)
`StatLean/ConcentrationInequalities/Chaining/GenericChaining.lean`
Do NOT edit any other file. Do NOT change any statement, hypothesis, constant, or tag. If a statement looks unprovable AS STATED, stop and report precisely why — do not weaken it.

## Build gate
`lake build StatLean.ConcentrationInequalities.Chaining.GenericChaining` — exit 0.

## The goal — close these sorries (read the file for exact statements)
1. `generic_chaining_tail` — per-admissible-sequence high-probability bound: `μ{(6+2u)·K·(γ(A)).toReal < ⨆_{t∈T} |X t − X t₀|} ≤ ofReal(2·exp(−u²))` (fallback; see below).
2. `generic_chaining_of_admissible` — expectation at a fixed admissible sequence, constant 10, `ofReal`-form.
3. `generic_chaining` — Theorem 8.5.2: `ofReal(∫ ⨆ X) ≤ 10·K·gammaTwo T` (mean-zero, `hint` integrability).
4. `generic_chaining_real` — the real display via `gammaTwo_lt_top_of_finite`.

READ FIRST (toolbox; sorry-backed citable): `Chaining/GammaTwo.lean` (`AdmissibleSequence` structure — fields `seq : ℕ → Finset E`, subset/nonempty/card-bounds; `gammaFunctional A = ⨆ t ∈ T, ∑' k, ofReal((√2)^k · infDist t ↑(A.seq k))`; `gammaTwo = ⨅ A, γ(A)`; `gammaTwo_le`, `gammaTwo_lt_top_of_finite`, `nonempty_admissibleSequence`, `ofReal_infDist_zero_le_gammaFunctional`, `ofReal_diam_le_two_mul_gammaFunctional`), `Chaining/SubGaussianIncrements.lean` (B1 tail form `measure_abs_sub_ge_le`), `Chaining/TailToExpectation.lean` (tail⇒expectation integration), `Chaining/DyadicNets.lean` (closest-point projections — here projections go to `A.seq k`, not dyadic nets: `Metric.exists_infDist`-style attainment on finite sets, `Metric.infDist_le_dist_of_mem`).

## Proof roadmap (HDP p. 247–248, Steps 1–3, Eqs. (8.48)–(8.50))
- Fix `A`; define per-level projections `π_k t ∈ A.seq k` attaining/near-attaining `infDist t (A.seq k)` (finite nonempty → attained: `Set.Finite.exists_min`-flavored or `Finset.exists_min_image`).
- Chain: for finite T the walk `π_0 t, π_1 t, …` stabilizes at `t` for large `k` (the stub exports `AdmissibleSequence.exists_eventually_dist_zero`-style from GammaTwo — check; T finite means eventually `infDist t (A.seq k) = 0`... careful: `A.seq k ⊆ T` grows but need not contain `t`; the designed argument (read the stub's Proof-formalization notes) telescopes `X t − X t₀ = ∑_k (X (π_{k+1} t) − X (π_k t))` in the a.e./eventually sense OR uses the standard `2·infDist` chain-step bound `d(π_{k+1}t, π_k t) ≤ d(π_{k+1}t, t) + d(t, π_k t) ≤ infDist t (A.seq k+1) + infDist t (A.seq k) ≤ 2·(level-k term)`.
- Per-level union bound: pairs `(π_{k+1}t, π_k t)` range over ≤ `|A.seq (k+1)|·|A.seq k| ≤ 2^{2^{k+1}}·2^{2^k} ≤ 2^{2^{k+2}}`-ish; threshold `τ_k = 2^{k/2}(√(2log2)+1+u)·(chain-step)`: B1 tail per pair `≤ 2·exp(−τ²/(K·d)²)`; sum over k of `2^{2^{k+1}}·2·exp(−(2^k)(2log2)(1+u²)…)` telescopes to `≤ 2·exp(−u²)` (geometric domination: `(a+b+c)² ≥ a²+b²+c²` for nonnegs — `nlinarith` or the designed inflation; `Real.exp_add`, `Finset.sum_geometric_le`-flavored bounds, `ENNReal.tsum_le_of_sum_le`).
- Sum of thresholds over k = `(6+2u)·K·γ(A)`-shaped: `∑_k 2^{k/2}·(2·infDist-terms) ≤ 2·γ-series`, with the constant bookkeeping per the stub docstring (follow its frozen accounting: threshold factor `(6+2u)`).
- (2): integrate (1) via `TailToExpectation` (`∫ sup ≤ threshold(0) + ∫_{u>0} tail`): `6 + 2√π ≤ 10`. `⊤`-γ case: `le_top`-trivial (the statement is `ofReal(...) ≤ 10·K·γ(A)` — if γ(A) = ⊤, RHS = ⊤ unless `K = 0`; for `K = 0` increments force a.e.-constancy — check the stub's K=0 handling note; `SubGaussianIncrements.sub_ae_eq_zero` covers it).
- (3): `E sup X ≤ E sup |X − X_{t₀}|` under mean-zero (add-subtract `X t₀`, `∫ X t₀ = 0` via `hint`+`hmean`); then `iInf` over A (`le_iInf` + (2)).
- (4): descend from `ofReal` via `gammaTwo_lt_top_of_finite` (`ENNReal.ofReal_le_ofReal_iff`-family + `ENNReal.toReal` monotone).

## Fallback (graded)
If exactly one goal resists, it must be `generic_chaining_tail` (leave its single `sorry`). (2)(3)(4) MUST close (citing it).

## Constraints
- New Mathlib imports fine; StatLean imports only from `StatLean.ConcentrationInequalities.*`.
- `private` named helpers allowed; NO sorried `have`s, no `axiom`/`admit`, no new hypotheses.
- Keep all tags/docstrings intact.
- Commit: `git add StatLean/ConcentrationInequalities/Chaining/GenericChaining.lean && git commit -m "conc/hdp-chain-generic: close Thm 8.5.2"`.

## DONE means
`lake build StatLean.ConcentrationInequalities.Chaining.GenericChaining` exits 0; at most the designated fallback sorry remains; report closed sorries + key Mathlib lemmas.