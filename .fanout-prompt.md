# hdp-bundle-vc-support — SauerShelah + DimensionReduction + BinomialSumBound + SupRatApprox [HDP §8.3 support]
You are a Lean 4 proof-closure session on branch conc/hdp-bundle-vc-support of the StatLean project (Lean v4.29.1, pinned Mathlib 5e932f97 pre-built under .lake/packages/mathlib). Inside an srun allocation: plain `lake build <Target>`; NEVER `lake update`; never touch lakefile/manifest/toolchain. Iterate until GREEN and COMMIT BEFORE ENDING (wait for the final build, then commit).
CONTEXT: no CLAUDE.md/notes/tools here. Mathlib search: `grep -rn "<name>" .lake/packages/mathlib/Mathlib/` + exact?/apply?. REAL on this branch: VC/Defs + VC/HalfSpaces + VC/CoveringByVC spine + VC/LawOfLargeNumbers + existing SubGaussian/Hoeffding (batch-1). Statements FROZEN.
## Touch-set (ONLY these four; one at a time, building each)
1. StatLean/ConcentrationInequalities/ForMathlib/BinomialSumBound.lean — gate `...ForMathlib.BinomialSumBound`. (en/d)^d bound: add_pow truncation (Finset.sum_le_sum_of_subset_of_nonneg), (d/n)^k ≥ (d/n)^d for k ≤ d, Real.add_one_le_exp for (1+d/n)^n ≤ e^d.
2. StatLean/ConcentrationInequalities/ForMathlib/SupRatApprox.lean — gate `...ForMathlib.SupRatApprox`. ℝ→ℚ sup bridge for right-approximable bounded functions: Rat.exists_rat_btwn / exists_rat_gt density + ciSup squeeze (le_ciSup/ciSup_le with BddAbove).
3. StatLean/ConcentrationInequalities/VC/SauerShelah.lean — gate `...VC.SauerShelah`. Pajor via Mathlib Finset.card_le_card_shatterer on the traceFamily bridge (shatters_traceFamily_iff: our set-level Shatters ↔ Mathlib Finset.Shatters of the trace — keep Mathlib's dot-qualified); count trace-shattered sets: each is C-shattered (T.card ≤ d) and ⊆ Λ → Finset.powersetCard counting (Finset.card_powersetCard) → ∑_{k≤d} C(n,k) → file 1's closed form.
4. StatLean/ConcentrationInequalities/VC/DimensionReduction.lean — gate `...VC.DimensionReduction`. Lem 8.3.14: per-pair empFrac concentration via the EXISTING PROVEN hoeffding (read SubGaussian/Hoeffding.lean for its exact ℕ-indexed signature) on indicator variables (∈[0,1] ⇒ IsSubGaussian proxy (1/2)² via existing isSubGaussian_of_mem_Icc); one-sided at deviation (3/4)ε²-shaped per stubs; union bound over ≤ |F|² pairs; probability accounting to 0.99 per the frozen docstring numerals.
## Fallbacks (≤ ONE per file, docstring-designated): sum_choose_le_exp_mul_pow / iSup_eq_iSup_rat_of_right_approx / card_traceFamily_le_sum_choose / dimension_reduction.
## Constraints
No axiom/admit; no new hypotheses; keep tags; private helpers OK.
Commit: git add StatLean/ConcentrationInequalities/ && git commit -m "conc/hdp-bundle-vc-support: close VC support layer"
## DONE means
All four targets exit 0 (WAIT); ≤ 4 designated fallbacks; report per-file closures.