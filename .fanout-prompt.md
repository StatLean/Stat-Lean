# hdp-contraction — Theorem 6.6.1, contraction principle [HDP §6.6]

You are a Lean 4 proof-closure session on branch `conc/hdp-contraction` of the StatLean project (Lean v4.29.1, pinned Mathlib `5e932f97` pre-built under `.lake/packages/mathlib`). You are inside an srun allocation on FAS-RC: run plain `lake build <Target>`; NEVER `lake update`; never touch `lakefile.lean` / `lake-manifest.json` / `lean-toolchain`. Iterate until GREEN.

CONTEXT: this worktree has no CLAUDE.md/notes/tools (gitignored). Everything you need is in this prompt + the stub files. Mathlib search: `grep -rn "<name>" .lake/packages/mathlib/Mathlib/` and in-proof `exact?` / `apply?` / `rw?`. Upstream StatLean lemmas in OTHER files are `sorry`-backed statements — cite them as black boxes; only YOUR file must become sorry-free.

## Touch-set (edit ONLY this file)
`StatLean/ConcentrationInequalities/Symmetrization/Contraction.lean`
Do NOT edit any other file. Do NOT change any statement, hypothesis, constant, or tag. If a statement looks unprovable AS STATED, stop and report precisely why — do not weaken it.

## Build gate
`lake build StatLean.ConcentrationInequalities.Symmetrization.Contraction` — exit 0.

## The goal — close these sorries (read the file for exact statements)
1. `contraction_principle_of_abs_le_one` — the normalized core: for fixed vectors `x i` in a normed space and coefficients `|a i| ≤ 1`, `∫ ‖∑ a i • s i • x i‖ ∂(signVec n) ≤ ∫ ‖∑ s i • x i‖ ∂(signVec n)`-shaped (read the exact stub statement; fallback — see below).
2. `contraction_principle` — HDP Theorem 6.6.1 packaging with `‖a‖_∞` scaling.

READ FIRST (toolbox; sorry-backed citable): `Symmetrization/Rademacher.lean` (`radLaw`, `signVec n = Measure.pi (fun _ => radLaw)`, `signVec_ae_pm`, `signVec_map_mul_pm`, instances, `radLaw_integral`).

## Proof roadmap (HDP p. 184 — the convexity/extreme-point argument)
- Define `f : (Fin n → ℝ) → ℝ`, `f a = ∫ ‖∑ i, a i • (s i • x i)‖ ∂(signVec n)`-shaped (as a function of the COEFFICIENT vector, signs integrated out). `f` is a finite integral of norms of affine functions of `a` ⇒ CONVEX (`ConvexOn`: norm of affine is convex — `convexOn_norm`-composition: for each fixed sign vector `s`, `a ↦ ‖∑ aᵢ sᵢ xᵢ‖` is a composition of the linear map `a ↦ ∑ aᵢsᵢxᵢ` with `‖·‖` — `(convexOn_norm _).comp_linearMap`-style; then integrating preserves convexity: finite sum/integral of convex is convex — for the PRODUCT measure over finitely many ±1 atoms, the integral IS a finite weighted sum (`radLaw` is two Diracs!): expand via `signVec`-integral-as-finite-sum if the stub provides it, else via `MeasureTheory.integral_fintype`-flavored decomposition of `Measure.pi` over two-point factors — check `Rademacher.lean` exports; `radLaw_integral f = (f 1 + f (−1))/2` iterated).
- Maximum of a convex function on the cube `[−1,1]ⁿ` is attained at a VERTEX (`a ∈ {−1,1}ⁿ`): induction on coordinates using one-dimensional convexity (`ConvexOn.le_max_of_mem_segment`-style: for each coordinate, `f(…, aᵢ, …) ≤ max f(…,1,…) f(…,−1,…)` since `aᵢ ∈ [−1,1] = segment` — `ConvexOn` on the segment + endpoints; iterate over `Fin n` by induction/`Finset.induction`).
- At a vertex `ε ∈ {−1,1}ⁿ`: `∑ εᵢsᵢxᵢ` with `s ~ signVec` is EQUIDISTRIBUTED with `∑ sᵢxᵢ` (`signVec_map_mul_pm` — the flip-invariance of the product sign measure). Hence `f(ε) = f(1)` and `f(a) ≤ f(1)` for all `|aᵢ| ≤ 1`.
- Packaging: general `a` with `‖a‖_∞ = M > 0`: apply the core to `a/M` and pull `M` out by norm-homogeneity (`norm_smul`, `integral_const_mul`); `M = 0` degenerate (all `aᵢ = 0`, LHS = 0). Mind the sup-norm carrier in the stub (`Pi.norm`-based? read it; `Pi.norm_le_iff`/`norm_le_pi_norm` for the `|aᵢ| ≤ ‖a‖` extraction).

## Fallback (graded)
If exactly one goal resists, it must be `contraction_principle_of_abs_le_one` (leave its single `sorry`); the packaging must still be derived FROM it.

## Constraints
- New Mathlib imports fine; StatLean imports only from `StatLean.ConcentrationInequalities.*`.
- `private` named helpers allowed; NO sorried `have`s, no `axiom`/`admit`, no new hypotheses.
- Keep all tags/docstrings intact.
- Commit: `git add StatLean/ConcentrationInequalities/Symmetrization/Contraction.lean && git commit -m "conc/hdp-contraction: close Thm 6.6.1"`.

## DONE means
`lake build StatLean.ConcentrationInequalities.Symmetrization.Contraction` exits 0; at most the designated fallback sorry remains; report closed sorries + key Mathlib lemmas.