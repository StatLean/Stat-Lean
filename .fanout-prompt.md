# hdp-covering-by-vc — Theorem 8.3.13, covering numbers via VC dimension [HDP §8.3.4–8.3.5]

You are a Lean 4 proof-closure session on branch `conc/hdp-covering-by-vc` of the StatLean project (Lean v4.29.1, pinned Mathlib `5e932f97` pre-built under `.lake/packages/mathlib`). You are inside an srun allocation on FAS-RC: run plain `lake build <Target>`; NEVER `lake update`; never touch `lakefile.lean` / `lake-manifest.json` / `lean-toolchain`. Iterate until GREEN.

CONTEXT: this worktree has no CLAUDE.md/notes/tools (gitignored). Everything you need is in this prompt + the stub files. Mathlib search: `grep -rn "<name>" .lake/packages/mathlib/Mathlib/` and in-proof `exact?` / `apply?` / `rw?`. Upstream StatLean lemmas in OTHER files are `sorry`-backed statements — cite them as black boxes (by design); only YOUR file must become sorry-free.

## Touch-set (edit ONLY this file)
`StatLean/ConcentrationInequalities/VC/CoveringByVC.lean`
Do NOT edit any other file. Do NOT change any statement, hypothesis, constant, or tag. If a statement looks unprovable AS STATED, stop and report precisely why — do not weaken it.

## Build gate
`lake build StatLean.ConcentrationInequalities.VC.CoveringByVC` — exit 0.

## The goal — close the file's sorries (read it for exact statements)
The workhorse `sepFamily_card_le_of_vcDim_le` (every ε-separated finite subfamily in the `empFrac`/`μ.real (S ∆ T)` squared-distance has card ≤ (2/ε)^{21d}), the maximal-separated-family/covering conclusion (Theorem 8.3.13-shaped export consumed by `VC/LawOfLargeNumbers.lean`), the private avg-Dirac empirical-measure lemmas, and the pure-real footnote-4 algebra `card_le_pow_of_entropy_bound` (fallback; see below).

READ FIRST (toolbox; sorry-backed citable): `VC/Defs.lean` (proven: `Shatters`, `vcDim`, `traceFamily`, `empFrac` + API), `VC/SauerShelah.lean` (`card_traceFamily_le_sum_choose` + growth bounds), `ForMathlib/BinomialSumBound.lean` (`sum_choose_le_exp_mul_pow`, (en/d)^d), `VC/DimensionReduction.lean` (`dimension_reduction`: ε-separated in L²(μ) stays ε/2-separated in L²(μₙ) w.p. ≥ 0.99 for n ≥ Cε⁻⁴log|F|), existing proven `SubGaussian/Hoeffding.lean`.

## Proof roadmap (HDP pp. 238–239, proof of Theorem 8.3.13; the stub docstrings carry the split plan)
- Separated-family bound: let `F` be ε-separated (all pairwise `μ.real (S ∆ T) > ε²`-style — read the stub's exact separation convention). Dimension reduction (cite `dimension_reduction`) gives a sample `x : Fin n → Ω`, `n ≈ ⌈100·ε⁻⁴·log |F|⌉`, on which members stay ε/2-separated in the empirical distance — in particular PAIRWISE DISTINCT traces on the sample points (`empFrac_pos_iff` extracts a distinguishing point from positive empirical symmetric difference).
- Count: distinct traces on an n-point set are ≤ `traceFamily`-card ≤ `∑_{k≤d} C(n,k)` (Sauer–Shelah via the trace bridge) ≤ `(e·n/d)^d` (BinomialSumBound). So `|F| ≤ (e·n/d)^d` with `n` depending on `log |F|`.
- Footnote-4 algebra (`card_le_pow_of_entropy_bound`): from `∀ n, 100ε⁻⁴·log N ≤ n → N ≤ (e·n/d)^d` conclude `N ≤ (2/ε)^{21d}` — pure-real `rpow`/log manipulation; square `N ≤ A^d·√N ⇒ N ≤ A^{2d}` (`sq_sqrt`) to avoid fractional powers where the stub's plan says so.
- Covering conclusion: a MAXIMAL ε-separated family exists because cardinalities are bounded (no Zorn: `Set.Finite`/`Finset` maximal-cardinality choice over a bounded ℕ set — the stub's plan), and maximality ⇒ its members ε-cover the class.
- Probability side: `dimension_reduction`'s 0.99 probability only needs EXISTENCE of one good sample (`0.99 > 0` ⇒ the event is nonempty — extract a witness via `MeasureTheory.measure_pos` ⇒ `Set.Nonempty`).

## Fallback (graded)
If exactly one lemma resists, it must be `card_le_pow_of_entropy_bound` (the pure-real algebra; leave its single `sorry`). The probabilistic/combinatorial spine MUST close.

## Constraints
- New Mathlib imports fine; StatLean imports only from `StatLean.ConcentrationInequalities.*`.
- `private` named helpers allowed; NO sorried `have`s, no `axiom`/`admit`, no new hypotheses.
- Keep all tags/docstrings intact.
- Commit: `git add StatLean/ConcentrationInequalities/VC/CoveringByVC.lean && git commit -m "conc/hdp-covering-by-vc: close Thm 8.3.13"`.

## DONE means
`lake build StatLean.ConcentrationInequalities.VC.CoveringByVC` exits 0; at most the designated fallback sorry remains; report closed sorries + key Mathlib lemmas.