# hdp-bundle-emp-tail — LipschitzNet + LipschitzCovering + SubGaussianIncrements + LipschitzDense [HDP §8.2 support]
You are a Lean 4 proof-closure session on branch conc/hdp-bundle-emp-tail of the StatLean project (Lean v4.29.1, pinned Mathlib 5e932f97 pre-built under .lake/packages/mathlib). Inside an srun allocation: plain `lake build <Target>`; NEVER `lake update`; never touch lakefile/manifest/toolchain. Iterate until GREEN and COMMIT BEFORE ENDING (wait for the final build, then commit).
CONTEXT: no CLAUDE.md/notes/tools here. Mathlib search: grep over .lake/packages/mathlib + exact?/apply?. REAL on this branch: EmpiricalProcess/{Defs(complete), LipschitzApprox, LipschitzLLN} + all Orlicz bridges + Chaining. Statements FROZEN (LipschitzLLN was proven against them).
## Touch-set (ONLY these four; one at a time, building each)
1. StatLean/ConcentrationInequalities/EmpiricalProcess/LipschitzNet.lean — gate `...EmpiricalProcess.LipschitzNet`. Cone-envelope net lemmas: lipschitzWith_coneFun (finite sup of 1-Lipschitz cones via LipschitzWith.max induction — the stub's lipschitzWith_finset_sup' helper), coneFun range ⊆ [0,1] (min/max clamping), zero_mem_lipschitzNet (k₀ = 0, all-decreasing steps), card bounds ((m+1)·3^m counting via Fintype.card products).
2. StatLean/ConcentrationInequalities/EmpiricalProcess/LipschitzCovering.lean — gate `...EmpiricalProcess.LipschitzCovering`. Ex 8.9 assembly: IsEpsilonNet packaging of LipschitzApprox (REAL) + coveringNumber ≤ card (Maximal/CoveringNumbers API, batch-1 proven) + (m+1)·3^m ≤ e^{12/ε} arithmetic (Real.log/exp algebra + Nat.cast) + subset-monotone toNat exports.
3. StatLean/ConcentrationInequalities/EmpiricalProcess/SubGaussianIncrements.lean — gate `...EmpiricalProcess.SubGaussianIncrements`. Empirical increments: per-summand (f−g)∘Xᵢ ∈ [−δ,δ] ⇒ existing hasSubgaussianMGF_of_mem_Icc/isSubGaussian_of_mem_Icc (batch-1) + Mathlib HasSubgaussianMGF.sum_of_iIndepFun (iid via hindep.comp) → proxy δ²-shaped; B3 (REAL) to the ψ₂ norm; the Set.univ SubGaussianIncrements packaging; mean-zero of increments via map-law transfer (integral_map).
4. StatLean/ConcentrationInequalities/EmpiricalProcess/LipschitzDense.lean — gate `...EmpiricalProcess.LipschitzDense`. Deterministic R1/R2/R3 pointwise iSup identities: scaling bijection (L•g), shift invariance (Defs lemmas, REAL), monotone finite exhaustion vs the net union (dist f net ≤ 3/m via LipschitzApprox REAL + abs_empiricalProcess_sub_le from Defs REAL ⇒ per-ω sup equality via ciSup squeeze: le_ciSup/ciSup_le + epsilon-of-room).
## Fallbacks (≤ ONE per file, docstring-designated only).
## Constraints
No axiom/admit; no new hypotheses; keep tags; private helpers OK.
Commit: git add StatLean/ConcentrationInequalities/EmpiricalProcess/ && git commit -m "conc/hdp-bundle-emp-tail: close empirical support layer"
## DONE means
All four targets exit 0 (WAIT); ≤ 4 designated fallbacks; report per-file closures.