# dinf-tail: faithful general-T high-probability Dudley (Eq 8.15)

TOUCH-SET (only this file):
- StatLean/ConcentrationInequalities/Chaining/DudleyTail.lean (3 sorries:
  general `dudley_tail_three_term`, `dudley_tail`,
  `dudley_tail_three_term_countable` in the "Faithful general forms" section)

The finite template `dudley_tail_three_term_of_finite` (same file, complete
proof) is your blueprint. Frozen upstream statements you consume (possibly
still sorried elsewhere — they typecheck): `exists_finset_net_of_cov_ne_top`,
`isBounded_of_coveringNumber_ne_top` (DyadicNets.lean); `residual_tail_le`
(SubsetChaining.lean); `sum_window_le_dudleySum` needs Summable — you have
`hS` directly, so use `Summable.sum_le_tsum` where the template used the
hfin-powered window lemma; `dudleyLSum_eq_ofReal_of_summable`,
`dudleyLSum_le_two_mul_dudleyLIntegral`,
`ofReal_diam_mul_sqrt_log_two_le_four_mul_dudleyLSum` (EntropySum.lean).

## Roadmap `dudley_tail_three_term` (general, per-F)

Structure of the finite template: per-level bad events over close pairs of
consecutive T-nets, each with probability `2·e^{-(j+1)}·e^{−u²}`-shaped,
summing (geometric `sum_exp_neg_succ_le_one`) to `≤ 2/(e−1)·e^{−u²}`; on the
good event, telescoping gives the pair-sup ≤ 2·(anchored one-sided sup) ≤
2·Σ τ_j ≤ thr. General version:
1. Same nets of T (`exists_finset_net_of_cov_ne_top` + hcov; coarse scale
   from `isBounded_of_coveringNumber_ne_top`), same per-level thresholds
   τ_j and bad events, but the chain STOPS at an arbitrary level n and the
   good-event bound is over the FINITE SUBSET F:
   on (⋂ levels ≤ n good) ∩ {residual ≤ δ}:
   `max over F×F |X_s − X_t| ≤ 2·(Σ_{j≤n} τ_j + δ)`.
   The `Σ_{j≤n} τ_j ≤ (9·dudleySum + …)/2`-side arithmetic is the
   template's, with `Summable.sum_le_tsum … hS` replacing the hfin window
   lemma (the τ series and its ≤ dudleySum comparisons are unchanged).
2. Union bound: template's per-level sum PLUS the residual event from
   `residual_tail_le` (net radius ε_n, threshold δ):
   `μ{thr + 2δ < max_F×F} ≤ 2/(e−1)·e^{−u²} + 2|F|·exp(−δ²/(Kε_n)²)`.
   K = 0: handle as in the template's degenerate branch (all increments
   a.e. 0). For K > 0 and FIXED δ > 0, let n → ∞: `ε_n → 0` so the residual
   probability → 0 (`Real.exp_atBot`-style: exponent → −∞; or bound by any
   summable dominating sequence and `tendsto_of_tendsto_of_tendsto_of_le_of_le`).
   Since the EVENT does not depend on n, `μ{thr + 2δ < …} ≤ inf over n of
   the RHS = 2/(e−1)·e^{−u²} ≤ 2e^{−u²}` (`ge_of_tendsto'` on the constant
   sequence, or `le_of_forall_lt_iff_le` with the explicit limit).
3. δ ↓ 0: the events `{thr + 2δ_m < max}` for `δ_m := 1/(m+1)` increase to
   `{thr < max}` (strict `<` unions correctly:
   `thr < x ↔ ∃ m, thr + 2/(m+1) < x` — `exists_nat_one_div_lt`-style);
   `μ` continuity from below (`MeasureTheory.tendsto_measure_iUnion_atTop`
   or `measure_iUnion_eq_iSup` for a monotone family) gives
   `μ{thr < max} = ⨆ m, μ{…} ≤ 2e^{−u²}`.

## Roadmap `dudley_tail` (display, general)

Mirror `dudley_tail_of_finite` (same file): event inclusion — the display
threshold dominates the three-term threshold:
`9·dudleySum + 17·diam + 12u·diam ≤ 200·((dudleyLIntegral T D).toReal + u·diam)`
using `dudleySum ≤ 2·(dudleyLIntegral T D).toReal` (from
`dudleyLSum_eq_ofReal_of_summable hS` + `dudleyLSum_le_two_mul_dudleyLIntegral`
+ `ENNReal.toReal_mono hDL`-style bridging: `ofReal (dudleySum) ≤ 2·L` with
`L ≠ ⊤` gives the real inequality via `ENNReal.ofReal_le_iff_le_toReal`) and
`diam·√log2 ≤ 8·(dudleyLIntegral T D).toReal` (from the ℝ≥0∞ absorber +
`dudleyLSum ≤ 2·dudleyLIntegral` + the same toReal bridge; then the
template's `nlinarith` with `√log2 ≥ 0.83`-style bounds — copy its `key`
block, swapping `dudleyIntegral T D` for `(dudleyLIntegral T D).toReal`).
Then `measure_mono` into the general `dudley_tail_three_term`.

## Roadmap `dudley_tail_three_term_countable`

Enumerate T (`Set.Countable.exists_eq_range` on hne), exhaustion
`S n := insert hne.some ((Finset.range n).image f)` with `↑(S n) ⊆ T`.
Event identification: `{thr < ⨆ t ∈ T, ⨆ s ∈ T, |X t ω − X s ω|}` equals
`⋃ n, {thr < max over (S n) ×ˢ (S n)}`: thr ≥ 0 (its factors are
nonneg — `dudleySum_nonneg`, diam_nonneg, hu; K ≥ 0), so
`thr < biSup ↔ ∃ t s ∈ T, thr < |X t ω − X s ω|` (the junk-0 branches of
the real biSup cannot exceed thr ≥ 0 — mirror the `hsub` device in
`tail_max_finset_le`, FinsetMaximal.lean), and each witness pair lands in
some S n; conversely `max over (S n)² ≤ biSup` via `le_biSup_finset`-style
bounds (each |X t − X s| with t,s ∈ T is ≤ the double biSup — two nested
`le_biSup` steps; mind junk: values are abs, nonneg, so inner/outer biSups
dominate members — mirror `hG2H`'s `htc/hsc` steps in this file).
The union is MONOTONE (S n ⊆ S (n+1)); measure continuity from below
(`tendsto_measure_iUnion_atTop` / `measure_iUnion_eq_iSup`) + the general
per-F theorem at F := S n (hF := the exhaustion inclusion) closes it.

FALLBACK (single named sorry): `dudley_tail_three_term_countable`. The
per-F three-term and display forms MUST close.

DONE: at most the fallback sorry remains in DudleyTail.lean; module + area
builds error-free.
## Common rules (all dinf sessions)

You are a Lean 4 (v4.29.1, Mathlib pinned) proof-closure session for the StatLean
library, working in a git worktree of the Stat-Lean repo on your own branch. The
statements you must prove are ALREADY STUBBED with `sorry` — they are FROZEN
CONTRACTS. You may NOT change any statement, hypothesis, constant, definition, or
docstring of a stubbed declaration; you may NOT touch files outside your touch-set;
you may NOT use `axiom`, `admit`, or add new hypotheses. If you become convinced a
frozen statement is FALSE or unprovable as stated, leave its `sorry` in place and
write a precise blocker report (what corner breaks, with a counterexample sketch) in
your final commit message — do NOT weaken or alter the statement.

Build discipline: iterate with `lake build StatLean.ConcentrationInequalities.<YourModule>`
(fast); before finishing run `lake build StatLean.ConcentrationInequalities` — it must
compile with NO errors (sorry warnings from files outside your touch-set are expected;
your own files must have 0 sorries except, if truly stuck, your single designated
fallback below). Commit early and often with clear messages; your session auto-commits
and pushes at close.

Mathlib search: you have no network tools. Use `exact?`/`apply?`/`rw?` in-proof, and
`grep -rn "<pattern>" .lake/packages/mathlib/Mathlib/` for names. The repo's own
lemmas: grep under StatLean/.

House gotchas: real `⨆ i ∈ s, f i` over a Finset carries junk `sSup ∅ = 0` from
out-of-set indices — the junk-free finite max is `Finset.sup'`; convert with
`biSup_finset_eq_sup'` (needs the family nonneg, e.g. `fun _ _ => abs_nonneg _`) or
`biSup_finset_eq_sup'_of_sup'_nonneg` (needs `0 ≤ sup'`), and `le_biSup_finset` for
the unconditional member bound (all in Chaining/FinsetMaximal.lean). `simp only` does
not β-reduce after `rw [hSdef]`-style set-unfolds reliably — `dsimp only` first when
rewriting under `(fun x => ...) y` redexes. `Finset.sup'_cons` carries the tail's
Nonempty only on its RHS — supply it explicitly. Never write the two-character
sequence `-` `/` inside a docstring. Keep lines ≤ 100 chars.