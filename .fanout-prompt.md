# hdp-half-spaces — Example 8.3.5 / Exercise 8.17: vc(half-spaces in ℝᵐ) = m+1 [HDP §8.3.1]

You are a Lean 4 proof-closure session on branch `conc/hdp-half-spaces` of the StatLean project (Lean v4.29.1, pinned Mathlib `5e932f97` pre-built under `.lake/packages/mathlib`). Inside an srun allocation: plain `lake build <Target>`; NEVER `lake update`; never touch lakefile/manifest/toolchain. Iterate until GREEN and COMMIT BEFORE ENDING (wait for the final build, then commit).

CONTEXT: no CLAUDE.md/notes/tools here. Mathlib search: `grep -rn "<name>" .lake/packages/mathlib/Mathlib/` + `exact?`/`apply?`. Upstream StatLean lemmas may be sorry-backed — cite as black boxes. `VC/Defs.lean` (Shatters, vcDim, mono lemmas) is fully proven.

## Touch-set (edit ONLY this file)
`StatLean/ConcentrationInequalities/VC/HalfSpaces.lean`
No statement/hypothesis/constant/tag changes. If unprovable AS STATED, stop and report.

## Build gate
`lake build StatLean.ConcentrationInequalities.VC.HalfSpaces` — exit 0.

## The goal
Close the file's sorries (read it): the shattered-witness lower bound, the Radon upper bound `not_shatters_halfSpaceClass` (this item's designated fallback per the docstring), the `vcDim_halfSpaceClass` assembly, and the ℝ² half-plane corollary (Example 8.3.3, `m = 2` instance).

## Proof roadmap (HDP pp. 233–234 + Exercise 8.17)
- LOWER (must close): the witness config from the stub docstring (0 and the basis vectors, or the stub's exact choice — read it): for any labeling `T ⊆ Λ`, exhibit `(a, b)` with `Λ ∩ {x | ⟨a,x⟩ ≤ b} = T` — explicit inner-product algebra on `EuclideanSpace ℝ (Fin m)` (`EuclideanSpace.single`, `inner_single_left`, case analysis on membership; choose `a = ∑_{i ∈ T'} eᵢ`-style with threshold separating 0/1 sums — follow the docstring's recorded construction).
- UPPER (fallback): no `(m+2)`-point set is shattered — Radon: any `m+2` points in ℝᵐ admit a partition `I ⊔ J` with intersecting convex hulls (`radon_partition`, pin-verified in `Mathlib.Analysis.Convex.Radon` — grep for the exact name/signature: it may be stated for `AffineIndependent`-negation or raw finite families; adapt). If a half-space contained exactly `I` (labeling `T = I`), convexity of half-spaces (`convex_halfSpace_le`-family — grep `Convex` + `halfSpace`/`{x | inner a x ≤ b}` lemmas; or prove convexity inline: linear + `Convex.inter`) would put the common hull point on both sides — contradiction (`Set.mem_of_subset_of_mem` on hulls: `convexHull_min`/`Convex.convexHull_subset_iff`).
- Assembly: `vcDim = m+1` from `Shatters.card_le_vcDim` (lower witness card `m+1`) + `vcDim_le_iff` (upper: any shattered `Λ` has `card ≤ m+1` — else a `(m+2)`-subset is shattered by `Shatters.mono_right`, contradicting the upper bound). ℕ∞ arithmetic: `Nat.cast` order lemmas.
- ℝ² corollary: instantiate `m := 2`.

## Fallback (graded)
Only `not_shatters_halfSpaceClass` (the Radon upper bound). The lower-bound witness and the assembly (citing the fallback) MUST close.

## Constraints
No `axiom`/`admit`; no new hypotheses; keep tags; `private` named helpers OK.
Commit: `git add StatLean/ConcentrationInequalities/VC/HalfSpaces.lean && git commit -m "conc/hdp-half-spaces: close Ex 8.3.5"`.

## DONE means
Target builds exit 0 (WAIT for it); ≤ 1 designated fallback sorry; report closures + key lemmas.