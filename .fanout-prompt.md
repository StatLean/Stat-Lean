# hdp-orlicz-subgaussian-mgf — B2 bridge: ψ₂ norm ⇒ HasSubgaussianMGF (C = 3) [HDP Prop 2.6.1 (iii)⇒(iv)]

You are a Lean 4 proof-closure session on branch `conc/hdp-orlicz-subgaussian-mgf` of the StatLean project (Lean v4.29.1, pinned Mathlib `5e932f97` pre-built under `.lake/packages/mathlib`). You are inside an srun allocation on FAS-RC: run plain `lake build <Target>`; NEVER `lake update`; never touch `lakefile.lean` / `lake-manifest.json` / `lean-toolchain`. Iterate until GREEN.

CONTEXT: this worktree has no CLAUDE.md/notes/tools (gitignored). Everything you need is in this prompt + the stub files. Mathlib search: `grep -rn "<name>" .lake/packages/mathlib/Mathlib/` and in-proof `exact?` / `apply?` / `rw?`. Upstream StatLean lemmas in OTHER files are `sorry`-backed statements — cite them as black boxes; only YOUR file must become sorry-free.

## Touch-set (edit ONLY this file)
`StatLean/ConcentrationInequalities/Orlicz/SubGaussianMGF.lean`
Do NOT edit any other file. Do NOT change any statement, hypothesis, constant, or tag. If a statement looks unprovable AS STATED, stop and report precisely why — do not weaken it.

## Build gate
`lake build StatLean.ConcentrationInequalities.Orlicz.SubGaussianMGF` — exit 0.

## The goal — close the file's sorries (read it for exact statements)
The raw core (book-literal condition `∫⁻ ofReal (exp ((X/K)²)) ≤ 2` + mean-zero ⇒ `E exp(λX) ≤ exp(3λ²K²/2)`-shaped MGF bound / `HasSubgaussianMGF X (3*K²) μ`), the norm-facing wrapper `hasSubgaussianMGF_of_subGaussianNorm_le` (via the attainment lemma), the `IsSubGaussian X (3*K²) μ` corollary, and the integrability helper (this file's fallback).

## Proof roadmap (HDP p. 37, (iii)⇒(iv); book constant K₄ = √(3/2)·K₃, i.e. proxy 3K² in Mathlib's exp(λ²c/2) convention)
- The book's argument: for |λK| ≤ 1 use `e^u ≤ u + e^{u²}` pointwise (u = λX): `E e^{λX} ≤ λ·E X + E e^{λ²X²} ≤ 0 + E e^{X²/K²}` (since λ² ≤ 1/K²... careful: needs `E e^{λ²X²} ≤ (E e^{X²/K²})^{λ²K²}`-style interpolation OR the cruder `λ²X² ≤ X²/K²` when |λ| ≤ 1/K giving `E ≤ 2 ≤ e^{3λ²K²/2}` only when 3λ²K²/2 ≥ log 2, i.e. |λ| ≥ √(2log2/3)/K — SPLIT REGIMES): for |λ| ≤ 1/K combine `e^u ≤ u + e^{u²}` with Jensen-free bound `E e^{λ²X²} ≤ (E e^{X²/K²})^{λ²K²} ≤ 2^{λ²K²} ≤ e^{λ²K²·log2}` — the power-interpolation needs `lintegral`-Hölder (`ENNReal.lintegral_rpow`-flavored) OR use the elementary route: `e^{λ²x²} ≤ 1 + λ²K²·(e^{x²/K²} − 1)` for λ²K² ≤ 1 (convexity of `t ↦ a^t` / `Real.rpow` in the exponent — check `Real.add_pow_le_pow_mul_pow_of_sq_le_sq`? simpler: for `0 ≤ θ ≤ 1`, `e^{θ·v} ≤ 1 + θ(e^v − 1)` by convexity of exp on [0,v] — `Real.add_pow_le…` no: use `Real.exp_le_one_add_of...`? The named brick is in `ForMathlib/ExpTaylorBounds.lean` — READ IT: the stub cluster stocked exactly these elementary exponential-Taylor inequalities (e.g. `exp_le_one_add_add_sq_half_of_nonpos`, θ-interpolation bounds). Cite them (sorry-backed fine).) Then `E e^{λX} ≤ 0 + 1 + λ²K²·(2−1)·… ≤ e^{2λ²K²}`-flavored; for |λ| > 1/K use `λx ≤ λ²K²/2 + x²/(2K²)` (AM-GM, `nlinarith [sq_nonneg (λ*K - x/K)]`): `E e^{λX} ≤ e^{λ²K²/2}·E e^{X²/(2K²)} ≤ e^{λ²K²/2}·√(E e^{X²/K²}) ≤ e^{λ²K²/2}·√2 ≤ e^{λ²K²/2 + log2/2} ≤ e^{3λ²K²/2}` (since λ²K² > 1 ≥ log2). The `√(E e^{X²/K²})` step is Cauchy–Schwarz/Jensen on `∫ e^{X²/(2K²)}` — `MeasureTheory` Hölder: `lintegral_rpow_le` names or `ENNReal.lintegral_mul_le_Lp_mul_Lq`; or elementary `e^{v/2} ≤ (1+e^v)/2` (AM-GM on `e^{v/2}·1`): `E e^{X²/(2K²)} ≤ (1 + 2)/2 = 3/2 ≤ e^{λ²K²/2}`… — MANY elementary routes work; the docstring's Proof-formalization notes record the designed one. Follow the file's notes first; any correct route landing the stated `3*K^2` is acceptable.
- Mathlib target predicate: `ProbabilityTheory.HasSubgaussianMGF X c μ` = `∀ λ, mgf X μ λ ≤ exp (λ²c/2)` + integrability side conditions (grep its definition in `Mathlib/Probability/Moments/SubGaussian.lean` and construct it field-by-field or via its constructor lemmas).
- Integrability (fallback lemma): `∫⁻ ofReal(exp((X/K)²)) ≤ 2` ⇒ `Integrable (fun ω => exp (λ * X ω)) μ` for every λ — via `λx ≤ λ²K²/2 + (x/K)²/2… ≤ const + (x/K)²` and `integrable` domination by the finite lintegral (convert with `integrable_of_lintegral_ofReal_lt_top`-style: grep `lintegral_lt_top` ↔ `Integrable` bridges, `MeasureTheory.integrable_of_le` + nonneg).
- Wrapper: `subGaussianNorm X μ ≤ K` + attainment (`lintegral_exp_sq_le_two_of_subGaussianNorm_le` from `Orlicz/Attainment.lean`, sorry-backed citable) gives the raw condition at K; apply the core. `IsSubGaussian` corollary: `IsSubGaussian` (see `SubGaussian/Defs.lean`, proven) is `HasSubgaussianMGF` of the centered variable; under `hmean : ∫ X = 0` centering is the identity — `HasSubgaussianMGF.congr`-style transport (`sub_zero`).

## Fallback (graded)
If exactly one lemma resists, it must be the integrability helper named in the file (read its docstring: `integrable_exp_mul_of_lintegral_exp_sq_le_two`). The MGF core and both wrappers MUST close.

## Constraints
- New Mathlib imports fine; StatLean imports only from `StatLean.ConcentrationInequalities.*`.
- `private` named helpers allowed; NO sorried `have`s, no `axiom`/`admit`, no new hypotheses.
- Keep all tags/docstrings intact.
- Commit: `git add StatLean/ConcentrationInequalities/Orlicz/SubGaussianMGF.lean && git commit -m "conc/hdp-orlicz-subgaussian-mgf: close B2 bridge"`.

## DONE means
`lake build StatLean.ConcentrationInequalities.Orlicz.SubGaussianMGF` exits 0; at most the designated fallback sorry remains; report closed sorries + key Mathlib lemmas.