# hdp-debt-dudley-abs — close the single debt discrete_dudley_abs [HDP]
You are a Lean 4 single-debt session on branch conc/hdp-debt-dudley-abs of the StatLean project (Lean v4.29.1, pinned Mathlib 5e932f97 pre-built under .lake/packages/mathlib). Inside an srun allocation: plain lake build; NEVER lake update; never touch lakefile/manifest/toolchain. You have ~3.5h for ONE lemma — iterate to GREEN, then COMMIT (wait for the final build). No sub-agents, no parallel orchestration: work the proof yourself, directly, and VERIFY EVERY INTEGRATION STEP WITH A BUILD (a previous attempt integrated unverified code and died at the gate).
CONTEXT: no CLAUDE.md/notes/tools here. Mathlib search: grep over .lake/packages/mathlib + exact?/apply?. EVERYTHING in StatLean/ConcentrationInequalities is REAL except 4 sorries; yours is discrete_dudley_abs in Chaining/DiscreteDudley.lean. Statement FROZEN; only the sorry body (+ private helpers in the same file) may change. NOTE: a broken previous attempt at this proof exists on branch conc/hdp-debt-tranche2 (git show cannon/conc/hdp-debt-tranche2:StatLean/ConcentrationInequalities/Chaining/DiscreteDudley.lean if fetched) — its SKELETON was sound (kappa-prime selection + re-anchoring) but it referenced integrable_biSup_sub before its declaration point, had two type mismatches and two positivity failures; you may mine it for structure but rebuild the proof verifying each step.
## Debt A — Chaining/DiscreteDudley.lean: discrete_dudley_abs (constant 20)
Prev assessment: needs the 𝒩-based threshold scale κ' = max{k : 𝒩(T,2^{-k}) = 1} + re-anchoring. ALL inputs REAL: DyadicNets (nets, scales, projections), EntropyIntegrand (vanishing: 𝒩 = 1 for ε ≥ diam, antitonicity), EntropySum (window sums, dudleySum summability), PsiTwoMaximal (expectation_abs_max_le), TailToExpectation (first-moment integration: E|Y| ≤ √π·K·d via the gaussian tail), Maximal/CoveringNumbers (coveringNumber_anti). Build κ'-selection as a private Int-valued max (bounded above: 𝒩 ≥ 2 below the min-positive-distance scale — T finite nonempty; use Int.exists greatest via bounded set or Nat.find on the shifted index). Then: chain from κ' (all levels have N_k ≥ 2 ⇒ √log(2N_k²) ≤ √3√log N_k per docstring), one re-anchor term E|X_{t₀'} − X_{t₀}| ≤ √π·K·diam ≤ (docstring: absorbed via diam·√log2 ≤ 4·dudleySum). Follow the module docstring's frozen accounting to land ≤ 20. Gate `lake build StatLean.ConcentrationInequalities.Chaining.DiscreteDudley`.
## Touch-set: StatLean/ConcentrationInequalities/Chaining/DiscreteDudley.lean ONLY.
## DONE: lake build StatLean.ConcentrationInequalities.Chaining.DiscreteDudley exits 0 (WAIT for it); 0 sorries in the file; commit: git add StatLean/ConcentrationInequalities/Chaining/DiscreteDudley.lean && git commit -m 'conc/hdp-debt-dudley-abs: close discrete_dudley_abs'; report the key steps.