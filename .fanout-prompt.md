# Close the sorries in PointEstimation/UMVU/LehmannScheffe.lean + InformationInequality/Attainment.lean

**RUN EVERY BUILD IN THE FOREGROUND. Never background a build, never `srun`/`sbatch` from inside, never `sleep`/poll, never schedule a wakeup, never say "the build is running in the background". Plain `lake build <module>` and WAIT for it to print. A previous session lost its whole run by backgrounding — do not repeat that.**

Lean 4 / Mathlib proof engineer on `StatLean`. Pin `v4.29.1`. (Repo `CLAUDE.md` is gitignored and NOT here. Project rules: never `lake update`; `sorry` is planned debt; never launder unproven content into hypotheses; lines ≤ 100 chars.)

You are ON the cluster on branch `pe/umvu`. These are 3 of the last 10 open sorries in the PointEstimation area.

## Scope — edit ONLY these 2 files
1. `StatLean/PointEstimation/UMVU/LehmannScheffe.lean` (2 sorries)
2. `StatLean/PointEstimation/InformationInequality/Attainment.lean` (1 sorry)

Nothing else — NOT `UMVU/CovarianceCriterion.lean`, `Completeness/*`, `Sufficiency/*` (all closed; use them). You MAY add `import Mathlib.*`, import closed project modules, add `private` helpers. Public signatures/docstrings FROZEN **except** the one pre-authorized addition in item 2 below.

## Closed, axiom-clean bricks you should lean on
- `UMVU.CovarianceCriterion.isUMVU_iff_uncorrelated_unbiasedZero` — the UMVU characterisation (uncorrelated with every unbiased-estimator-of-zero). **Fully proven.**
- `Completeness.ExpFamily.isCompleteStat_of_interior_nonempty` — completeness of the natural statistic of a full-rank exponential family. **Fully proven.**
- `Sufficiency.Factorization.*` and `Sufficiency.RegularConditional.hasSufficientKernel_of_isSufficient_dominated`. **Proven.**
- In-file and proven: `integrable_rbEstimator_statLaw`, `memLp_two_rbEstimator_statLaw`, `unique_unbiased_function_of_complete`.

## Work in this order
1. **`isUMVU_of_fullRank_expFamily`** (~286, sorry ~317) — its TODO says to assemble from completeness of the natural statistic plus sufficiency. Both ingredients are closed (above). ⚠️ **Route the UMVU conclusion through `CovarianceCriterion.isUMVU_iff_uncorrelated_unbiasedZero`, NOT through `isUMVU_of_complete_sufficient`** — the latter depends on the still-open `variance_rbEstimator_le_of_complete` below, so going that way would make your result show `sorryAx`. Combine: full-rank ⇒ complete natural statistic; complete + unbiased-of-zero ⇒ that estimator is a.e. 0 (`unique_unbiased_function_of_complete`); hence the covariance criterion is satisfied trivially.
2. **`variance_rbEstimator_le_of_complete`** (~187, sorry ~195, private) — the Rao–Blackwell variance inequality. The known obstruction: the frozen `MemEstL2` competitor class supplies no *global measurable representative*, so the conditional expectation of a competitor need not be measurable across all `θ`. **PRE-AUTHORIZED FIX:** if you need it, add a measurability/representative hypothesis to this **private** lemma (it is private, so you may change its signature) — e.g. require the competitor to have a jointly measurable version, or route the whole argument through Mathlib's `MeasureTheory.condExp` (which yields a genuinely measurable version by construction) instead of an abstract conditional distribution. Then Jensen/conditional-variance decomposition gives `Var(E[δ|T]) ≤ Var δ`. Report exactly what you added. If instead you conclude the private lemma is unprovable as used by `isUMVU_of_complete_sufficient`, say so precisely.
3. **`expFamily_of_cramer_rao_attained_core`** (~147, sorry ~158, private, in `Attainment.lean`) — the Cramér–Rao **equality case**: attainment forces the exponential-family form. Its TODO says three bricks are missing; read it and attempt them. The forward direction `cramer_rao_attained_of_affine` (~79) is proven — reuse its algebra. Route: equality in Cauchy–Schwarz forces `score(θ,x) = a(θ)·(δ(x) − g(θ))` a.e.; integrating in `θ` gives the exponential form. If a brick is genuinely absent from Mathlib, lift it to ONE named `private` lemma with a precise `-- TODO:` naming the missing brick, and report.

**If a statement is actually false, STOP and report the counterexample** — honest refusal is desired (sibling sessions have correctly caught several false statements this way). Never fake a proof or silently weaken a public statement.

## Discipline
- Commit after each lemma compiles — bank incrementally.
- `#print axioms` on each theorem you close. **`isUMVU_of_fullRank_expFamily` must come out clean** (`propext, Classical.choice, Quot.sound`) — if it shows `sorryAx` you routed it through the open variance lemma; fix the route.

## Report at the end
Per-file `lake build` status, remaining sorry count per file, CLOSED vs LIFTED with exact obstructions, any hypothesis you added to the private lemma, `#print axioms` outputs, and any statement you believe false (with counterexample).