# Close the sorries in PointEstimation/LinearModel/{RandomDesign,LeastSquares,Equivariant}.lean

**RUN EVERY BUILD IN THE FOREGROUND. Never background a build, never `srun`/`sbatch` from inside, never `sleep`/poll, never schedule a wakeup. Plain `lake build <module>` and WAIT for it. Backgrounding a build wastes the whole session with work unverified — do not do it.**

Lean 4 / Mathlib proof engineer on `StatLean`. Pin `v4.29.1`. (The repo `CLAUDE.md` is gitignored and NOT in this worktree — rules you need are below. Project rules: never `lake update`; `sorry` is planned debt tied to a named lemma; never launder unproven content into hypotheses; lines ≤ 100 chars.)

You are ON the cluster in a fan-out worktree on branch `pe/lm-retry`. This is the normal-linear-model chapter (UMVU + equivariance/MRE for the Gaussian linear model).

## Scope — edit ONLY these 3 files
`StatLean/PointEstimation/LinearModel/{RandomDesign,LeastSquares,Equivariant}.lean`. Touch nothing else — NOT `LinearModel/Canonical.lean` (DONE, 0-sorry, treat as black box) or any `Defs.lean` (frozen). You MAY add `import Mathlib.*`, import closed project modules, and add `private` helpers. Public signatures/docstrings FROZEN.

## CRITICAL — route UMVU through the covariance criterion, NOT complete-sufficiency
`UMVU.CovarianceCriterion.isUMVU_iff_uncorrelated_unbiasedZero` is fully proven and axiom-clean — **use it.** Do **NOT** route any UMVU conclusion through `UMVU.LehmannScheffe.isUMVU_of_complete_sufficient`: that theorem depends on an open `sorry` (`variance_rbEstimator_le_of_complete`), so any proof through it shows `sorryAx`. `Canonical.lean` is closed 0-sorry — reuse `canonicalStat_isCompleteStat`, the exponential-family recognition, and the mutual-absolute-continuity helper pattern (all `canonicalModel` members have strictly-positive Gaussian densities, so a `P p₀`-a.e. representative is automatically a `P p'`-a.e. representative for every `p'` — this is the key that unlocks the UMVU theorems axiom-cleanly).

## Work in this order (EASY FIRST — commit after each lemma compiles)
1. **`RandomDesign.lean`** — `not_exists_blue_of_known_design_moment` (~line 464). A negative result: no BLUE exists when the design moment is known (the class is too large / a strictly-better estimator exists). The file's `isUMVU_regression_coeff`, `covariance_regression_coeff` are already proven — reuse. Just this one sorry.
2. **`LeastSquares.lean`** (3 sorries: `isUMVU_lse_functional` ~90, `isUMVU_residualSumSq` ~107, `isUMVU_linear_functional_of_mean` ~117+). The LSE of any linear functional of the mean, and the residual sum of squares, are UMVU. Use `inner_lse_eq_inner_starProjection` (proven) to rewrite the LSE as an inner product with the `starProjection`, then the covariance criterion + Canonical completeness. These route through Canonical (0-sorry) per the in-file comments.
3. **`Equivariant.lean`** (4 sorries: `isCanonicalMRE_linear_combination` ~110, `isCanonicalScaleMRE_residual_pow` ~228, `isCanonicalScaleMRE_residual_variance` ~236, `isSubspaceMRE_lse_functional` ~275). Canonical MRE results. `isCanonicalScaleMRE_residual_*` use the χ² moment machinery (`integrable_pow_chiSquared`, `residualScaleConst`, and `MultipleTesting.ForMathlib.ChiSquared` — imported, treat as black box). `isCanonicalMRE_linear_combination`/`isSubspaceMRE_lse_functional` are the equivariant analogues of the UMVU results.

If a sorry needs a genuinely missing brick, lift it to ONE named `private` lemma with a precise `-- TODO:` and report it — never fake a proof or weaken a public statement.

## Discipline
- Commit after each lemma compiles.
- **If any public statement is actually false, STOP, leave it sorried, report the counterexample.** Honest refusal is the desired outcome.
- For each closed headline: `#print axioms <name>` — expect only `propext, Classical.choice, Quot.sound`. **Any `sorryAx` means you routed through a sorried lemma (likely `isUMVU_of_complete_sufficient`) — fix the route.** Report all axioms outputs.

## Report at the end
Per-file `lake build` status, remaining sorry count per file, which sorries CLOSED vs LIFTED-as-debt, the `#print axioms` outputs for closed headlines (confirm NO `sorryAx`), and any statement you believe false (with counterexample).