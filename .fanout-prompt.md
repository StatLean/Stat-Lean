# hdp-chain-dudley — Theorem 8.1.3 / Eqs (8.13)(8.14)(8.16) integral Dudley [HDP §8.1]

You are a Lean 4 proof-closure session on branch `conc/hdp-chain-dudley` of the StatLean project (Lean v4.29.1, pinned Mathlib `5e932f97` pre-built under `.lake/packages/mathlib`). You are inside an srun allocation on FAS-RC: run plain `lake build <Target>`; NEVER `lake update`; never touch `lakefile.lean` / `lake-manifest.json` / `lean-toolchain`. Iterate until GREEN.

CONTEXT: this worktree has no CLAUDE.md/notes/tools (gitignored). Everything you need is in this prompt + the stub files. Mathlib search: `grep -rn "<name>" .lake/packages/mathlib/Mathlib/` and in-proof `exact?` / `apply?` / `rw?`. Upstream StatLean lemmas in OTHER files are `sorry`-backed statements — cite them as black boxes (by design); only YOUR file must become sorry-free.

## Touch-set (edit ONLY this file)
`StatLean/ConcentrationInequalities/Chaining/Dudley.lean`
Do NOT edit any other file. Do NOT change any statement, hypothesis, constant, or tag. If a statement looks unprovable AS STATED, stop and report precisely why — do not weaken it.

## Build gate
`lake build StatLean.ConcentrationInequalities.Chaining.Dudley` — exit 0.

## The goal — close these sorries (read the file for exact statements)
1. `dudley_inequality` — Eq (8.16)-capped integral form, mean-zero, `≤ 12·√3·K·dudleyIntegral T D`, from `discrete_dudley` + the ∑ ≤ 2∫ comparison.
2. `dudley_inequality_abs` — Eq (8.13) anchored abs form, NO mean-zero, `≤ 40·K·dudleyIntegral T D` (THE consumer-facing form), from `discrete_dudley_abs`.
3. `dudley_inequality_abs_pair` — Eq (8.14), `≤ 80·K·…` via triangle through a fixed anchor.
4. `dudley_inequality_Ioi` — the ∫₀^∞ display (`dudleyIntegral_Ioi_eq`-style: integrand vanishes for ε ≥ diam since 𝒩 = 1).
5. `dudley_inequality_countable` — the ONE countable lift (fallback; see below): monotone convergence over a finite exhaustion, `coveringNumber_subset_le`-style ε/2 loss absorbed in the frozen constant 80, `∫⁻`/`ENNReal` form.

READ FIRST (toolbox; sorry-backed citable): `Chaining/DiscreteDudley.lean` (`discrete_dudley` 6√3, `discrete_dudley_abs` 20, `integrable_biSup_sub`), `Chaining/EntropySum.lean` (`dudleySum`, `dudleyIntegral`, `dudleyLIntegral`, `sum_window_le_two_mul_dudleyIntegral` — the ∑ ≤ 2∫ bridge — plus summability/vanishing lemmas), `Chaining/EntropyIntegrand.lean` (`sqrtLogCov` monotonicity/vanishing: 𝒩 = 1 and integrand 0 for ε ≥ diam), `Chaining/SubGaussianIncrements.lean` (`mono_set` etc.), `Maximal/CoveringNumbers.lean` (proven: `coveringNumber` API; grep Mathlib `Metric.coveringNumber` lemmas for subset-monotonicity — `Metric.coveringNumber_subset_le`-style names, check exact pin name via grep).

## Proof roadmap (HDP p. 226 + Remarks 8.1.5–8.1.7)
- (1)/(2): chain the discrete forms with `sum_window_le_two_mul_dudleyIntegral` (`dudleySum ≤ 2·dudleyIntegral`): `6√3·K·dudleySum ≤ 12√3·K·dudleyIntegral`; `20·K·dudleySum ≤ 40·K·dudleyIntegral`. Mind hypotheses: (1) carries `hint`/`hmean` (pass through), (2) does not.
- (3): pointwise `|X t − X s| ≤ |X t − X t₀| + |X s − X t₀|`, `⨆` over pairs ≤ 2·(⨆ anchored) (`Real.iSup` algebra over finite nonempty index — `ciSup_le` + `le_ciSup` with `BddAbove` from finiteness; integrability from `integrable_biSup_sub`).
- (4): `dudleyIntegral T D = ∫ over Ioi 0` when the integrand vanishes off `Ioc 0 D` (`MeasureTheory.setIntegral_eq_of_...`-style: `integral_Ioc_eq_integral_Ioi`? — grep; or split `Ioi 0 = Ioc 0 D ∪ Ioi D` with the second integral 0 via `sqrtLogCov`-vanishing + `setIntegral_congr` to the zero function).
- (5): exhaust countable `T = ⋃ₙ Tₙ` (finite increasing, e.g. images of `T.toCountable` enumeration prefixes intersected with T — cleanest: `Set.Countable.exists_eq_range`-style enumeration + `Tₙ := range restricted to Fin n` as Finsets via images); anchored sup is monotone in `n` → `MeasureTheory.lintegral_iSup` (monotone convergence in ∫⁻); per-`n` apply (2) transported to `∫⁻` (`ofReal_integral_eq_lintegral_ofReal` needs the integrability export); covering numbers of `Tₙ ⊆ T` bounded via subset-monotonicity at ε/2 (the docstring's frozen 2×40 = 80 accounting); `dudleyLIntegral` comparisons per the EntropySum lemmas.

## Fallback (graded)
If exactly one goal resists, it must be `dudley_inequality_countable` (leave its single `sorry`). All finite-T forms (1)-(4) MUST close.

## Constraints
- New Mathlib imports fine; StatLean imports only from `StatLean.ConcentrationInequalities.*`.
- `private` named helpers allowed; NO sorried `have`s, no `axiom`/`admit`, no new hypotheses.
- Keep all tags/docstrings intact.
- Commit: `git add StatLean/ConcentrationInequalities/Chaining/Dudley.lean && git commit -m "conc/hdp-chain-dudley: close Thm 8.1.3 integral forms"`.

## DONE means
`lake build StatLean.ConcentrationInequalities.Chaining.Dudley` exits 0; at most the designated fallback sorry remains; report closed sorries + key Mathlib lemmas.