# hdp-bundle-orlicz-tail — Basic + Generators + ExpTaylorBounds + SubGaussianTail + Equivalence + SubExponentialTail [HDP §2.6/§2.8 support]
You are a Lean 4 proof-closure session on branch conc/hdp-bundle-orlicz-tail of the StatLean project (Lean v4.29.1, pinned Mathlib 5e932f97 pre-built under .lake/packages/mathlib). Inside an srun allocation: plain `lake build <Target>`; NEVER `lake update`; never touch lakefile/manifest/toolchain. Iterate until GREEN and COMMIT BEFORE ENDING (wait for the final build, then commit).
CONTEXT: no CLAUDE.md/notes/tools here. Mathlib search: grep over .lake/packages/mathlib + exact?/apply?. REAL on this branch: Orlicz/{Defs(complete), Attainment, Triangle, TailToNorm, SubGaussianMGF, SubExponentialMGF, Product, Bernstein, SubGaussianMoments, NormFromMoments, SumNorm, VectorNorm} — everything was proven against THESE support statements; do NOT change them.
## Touch-set (ONLY these six; one at a time, building each)
1. StatLean/ConcentrationInequalities/Orlicz/Generators.lean — gate `...Orlicz.Generators`. psiTwo/psiOne zero/nonneg/monotone/convex/continuous/measurable/tendsto: Real.exp API (exp_le_exp, convexOn_exp, Real.continuous_exp, tendsto_exp_atTop) + composition algebra (ConvexOn.comp with sq on Ici 0 — mirror the technique notes in the docstrings).
2. StatLean/ConcentrationInequalities/ForMathlib/ExpTaylorBounds.lean — gate `...ForMathlib.ExpTaylorBounds`. Elementary exp-Taylor pointwise bounds per stubs (nlinarith + Real.add_one_le_exp + exp series lemmas).
3. StatLean/ConcentrationInequalities/Orlicz/Basic.lean — gate `...Orlicz.Basic`. Luxemburg order/algebra API: witness extraction (csInf/iInf₂ in ℝ≥0∞), congr/mono/neg/abs lemmas (gauge-set identities under |·| and −), orliczNorm_zero (every K > 0 is a gauge when ψ 0 ≤ 0: lintegral of ofReal(ψ 0) ≤ 0-algebra), orliczNorm_const_mul (gauge bijection K ↦ ‖c‖₊K).
4. StatLean/ConcentrationInequalities/Orlicz/SubGaussianTail.lean — gate `...Orlicz.SubGaussianTail`. B1 raw core: Markov (mul_meas_ge_le_lintegral₀) on the event equality {t ≤ |X|} = {exp(t²/K²) ≤ exp((X/K)²)} + the norm wrapper via Attainment (REAL).
5. StatLean/ConcentrationInequalities/Orlicz/Equivalence.lean — gate `...Orlicz.Equivalence`. Composite arrows (i)⇒(ii), (iv)⇒(i) per stubs: chain the REAL sibling lemmas; (iv)⇒(i) is the existing Chernoff/two-sided-tail pattern (mirror SubGaussian/TailBounds.lean, batch-1 proven).
6. StatLean/ConcentrationInequalities/Orlicz/SubExponentialTail.lean — gate `...Orlicz.SubExponentialTail`. ψ₁ Markov tail (same shape as file 4 with exp(|X|/K)).
## Fallbacks (≤ ONE per file, docstring-designated only).
## Constraints
No axiom/admit; no new hypotheses; keep tags; private helpers OK.
Commit: git add StatLean/ConcentrationInequalities/ && git commit -m "conc/hdp-bundle-orlicz-tail: close Orlicz support layer"
## DONE means
All six targets exit 0 (WAIT); ≤ 6 designated fallbacks; report per-file closures.