# hdp-chain-dudley attempt 2 — fix the two residual build errors [HDP §8.1]

You are a Lean 4 proof-repair session on branch `conc/hdp-chain-dudley` of the StatLean project (Lean v4.29.1, pinned Mathlib `5e932f97` pre-built under `.lake/packages/mathlib`). You are inside an srun allocation on FAS-RC: run plain `lake build <Target>`; NEVER `lake update`; never touch `lakefile.lean` / `lake-manifest.json` / `lean-toolchain`.

SITUATION: the previous session closed all Dudley integral forms in `StatLean/ConcentrationInequalities/Chaining/Dudley.lean` (only the authorized fallback `dudley_inequality_countable` remains sorried) but ENDED before its final build finished. The verification gate found exactly TWO residual errors on this branch's tip:

1. `Dudley.lean:183:31 — Application type mismatch: The argument …`
2. `Dudley.lean:192:17 — Tactic 'rewrite' failed: Did not find an occurrence of the pattern …`

## Your job
`lake build StatLean.ConcentrationInequalities.Chaining.Dudley` — reproduce the two errors, fix them MINIMALLY (proof-side only), and ITERATE TO GREEN (exit 0). The full error messages will appear in your build output; read the surrounding proof context (lines ~170–200) to understand what the previous session intended.

## Hard constraints
- Edit ONLY `StatLean/ConcentrationInequalities/Chaining/Dudley.lean`, and only PROOF BODIES — do NOT change any statement, hypothesis, constant, docstring, or tag.
- Do NOT touch the `dudley_inequality_countable` sorry (it is the authorized fallback debt).
- No `axiom`/`admit`; no new sorries.
- Upstream StatLean lemmas in other files may be sorry-backed — cite them as black boxes.
- When green: `git add StatLean/ConcentrationInequalities/Chaining/Dudley.lean && git commit -m "conc/hdp-chain-dudley: fix two residual elaboration errors (attempt 2)"`.

## DONE means
`lake build StatLean.ConcentrationInequalities.Chaining.Dudley` exits 0 from a clean state; report the two fixes in one sentence each.