# hdp-debt-final4 — the last 4 sorries of Batch 10 [HDP]
You are a Lean 4 debt-closure session on branch conc/hdp-debt-final4 of the StatLean project (Lean v4.29.1, pinned Mathlib 5e932f97 pre-built under .lake/packages/mathlib). Inside an srun allocation: plain lake build; NEVER lake update; never touch lakefile/manifest/toolchain. COMMIT AND PUSH AFTER EACH CLOSED DEBT (git push origin conc/hdp-debt-final4 — the tmux server has died mid-session before; pushed commits are the only durable state). ~35 min per debt; over ~50 min → leave a one-sentence blocker, move on. No sub-orchestration; verify every step with a build.
CONTEXT: no CLAUDE.md/notes/tools here. Mathlib search: grep over .lake/packages/mathlib + exact?/apply?. EVERYTHING in StatLean/ConcentrationInequalities is REAL except exactly these 4 sorries. Statements FROZEN; only sorry bodies (+ private helpers in the same file).
## The 4 debts
1. Chaining/FinsetMaximal.lean:65 — biSup_finset_eq_sup' — finite biSup = Finset.sup': the SAME identity is proven as a PRIVATE lemma inside Chaining/GenericChaining.lean (REAL — read it and copy the technique: iSup_congr + Set.Finite.toFinset plumbing) or grep Finset.sup'_eq_ciSup. Gate ...Chaining.FinsetMaximal.
2. Chaining/FinsetMaximal.lean:185 — expectation_max_finset_le — adapter over PROVEN Maximal/FiniteMaximal.expectation_max_le: reindex s via (s.equivFin : s ≃ Fin s.card) — define Y : Fin s.card → Ω → ℝ := fun i => X (s.equivFin.symm i), transport per-coordinate IsSubGaussian, apply the proven Fin-indexed bound, convert sup via debt 1. Same gate.
3. Chaining/DyadicNets.lean:144 — one_lt_coveringNumber_of_two_mul_lt_dist — two points > 2ε apart ⇒ 𝒩 > 1: by_contra 𝒩 ≤ 1 gives a singleton (or empty) ε-net {c} (coveringNumber ≤ 1 unfolding via Maximal/CoveringNumbers REAL API + Set.encard_le_one); both points within ε of c → dist ≤ 2ε contradiction (dist_triangle). Gate ...Chaining.DyadicNets.
5. EmpiricalProcess/SubGaussianIncrements.lean:243 — hasSubgaussianMGF_empiricalProcess_sub — read the REAL sibling lemmas in the SAME file (several MGF-level lemmas around it are proven — mirror their structure): per-summand (f−g)∘Xᵢ ∈ [−δ,δ] mean-centered ⇒ Mathlib mgf_le_of_mem_Icc_of_integral_eq_zero / hasSubgaussianMGF_of_mem_Icc (pin) with the file's frozen proxy; combine via HasSubgaussianMGF.sum_of_iIndepFun (independence: hindep.comp (measurable f−g)); the n⁻¹ scaling via mgf_smul_left / the HasSubgaussianMGF scaling route used by the sibling. Gate ...EmpiricalProcess.SubGaussianIncrements.
## DONE means
Each touched target builds exit 0 (WAIT for the last); per-debt commits PUSHED; report closed/left per debt.

## Debt 5 (REPAIR) — Symmetrization/Empirical.lean: empirical_symmetrization_countable was closed on branch conc/hdp-debt-final5 (fetchable: git fetch origin conc/hdp-debt-final5 && git show origin/conc/hdp-debt-final5:StatLean/ConcentrationInequalities/Symmetrization/Empirical.lean) but FAILED its gate with these errors:
  error: StatLean/ConcentrationInequalities/Symmetrization/Empirical.lean:262:38: Unknown identifier `abs_add`
  error: StatLean/ConcentrationInequalities/Symmetrization/Empirical.lean:293:34: Application type mismatch: The argument
  error: StatLean/ConcentrationInequalities/Symmetrization/Empirical.lean:323:6: Type mismatch
  error: StatLean/ConcentrationInequalities/Symmetrization/Empirical.lean:343:8: Tactic `rewrite` failed: Did not find an occurrence of the pattern
Import that version into your worktree file, reproduce, fix minimally, iterate to green. Gate ...Symmetrization.Empirical.