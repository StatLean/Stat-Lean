# hdp-symmetrization — Lemma 6.3.2 symmetrization (canonical product form) [HDP §6.3]

You are a Lean 4 proof-closure session on branch `conc/hdp-symmetrization` of the StatLean project (Lean v4.29.1, pinned Mathlib `5e932f97` pre-built under `.lake/packages/mathlib`). You are inside an srun allocation on FAS-RC: run plain `lake build <Target>`; NEVER `lake update`; never touch `lakefile.lean` / `lake-manifest.json` / `lean-toolchain`. Iterate until GREEN.

CONTEXT: this worktree has no CLAUDE.md/notes/tools (gitignored). Everything you need is in this prompt + the stub files. Mathlib search: `grep -rn "<name>" .lake/packages/mathlib/Mathlib/` and in-proof `exact?` / `apply?` / `rw?`. Upstream StatLean lemmas in OTHER files are `sorry`-backed statements — cite them as black boxes; only YOUR file must become sorry-free.

## Touch-set (edit ONLY this file)
`StatLean/ConcentrationInequalities/Symmetrization/Symmetrization.lean`
Do NOT edit any other file. Do NOT change any statement, hypothesis, constant, or tag. If a statement looks unprovable AS STATED, stop and report precisely why — do not weaken it.

## Build gate
`lake build StatLean.ConcentrationInequalities.Symmetrization.Symmetrization` — exit 0.

## The goal — close these sorries
Read the file: `symmetrization_upper_pi` / `symmetrization_lower_pi` (canonical `Measure.pi ν ×ₘ signVec n` product forms) and the packaged `symmetrization_upper` / `symmetrization_lower` (HDP Lemma 6.3.2: `½·E‖∑ εᵢXᵢ‖ ≤ E‖∑ Xᵢ‖ ≤ 2·E‖∑ εᵢXᵢ‖`, Banach-space-valued Bochner integrals).

READ FIRST (toolbox; sorry-backed citable):
- `Symmetrization/Rademacher.lean`: `radLaw`, `signVec n = Measure.pi (fun _ => radLaw)`, `radLaw_ae_pm`, `radLaw_map_neg`, `signVec_map_mul_pm`, `measurePreserving_eval_signVec`, `iIndepFun_eval_signVec`, instances.
- `Symmetrization/SignFlip.lean`: `measurePreserving_condSwap` (the conditional coordinate swap on `(Measure.pi ν) ×ₘ (Measure.pi ν) ×ₘ signVec n`-style products), `integral_flip_diff`.
- `Symmetrization/NormAddMeanZero.lean`: `norm_le_integral_norm_add` / `integral_norm_le_integral_norm_add_prod` (HDP Eq (6.13): `E Z = 0 ⇒ E‖Y‖ ≤ E‖Y + Z‖` in product form).
- `ForMathlib/IndepTransport.lean`: product-integral transport lemmas.

## Proof roadmap (HDP p. 180, proof of Lemma 6.3.2)
- Upper (`E‖∑Xᵢ‖ ≤ 2E‖∑εᵢXᵢ‖`): introduce the independent copy X′ on the doubled product; `E‖∑Xᵢ‖ ≤ E‖∑(Xᵢ − X′ᵢ)‖` by Eq (6.13) applied conditionally (Fubini `MeasureTheory.integral_prod` / `integral_integral_swap` + the inner-integral Jensen from NormAddMeanZero — mean-zero of `−X′` under the second factor); `(Xᵢ − X′ᵢ)` is a symmetric family: the sign-flip `measurePreserving_condSwap` shows `∑(Xᵢ−X′ᵢ)` and `∑εᵢ(Xᵢ−X′ᵢ)` are equidistributed (integrate over signs; `signVec` a.e. ±1); then triangle: `E‖∑εᵢ(Xᵢ−X′ᵢ)‖ ≤ 2·E‖∑εᵢXᵢ‖` (the two halves are exchangeable copies — `MeasurePreserving.integral_comp` transport).
- Lower (`½E‖∑εᵢXᵢ‖ ≤ E‖∑Xᵢ‖`): same skeleton run backwards — `E‖∑εᵢXᵢ‖ ≤ E‖∑εᵢ(Xᵢ−X′ᵢ)‖ = E‖∑(Xᵢ−X′ᵢ)‖ ≤ 2E‖∑Xᵢ‖`; note the FIRST step needs Eq (6.13) with the signs FROZEN (Fubini over signs outermost, then per-sign the mean-zero copy argument; `εᵢ·X′ᵢ` has mean zero given ε).
- Bochner integrability side conditions: all statements carry explicit `Integrable`/`Measurable` hypotheses (read them; do not add more). Key Mathlib: `MeasureTheory.integral_prod`, `integral_integral_swap`, `norm_integral_le_integral_norm`, `MeasurePreserving.integral_comp`, `Measure.pi` marginal lemmas (`measurePreserving_eval_pi`?—grep), `Finset.sum_comm`, `integral_finset_sum`.
- The `pi` forms are the real work; the packaged forms are transport corollaries via `ForMathlib/IndepTransport` + `Rademacher` lemmas.

## Fallback (graded)
If after honest effort exactly one lemma resists, it must be `symmetrization_lower_pi` (leave its single `sorry`). The UPPER direction (consumed by Exercise 8.11 and Lemma 6.6.2) MUST close.

## Constraints
- New Mathlib imports fine; StatLean imports only from `StatLean.ConcentrationInequalities.*`.
- `private` named helpers allowed; NO sorried `have`s, no `axiom`/`admit`, no new hypotheses.
- Keep all tags/docstrings intact.
- Commit: `git add StatLean/ConcentrationInequalities/Symmetrization/Symmetrization.lean && git commit -m "conc/hdp-symmetrization: close Lem 6.3.2"`.

## DONE means
`lake build StatLean.ConcentrationInequalities.Symmetrization.Symmetrization` exits 0; at most the designated fallback sorry remains; report closed sorries + key Mathlib lemmas.