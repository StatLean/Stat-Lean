# Close the 5 sorries in HypothesisTesting/MLR/OneSided.lean (ONE file only)

**RUN EVERY BUILD IN THE FOREGROUND. Never background a build, never `srun`/`sbatch` from inside, never `sleep`/poll, never schedule a wakeup, never say "the build is running in the background". Plain `lake build StatLean.HypothesisTesting.MLR.OneSided` and WAIT for it to print. A previous session lost its entire run by backgrounding — do not repeat that.**

Lean 4 / Mathlib proof engineer on `StatLean`. Pin `v4.29.1`. (Repo `CLAUDE.md` is gitignored and NOT here. Project rules: never `lake update`; `sorry` is planned debt; never launder unproven content into hypotheses; lines ≤ 100 chars.)

You are ON the cluster on branch `ht/onesided-2`. This is the **one-sided UMP test under a monotone likelihood ratio** — a headline of the whole batch.

## Scope — edit ONLY `MLR/OneSided.lean`
Do **NOT** open or edit `MLR/TwoSided.lean` (a prior session burned itself there; it is also blocked on a separate missing-duality issue). Do not touch `MLR/Defs.lean` or `Tests/Defs.lean` (frozen). You MAY add `import Mathlib.*`, import closed project modules, add `private` helpers. Public signatures/docstrings FROZEN.

## The infrastructure is ALREADY BUILT AND PROVEN — read the file top-to-bottom first and USE it
These are all closed in-file; do not re-derive them:
`oneSidedTest`, `testRisk`, `density_integrable`, `integral_density_eq`, `density_integral_one`, `density_mul_integrable`, `measurable_oneSidedTest`, `isCriticalFn_oneSidedTest`, `oneSidedTest_mono`, `power_oneSidedTest_eq`, `exists_sep_const`, **`hasNPShape_oneSided`**, **`isMostPowerful_oneSided`**, `exists_pos_density_of_power_pos`, **`integral_mono_of_hasMLR`**, plus the co-monotone mirrors `powerAgainst_one_sub`, `exists_sep_const_rev`, **`hasNPShape_coOneSided`**, `exists_pos_density_below_of_coPower_pos`.

## The 5 sorries — assembly on top of that infrastructure (commit after each compiles)
1. **`isUMP_oneSided`** (~412, sorry ~475) — the one-sided test is UMP for `H₀: θ ≤ θ₀` vs `H₁: θ > θ₀`. For each simple alternative `θ' > θ₀` the test has NP shape (`hasNPShape_oneSided`), so `isMostPowerful_oneSided` gives most-powerful **against that θ'**; UMP is exactly that, uniformly in `θ'` — the test does not depend on `θ'`, which is the whole point. Also discharge the level clause via `power_oneSidedTest_eq`.
2. **`power_strictMono_oneSided`** (~484, sorry ~510) — the power function is strictly increasing in `θ`. Use `integral_mono_of_hasMLR` for the monotone (`≤`) direction; strictness comes from the **strict** MLR hypothesis in the signature plus `exists_pos_density_of_power_pos`. (The file's own comment notes the `≤` direction is `integral_mono_of_hasMLR`.)
3. **`isUMP_oneSided_shifted`** (~515, sorry ~550) — same as 1 with the null shifted; reuse 1's proof shape.
4. **`power_min_oneSided`** (~671, sorry ~721) — the co-monotone mirror: minimum power over the null. Use the `*_rev` / `*_coOneSided` mirrors listed above.
5. **`essentiallyComplete_oneSidedTest`** (~726, sorry ~757) — the one-sided tests form an essentially complete class: any test is matched or beaten by a one-sided test of the same size. Build the matching one-sided test via `exists_sep_const` at the given size, then compare with `isMostPowerful_oneSided`.

If a genuine gap remains, lift it to ONE named `private` lemma with a precise `-- TODO:` and report it — never fake a proof. **If a statement is actually false, STOP and report the counterexample** — honest refusal is desired (sibling sessions have correctly caught two false statements this way).

## Discipline
- Commit after each lemma compiles — bank progress incrementally.
- `#print axioms` on each theorem you close — expect only `propext, Classical.choice, Quot.sound`. Report any `sorryAx`.

## Report at the end
Final `lake build` status, remaining sorry count, which CLOSED vs LIFTED (with the exact obstruction), `#print axioms` outputs, and any statement you believe false (with counterexample).