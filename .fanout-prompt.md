# Close the 5 sorries in HypothesisTesting/Invariance/SufficiencyReduction.lean (ONE file)

**RUN EVERY BUILD IN THE FOREGROUND. Never background a build, never `srun`/`sbatch` from inside, never `sleep`/poll, never schedule a wakeup, never say "the build is running in the background". Plain `lake build StatLean.HypothesisTesting.Invariance.SufficiencyReduction` and WAIT for it to print. A previous session lost its whole run by backgrounding — do not repeat that.**

Lean 4 / Mathlib proof engineer on `StatLean`. Pin `v4.29.1`. (Repo `CLAUDE.md` is gitignored and NOT here. Project rules: never `lake update`; `sorry` is planned debt; never launder unproven content into hypotheses; lines ≤ 100 chars.)

You are ON the cluster on branch `ht/suff-red`. This is the **invariance ↔ sufficiency reduction** for hypothesis tests.

## Scope — edit ONLY this one file
`StatLean/HypothesisTesting/Invariance/SufficiencyReduction.lean`. Nothing else — NOT `Invariance/Defs.lean`, `AlmostInvariance.lean`, or the `PointEstimation/Sufficiency|Completeness` files. You MAY add `import Mathlib.*`, import closed project modules, add `private` helpers. Public signatures/docstrings FROZEN.

## The 5 sorries (work in this order, commit after each compiles)
1. **`power_invariant_iff_almostInvariant`** (~82, sorry ~92) — a test's power function is invariant under the induced action on `Θ` **iff** the test is almost invariant. Both directions are measure-theoretic bookkeeping: `power φ (g·θ) = ∫ φ d(P (g·θ))` and the model's invariance `P (g·θ) = (P θ).map (g • ·)` turn invariance of the power into `∫ (φ ∘ (g • ·) − φ) dP θ = 0` for all `θ`; combine with the `[0,1]` range to get the a.e. statement.
2. **`exists_almostInvariant_on_stat_of_invariant`** (~127, sorry ~143) — an invariant test is a.e. equal to an almost-invariant function of the sufficient statistic. Route: condition on the sufficient statistic (the conditional expectation is θ-free by sufficiency), then push the group action through.
3. **`exists_invariant_on_stat_of_invariant`** (~147, sorry ~170) — upgrade "almost invariant" to genuinely invariant, using `[MeasurableSpace G] [MeasurableMul G]` (average over the group / pick an invariant version).
4. **`isUMP_of_isUMPAlmostInvariant_sufficient`** (~96, sorry ~117) — UMP among almost-invariant tests + sufficiency ⟹ UMP invariant. Combines 1–3.
5. **`isUMPInvariant_comp_of_isUMPInvariant_stat`** (~175, sorry ~197) — transfer UMP-invariance along the statistic (compose with the sufficient statistic).

**Useful machinery (already in the repo, import as needed):** `HypothesisTesting/ForMathlib/GroupAverageMeasure.lean` (group-average / `condExp_eq_groupAverage`, with the invariant-σ-algebra bridge) and `Invariance/MaximalInvariant.lean`.

**Taint warning — check and report.** This file imports `Invariance/AlmostInvariance.lean`, which carries 2 open sorries. If a theorem you close routes through them, its `#print axioms` will show `sorryAx` — acceptable and expected, but you MUST report exactly which theorems are axiom-clean vs which inherit taint and from where. Do NOT weaken a statement to dodge it.

If a genuine gap remains, lift it to ONE named `private` lemma with a precise `-- TODO:` and report it — never fake a proof. **If a statement is actually false, STOP and report the counterexample** — honest refusal is desired (sibling sessions have correctly caught two false statements this way).

## Discipline
- Commit after each lemma compiles.
- `#print axioms` on each theorem you close.

## Report at the end
Final `lake build` status, remaining sorry count, which CLOSED vs LIFTED (with the exact obstruction), per-theorem `#print axioms` (clean vs sorryAx + source), and any statement you believe false (with counterexample).