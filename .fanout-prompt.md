# hdp-debt-tranche2 — the last 4 Batch-10 debts, one at a time [HDP]
You are a Lean 4 debt-closure session on branch conc/hdp-debt-tranche2 of the StatLean project (Lean v4.29.1, pinned Mathlib 5e932f97 pre-built under .lake/packages/mathlib). Inside an srun allocation: plain `lake build <Target>`; NEVER `lake update`; never touch lakefile/manifest/toolchain. COMMIT AFTER EACH CLOSED DEBT and WAIT for each build. Time budget: ~55 min per debt; if one exceeds it, leave its sorry with a one-sentence blocker note and move on. Closing 3 of 4 beats stalling.
CONTEXT: no CLAUDE.md/notes/tools here. Mathlib search: grep over .lake/packages/mathlib + exact?/apply?. EVERYTHING in StatLean/ConcentrationInequalities is REAL except exactly these 4 sorries. Statements FROZEN; only the 4 sorry bodies (+ new PRIVATE helpers in the same files) may change.
## Debt A — Chaining/DiscreteDudley.lean: discrete_dudley_abs (constant 20)
Prev assessment: needs the 𝒩-based threshold scale κ' = max{k : 𝒩(T,2^{-k}) = 1} + re-anchoring. ALL inputs REAL: DyadicNets (nets, scales, projections), EntropyIntegrand (vanishing: 𝒩 = 1 for ε ≥ diam, antitonicity), EntropySum (window sums, dudleySum summability), PsiTwoMaximal (expectation_abs_max_le), TailToExpectation (first-moment integration: E|Y| ≤ √π·K·d via the gaussian tail), Maximal/CoveringNumbers (coveringNumber_anti). Build κ'-selection as a private Int-valued max (bounded above: 𝒩 ≥ 2 below the min-positive-distance scale — T finite nonempty; use Int.exists greatest via bounded set or Nat.find on the shifted index). Then: chain from κ' (all levels have N_k ≥ 2 ⇒ √log(2N_k²) ≤ √3√log N_k per docstring), one re-anchor term E|X_{t₀'} − X_{t₀}| ≤ √π·K·diam ≤ (docstring: absorbed via diam·√log2 ≤ 4·dudleySum). Follow the module docstring's frozen accounting to land ≤ 20. Gate `lake build StatLean.ConcentrationInequalities.Chaining.DiscreteDudley`.
## Debt B — Chaining/Dudley.lean: dudley_inequality_countable
Three stages per prev assessment: (i) Bochner→∫⁻ bridge for the finite anchored bound (ofReal_integral_eq_lintegral_ofReal + integrable_biSup_sub REAL); (ii) covering change-of-variables 𝒩(F,ε) ≤ 𝒩(T,ε/2) for F ⊆ T (Metric.coveringNumber subset lemma at half radius — grep the pin; or the EntropyIntegrand transfer lemmas REAL); the frozen 80 = 2×40 absorbs the 2×; (iii) monotone convergence over a finite exhaustion (lintegral_iSup over a monotone sequence; mirror VC/LawOfLargeNumbersCountable's exhaustion REAL — enumerate T countable, prefixes as Finsets). Gate `...Chaining.Dudley`.
## Debt C — Chaining/GenericChaining.lean: generic_chaining_tail
Per-level event bookkeeping per the module docstring: thresholds 2^{k/2}(√(2log2)+1)+u scaled by chain-step 2·(level-infDist terms); per-pair B1 tails (SubGaussianIncrements.measure_abs_sub_ge_le REAL); union over |A.seq k|·|A.seq (k+1)| ≤ 2^{2^{k+1}}·2^{2^{k+2}}-style pairs; (a+b+c)² ≥ a²+b²+c² inflation (nlinarith); geometric sum of 2^{2^{k+1}}·exp(−2^k·2log2·…) collapses (each term ≤ 2^{-2^k}-flavored after the inflation kills the pair count); Σ ≤ 2exp(−u²). The consumer generic_chaining_of_admissible is REAL and already integrates this statement — only this event lemma. Gate `...Chaining.GenericChaining`.
## Debt D — VC/LawOfLargeNumbers.lean: rademacher_process_expectation_le
The conditional Dudley glue; ALL inputs REAL in this file's cone: subGaussianIncrements_inner_signVec (same file, REAL), dudley_inequality_abs (40, REAL), VC/CoveringByVC exports incl. card_le_pow_of_entropy_bound (REAL now), VC/EntropyIntegral (27√d, REAL). Assemble per the module docstring's frozen accounting (per-sample bound 2700-flavored: 40 × √6-normalization × 27 ≤ 2700 — read the docstring formula and land its numeral). Mind: T' = projected class ∪ {0} finite (empFrac-based embedding per the file's earlier REAL lemmas), diam ≤ 2, anchor 0. Gate `...VC.LawOfLargeNumbers`.
## DONE means
Per-debt commits pushed by the wrapper; each touched target builds exit 0 (WAIT for the last); final report: per-debt closed/left + blocker sentences.