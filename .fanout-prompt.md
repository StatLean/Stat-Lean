# hdp-chain-entropy — Dyadic entropy sums + the ∑ ≤ 2∫ comparison [HDP §8.1]
You are a Lean 4 proof-closure session on branch conc/hdp-chain-entropy of the StatLean project (Lean v4.29.1, pinned Mathlib 5e932f97 pre-built under .lake/packages/mathlib). Inside an srun allocation: plain `lake build <Target>`; NEVER `lake update`; never touch lakefile/manifest/toolchain. Iterate until GREEN and COMMIT BEFORE ENDING (wait for the final build, then commit).

CONTEXT: no CLAUDE.md/notes/tools here. Mathlib search: `grep -rn "<name>" .lake/packages/mathlib/Mathlib/` + exact?/apply?. Upstream StatLean lemmas may be sorry-backed — cite as black boxes. REAL on this branch: all Orlicz bridges (Attainment/Triangle/TailToNorm/SubGaussianMGF/SubExponentialMGF/Product/Bernstein), Symmetrization 6.3.2/6.6.1/6.6.2, Chaining DiscreteDudley/Dudley/GenericChaining.

## Touch-set (edit ONLY): StatLean/ConcentrationInequalities/Chaining/EntropyIntegrand.lean StatLean/ConcentrationInequalities/Chaining/EntropySum.lean
No statement/hypothesis/constant/tag changes. If unprovable AS STATED, stop and report precisely why.

## Build gate
`lake build StatLean.ConcentrationInequalities.Chaining.EntropySum` — exit 0.

## The goal
Close the file's sorries — READ THE FILE: every statement + its docstring Proof-formalization notes record the designed route and the single designated fallback.

## Proof roadmap (HDP p.226; the ∑ ≤ 2∫ tiling)
- EntropyIntegrand: sqrtLogCov monotonicity/vanishing (coveringNumber antitone in ε — our Maximal/CoveringNumbers.lean coveringNumber_anti, REAL; 𝒩 = 1 for ε ≥ diam via Metric.coveringNumber-API — grep the pin; log/sqrt monotone algebra), the toNat-shape consumer transfer lemma.
- EntropySum: dudleySum summability for finite nonempty T (left of the coarse scale terms vanish; right of the finest scale geometric — Summable.of_nonneg_of_le + summable_geometric_two-family); window-sum lemmas; the block-tiling comparison sum_window_le_two_mul_dudleyIntegral: per dyadic block Ioc(2^{-k-1}, 2^{-k}], the constant 2^{-k-1}·sqrtLogCov(2^{-k}) minorizes the set integral (setIntegral_ge_of_const_le-style with μ.real (Ioc a b) = b − a via Real.volume_Ioc; sqrtLogCov antitone on the block); blocks disjoint, tile inside Ioc 0 D → Finset sum ≤ integral (MeasureTheory.integral_finset_biUnion or setIntegral_mono_set + iUnion accounting); factor 2 from 2^{-k} = 2·2^{-k-1}.

## Fallback (graded)
Only the decl the module docstring designates as this work item's named-sorry fallback; everything else MUST close.

## Constraints
No axiom/admit; no new hypotheses; keep tags; private named helpers OK.
Commit: git add StatLean/ConcentrationInequalities/Chaining/EntropyIntegrand.lean StatLean/ConcentrationInequalities/Chaining/EntropySum.lean && git commit -m "conc/hdp-chain-entropy: close"

## DONE means
Target builds exit 0 (WAIT for it); ≤ 1 designated fallback sorry; report closures + key lemmas.