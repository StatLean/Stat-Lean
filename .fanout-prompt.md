# hdp-orlicz-vector — Prop 2.7.1 + Def 3.4.1 vector norms [HDP §2.7/§3.4]
You are a Lean 4 proof-closure session on branch conc/hdp-orlicz-vector of the StatLean project (Lean v4.29.1, pinned Mathlib 5e932f97 pre-built under .lake/packages/mathlib). Inside an srun allocation: plain `lake build <Target>`; NEVER `lake update`; never touch lakefile/manifest/toolchain. Iterate until GREEN and COMMIT BEFORE ENDING (wait for the final build, then commit).

CONTEXT: no CLAUDE.md/notes/tools here. Mathlib search: `grep -rn "<name>" .lake/packages/mathlib/Mathlib/` + exact?/apply?. Upstream StatLean lemmas may be sorry-backed — cite as black boxes. REAL on this branch: all Orlicz bridges (Attainment/Triangle/TailToNorm/SubGaussianMGF/SubExponentialMGF/Product/Bernstein), Symmetrization 6.3.2/6.6.1/6.6.2, Chaining DiscreteDudley/Dudley/GenericChaining.

## Touch-set (edit ONLY): StatLean/ConcentrationInequalities/Orlicz/SumNorm.lean StatLean/ConcentrationInequalities/Orlicz/VectorNorm.lean
No statement/hypothesis/constant/tag changes. If unprovable AS STATED, stop and report precisely why.

## Build gate
`lake build StatLean.ConcentrationInequalities.Orlicz.VectorNorm` — exit 0.

## The goal
Close the file's sorries — READ THE FILE: every statement + its docstring Proof-formalization notes record the designed route and the single designated fallback.

## Proof roadmap (HDP Prop 2.7.1 + Def 3.4.1/Lem 3.4.2)
- SumNorm: hasSubgaussianMGF_weighted_sum = B2 per-coordinate (hasSubgaussianMGF_of_subGaussianNorm_le, REAL) + Mathlib mgf-scaling for the vᵢ weights (HasSubgaussianMGF for c•X: grep the pin for a smul/const_mul lemma; else via mgf_smul_left through the definition) + HasSubgaussianMGF.sum_of_iIndepFun (pin-verified; per-index proxies); subGaussianNorm_weighted_sum_le = B3 (subGaussianNorm_le_of_isSubGaussian, REAL) on the sum with proxy ∑vᵢ²(3Kᵢ²) → √(6·∑vᵢ²·3Kᵢ²) = √18·… per frozen docstring; v ≡ 1 instance.
- VectorNorm: subGaussianNorm_inner_le_vecNorm (le_biSup over the sphere); coord = single-direction instance (EuclideanSpace.single, inner_single); isSubGaussianVec_of_coords via ⟪X,v⟫ = ∑vᵢXᵢ (EuclideanSpace.inner_eq_star_dotProduct-family or PiLp.inner_apply — CAREFUL: real inner order gotcha, prefer PiLp.inner_apply + RCLike.inner_apply with mul_comm) + orliczNorm_finset_sum_le (Triangle, REAL — note it takes hψ0 = psiTwo_zero-based ψ 0 ≤ 0 argument: pass (le_of_eq psiTwo_zero)) + homogeneity (orliczNorm_const_mul, Basic, sorry-backed citable);
- subGaussianVecNorm_le_of_indep (fallback): sphere sup ≤ √18·K via subGaussianNorm_weighted_sum_le at ∑vᵢ² = ‖v‖² = 1 (EuclideanSpace.norm_eq / PiLp.norm_sq_eq_of_L2); max_coord packaging (finite iSup ≤).

## Fallback (graded)
Only the decl the module docstring designates as this work item's named-sorry fallback; everything else MUST close.

## Constraints
No axiom/admit; no new hypotheses; keep tags; private named helpers OK.
Commit: git add StatLean/ConcentrationInequalities/Orlicz/SumNorm.lean StatLean/ConcentrationInequalities/Orlicz/VectorNorm.lean && git commit -m "conc/hdp-orlicz-vector: close"

## DONE means
Target builds exit 0 (WAIT for it); ≤ 1 designated fallback sorry; report closures + key lemmas.