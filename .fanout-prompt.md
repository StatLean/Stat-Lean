# hdp-orlicz-triangle — Orlicz-norm triangle inequality (Exercise 2.42) [HDP §2.6/§2.8]

You are a Lean 4 proof-closure session on branch `conc/hdp-orlicz-triangle` of the StatLean project (Lean v4.29.1, pinned Mathlib `5e932f97` pre-built under `.lake/packages/mathlib`). You are inside an srun allocation on FAS-RC: run plain `lake build <Target>`; NEVER `lake update`; never touch `lakefile.lean` / `lake-manifest.json` / `lean-toolchain`. Iterate until GREEN.

CONTEXT: this worktree has no CLAUDE.md/notes/tools (gitignored). Everything you need is in this prompt + the stub files. Mathlib search: `grep -rn "<name>" .lake/packages/mathlib/Mathlib/` and in-proof `exact?` / `apply?` / `rw?`. Upstream StatLean lemmas in OTHER files are `sorry`-backed statements — cite them as black boxes; only YOUR file must become sorry-free.

## Touch-set (edit ONLY this file)
`StatLean/ConcentrationInequalities/Orlicz/Triangle.lean`
Do NOT edit any other file. Do NOT change any statement, hypothesis, constant, or tag. If a statement looks unprovable AS STATED, stop and report precisely why — do not weaken it.

## Build gate
`lake build StatLean.ConcentrationInequalities.Orlicz.Triangle` — exit 0.

## The goal — close these sorries
Read the file: `orliczNorm_add_le` (general ψ: monotone-on-Ici-0 + convex-on-Ici-0 + measurable), the ψ₂/ψ₁ instances `subGaussianNorm_add_le` / `subExponentialNorm_add_le`, and any finset-sum corollary present.

Definitions in `Orlicz/Defs.lean` (read first): `orliczSet`, `orliczNorm = ⨅ K ∈ orliczSet ψ X μ, (K : ℝ≥0∞)`, `orliczNorm_le_of_mem` (proven), `mem_orliczSet_iff`. Generator facts in `Orlicz/Generators.lean` (`psiTwo_convexOn`, `psiTwo_monotoneOn`, `psiTwo_measurable`, ψ₁ analogues — sorry-backed, citable). The attainment lemma `lintegral_le_one_of_orliczNorm_le` and the ε-extraction helper live in `Orlicz/Attainment.lean` (sorry-backed, citable — they are being closed by a concurrent session).

## Proof roadmap (HDP Exercise 2.42; the classical Luxemburg argument)
- Core inequality: if `a ∈ orliczSet ψ X μ` and `b ∈ orliczSet ψ Y μ` then `a + b ∈ orliczSet ψ (X + Y) μ`. Pointwise: `|X+Y|/(a+b) ≤ (a/(a+b))·(|X|/a) + (b/(a+b))·(|Y|/b)` (triangle + algebra); then monotonicity (arguments in `Ici 0`) + convexity of ψ:
  `ψ(|X+Y|/(a+b)) ≤ (a/(a+b))·ψ(|X|/a) + (b/(a+b))·ψ(|Y|/b)`.
  Integrate: `∫⁻ ofReal(...) ≤ (a/(a+b))·1 + (b/(a+b))·1 = 1` — use `ENNReal.ofReal_add_le`-style splitting, `lintegral_add_le`-type lemmas (`MeasureTheory.lintegral_add_left/right` need measurability: that is what `hψ_meas` + `hX`/`hY` AEMeasurable provide via `(hψ_meas.comp ...)`-style `AEMeasurable` compositions), and `ENNReal.ofReal_mul` for the nonneg scalars.
- From the gauge-set closure to the norm inequality in ℝ≥0∞: for any `c > orliczNorm X + orliczNorm Y`, pick `a`-gauge and `b`-gauge with `a + b < c` (ε-extraction from both infima — cite `exists_mem_orliczSet_lt_of_orliczNorm_lt` from Attainment.lean if present, else do the `iInf` extraction inline as a private helper); conclude `orliczNorm (X+Y) ≤ a + b ≤ c`; finish by `le_of_forall_lt`-style (`ENNReal.le_of_forall_pos_le_add` or `le_of_forall_gt_iff_le`). Mind the `⊤` cases: if either norm is `⊤` the RHS is `⊤` and the inequality is trivial (`le_top`).
- ψ₂/ψ₁ instances: apply the general lemma with the Generators facts.
- The lambda in the statement is `fun ω => X ω + Y ω` — keep that exact shape (β-reduction with `simp only []` or `show` as needed).

## Fallback (graded)
If after honest effort exactly one lemma resists, it must be `orliczNorm_add_le` itself (leave its single `sorry`; the instances then follow from it once closed later — they must still be derived FROM it, not separately sorried).

## Constraints
- New Mathlib imports fine; StatLean imports only from `StatLean.ConcentrationInequalities.*`.
- `private` named helpers allowed; NO sorried `have`s, no `axiom`/`admit`, no new hypotheses.
- Keep all tags/docstrings intact.
- Commit: `git add StatLean/ConcentrationInequalities/Orlicz/Triangle.lean && git commit -m "conc/hdp-orlicz-triangle: close Ex 2.42 triangle"`.

## DONE means
`lake build StatLean.ConcentrationInequalities.Orlicz.Triangle` exits 0; at most the designated fallback sorry remains; report closed sorries + key Mathlib lemmas.