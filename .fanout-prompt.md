# hdp-vc-lln — Theorem 8.3.15, VC law of large numbers (finite core) [HDP §8.3.6]

You are a Lean 4 proof-closure session on branch `conc/hdp-vc-lln` of the StatLean project (Lean v4.29.1, pinned Mathlib `5e932f97` pre-built under `.lake/packages/mathlib`). You are inside an srun allocation on FAS-RC: run plain `lake build <Target>`; NEVER `lake update`; never touch `lakefile.lean` / `lake-manifest.json` / `lean-toolchain`. Iterate until GREEN.

CONTEXT: this worktree has no CLAUDE.md/notes/tools (gitignored). Everything you need is in this prompt + the stub files. Mathlib search: `grep -rn "<name>" .lake/packages/mathlib/Mathlib/` and in-proof `exact?` / `apply?` / `rw?`. Upstream StatLean lemmas in OTHER files are `sorry`-backed statements — cite them as black boxes (by design); only YOUR file must become sorry-free.

## Touch-set (edit ONLY this file)
`StatLean/ConcentrationInequalities/VC/LawOfLargeNumbers.lean`
Do NOT edit any other file. Do NOT change any statement, hypothesis, constant, or tag. If a statement looks unprovable AS STATED, stop and report precisely why — do not weaken it.

## Build gate
`lake build StatLean.ConcentrationInequalities.VC.LawOfLargeNumbers` — exit 0.

## The goal — close these sorries
1. `integral_indicator_one_real` — `∫ S.indicator 1 dμ = μ.real S` (small adapter).
2. `finset_sup'_eq_iSup_subtype` — `Finset.sup'` ↔ `⨆` over the subtype (adapter).
3. `subGaussianIncrements_inner_signVec` — the Rademacher linear process `v ↦ ⟨ε, v⟩` on a finite `T' ⊆ EuclideanSpace ℝ (Fin n)` has `SubGaussianIncrements` with `K = √6` under `signVec n` (w.r.t. the Euclidean metric).
4. `rademacher_process_expectation_le` — conditional Dudley step: for a fixed sample `x : Fin n → Ω` and finite class `F`, the expected Rademacher sup is `≤ (2700/2)·√d/√n`-flavored (read the stub's exact frozen form; fallback — see below).
5. `symmetrization_adapter` — Exercise 8.11 consumption: reshape `Symmetrization/Empirical.empirical_symmetrization` (Fintype ι index, `μ.map (X i) = P` law form, `μ.prod (signVec n)` product) to the `Finset (Set Ω)` + `empFrac` shape.
6. `vc_lln_finset` — HDP Theorem 8.3.15 finite core: `≤ 5400·√d/√n` (frozen; formula `2 × 40 × √6 × 27` per the docstring).

READ FIRST (toolbox; statements frozen, possibly sorry-backed):
- `VC/Defs.lean` (fully proven): `Shatters`, `vcDim`, `traceFamily`, `empFrac` (+ its `nonneg/le_one/pos_iff/measurable`).
- `Symmetrization/Rademacher.lean`: `radLaw`, `signVec n` (= `Measure.pi fun _ => radLaw`), `iIndepFun_eval_signVec`, `signVec_ae_pm`, instances.
- `Symmetrization/Empirical.lean`: `empirical_symmetrization` (EXACT statement — read it; index `ι` with `[Fintype ι] [Nonempty ι]`, class `F : ι → α → ℝ` bounded by 1, RHS `2 * ∫ ⨆ k |n⁻¹ ∑ p.2 i · F k (X i p.1)| ∂(μ.prod (signVec n))`).
- `Chaining/Dudley.lean`: `dudley_inequality_abs` (`≤ 40·K·dudleyIntegral T D`, finite T, anchor, `diam ≤ D`).
- `Chaining/SubGaussianIncrements.lean`: def + `mono_set`/`mono_const`.
- `VC/EntropyIntegral.lean`: `entropyIntegral_le` (`∫ ε in Ioc 0 2, √(22·d·log(4/ε)) ≤ 27·√d`-shaped; read exact form).
- `VC/CoveringByVC.lean`: Theorem 8.3.13-shaped covering bound for the class in empirical L² (read the exact export: separation/covering in `empFrac`-metric with `(2/ε)^{21d}`).
- `Orlicz/SumNorm.lean` / `Orlicz/TailToNorm.lean`: B3 (`subGaussianNorm_le_of_isSubGaussian`, `√(6σ2)`), weighted-sum norms.
- Existing proven: `SubGaussian/Hoeffding.lean`, `Maximal/FiniteMaximal.lean`.

## Proof roadmap (HDP pp. 239–241; the stub docstrings carry the full frozen constant bookkeeping — follow them)
- (1)/(2): `integral_indicator` (Mathlib: `MeasureTheory.integral_indicator_one` or `integral_indicator_const` + `measureReal` unfolding — grep `measureReal_def`, `Measure.real`); `Finset.sup'` vs `ciSup` over `↥F`: grep `Finset.sup'_eq_ciSup` in Mathlib — it may exist verbatim.
- (3): coordinates of `signVec` are iid `radLaw` (`iIndepFun_eval_signVec`), each `∈ [−1,1]` a.e. (`signVec_ae_pm`) hence `hasSubgaussianMGF_of_mem_Icc`-style with proxy 1 per coordinate; for `v, w ∈ T'`: `⟨ε,v⟩ − ⟨ε,w⟩ = ∑ εᵢ(vᵢ−wᵢ)` — Mathlib `HasSubgaussianMGF.sum_of_iIndepFun` with per-index proxies `(vᵢ−wᵢ)²` sums to `‖v−w‖²`; then B3 gives `subGaussianNorm ≤ √(6·‖v−w‖²) = √6 · dist v w`. Mind: `EuclideanSpace` inner/dist unfolding (`EuclideanSpace.dist_eq`), and the increments def uses `edist` — convert via `edist_dist`/`ENNReal.ofReal`.
- (4): fixed sample `x`; project the class: `T' := (empProj-style image of F under S ↦ (n^{-1/2}·𝟙_S(xᵢ))ᵢ or the stub's exact normalization) ∪ {0}`; increments from (3) via `mono_set` + `mono_const` scaling; `dudley_inequality_abs` at anchor 0, `D = 2`; covering numbers of `T'` in the scaled empirical metric from `VC/CoveringByVC` (valid for EVERY probability measure, applied at the empirical one); entropy integral via `VC/EntropyIntegral.entropyIntegral_le`. Constant per docstring.
- (5): instantiate `ι := ↥F` (`Fintype` from `F.finite_toSet.fintype` or `FinsetCoe.fintype`, `Nonempty` from `hFne`), `F k := (↑k : Set Ω).indicator 1`, `α := Ω`; bounded by 1 (`Set.indicator` of 1); law transfer `∫ 𝟙_S d(P.map (X i)) = μ.real S` via (1) + `hlaw`; rewrite both sides between `⨆ k` (subtype) and `Finset.sup'` via (2). Mind `Fin n` vs `ℕ` indexing: the Empirical statement uses `X : Fin n → Ω → α` — our `X : ℕ → Ξ → Ω` restricts by composition with `Fin.val`; independence restricts via `iIndepFun.precomp`-style (grep `iIndepFun` API: `iIndepFun.comp`, `.precomp`, or reindex through `Fin.valEmbedding` — find the pin's exact name).
- (6): chain (5) → Fubini on `P.prod (signVec n)` (`MeasureTheory.integral_prod`) → for each fixed `ξ`, (4) → collect: `2 × (per-sample bound)`. Constant: `2 × 2700 = 5400` per the docstrings.

## Fallback (graded)
If exactly one goal resists, it must be `rademacher_process_expectation_le` (leave its single `sorry`). (1)(2)(3)(5)(6) MUST close — (6) may then cite the fallback.

## Constraints
- New Mathlib imports fine; StatLean imports only from `StatLean.ConcentrationInequalities.*`.
- `private` named helpers allowed; NO sorried `have`s, no `axiom`/`admit`, no new hypotheses.
- Keep all tags/docstrings intact.
- Commit: `git add StatLean/ConcentrationInequalities/VC/LawOfLargeNumbers.lean && git commit -m "conc/hdp-vc-lln: close Thm 8.3.15 finite core"`.

## DONE means
`lake build StatLean.ConcentrationInequalities.VC.LawOfLargeNumbers` exits 0; at most the designated fallback sorry remains; report closed sorries + key Mathlib lemmas.