# hdp-emp-lln — Theorem 8.2.3, Lipschitz law of large numbers assembly [HDP §8.2]

You are a Lean 4 proof-closure session on branch `conc/hdp-emp-lln` of the StatLean project (Lean v4.29.1, pinned Mathlib `5e932f97` pre-built under `.lake/packages/mathlib`). You are inside an srun allocation on FAS-RC: run plain `lake build <Target>`; NEVER `lake update`; never touch `lakefile.lean` / `lake-manifest.json` / `lean-toolchain`. Iterate until GREEN.

CONTEXT: this worktree has no CLAUDE.md/notes/tools (gitignored). Everything you need is in this prompt + the stub files. Mathlib search: `grep -rn "<name>" .lake/packages/mathlib/Mathlib/` and in-proof `exact?` / `apply?` / `rw?`. Upstream StatLean lemmas in OTHER files are `sorry`-backed statements — cite them as black boxes (by design); only YOUR file must become sorry-free.

## Touch-set (edit ONLY this file)
`StatLean/ConcentrationInequalities/EmpiricalProcess/LipschitzLLN.lean`
Do NOT edit any other file. Do NOT change any statement, hypothesis, constant, or tag. If a statement looks unprovable AS STATED, stop and report precisely why — do not weaken it.

## Build gate
`lake build StatLean.ConcentrationInequalities.EmpiricalProcess.LipschitzLLN` — exit 0.

## The goal — close these sorries
1. `expectation_sup'_netUnion_le` — the per-`m` finite Dudley plug: expected `Finset.sup'` of `|empiricalProcess|` over the finite net union is `≤ 960/√n`-flavored bound (fallback; see below).
2. `expectation_sup_lipschitzUnitClass_le` — the 1-Lipschitz `[0,1]`-valued class bound via the monotone-convergence lift over `m`.
3. `lipschitz_lln` — HDP Theorem 8.2.3 headline: genuine full `L`-Lipschitz class, `≤ lipschitzLLNConst · L / √n` with `lipschitzLLNConst = 960` (frozen; formula `80·√6·√24 = 960` per the module docstring).

READ FIRST (your toolbox; statements frozen, possibly sorry-backed):
- `EmpiricalProcess/Defs.lean` (fully proven): `empiricalProcess P n X f ω = n⁻¹ ∑ f(Xᵢω) − ∫ f dP`, linearity/translation lemmas, `abs_empiricalProcess_sub_le` (|X_f − X_g| ≤ 2‖f−g‖∞ pointwise — the honesty engine).
- `EmpiricalProcess/LipschitzNet.lean`: `lipschitzNet m`, `lipschitzNetUnion m` (finite cone-envelope nets in `C(unitInterval, ℝ)`), `zero_mem_lipschitzNet`.
- `EmpiricalProcess/LipschitzApprox.lean`: `exists_mem_lipschitzNet_dist_le` (3/m-approximation).
- `EmpiricalProcess/LipschitzCovering.lean`: covering exports `coveringNumber_lipschitzUnitClass_ne_top`, `.toNat ≤ exp (12/ε)`-shaped bounds (subset-monotone versions for arbitrary `T` ⊆ class).
- `EmpiricalProcess/SubGaussianIncrements.lean`: `subGaussianIncrements_empiricalProcess` — the `SubGaussianIncrements (fun f ω => empiricalProcess P n X ↑f ω) (√6/√n) Set.univ μ`-shaped package (via B3), plus the MGF core.
- `EmpiricalProcess/LipschitzDense.lean`: R1 (scaling), R2 (shift), R3 (dense-net exhaustion) pointwise iSup identities.
- `Chaining/DudleyConsumers.lean`: `dudley_abs_of_cov_le_exp_div` — evaluated corollary `∫ ⨆_{t ∈ T} |X_t − X_{t₀}| ≤ 80·K·√(C·D)` under `𝒩(T,ε).toNat ≤ exp(C/ε)` on `(0, D]`, `diam T ≤ D`, finite T, anchor `t₀ ∈ T`.
- `Chaining/SubGaussianIncrements.lean`: `SubGaussianIncrements.mono_set` for the `Set.univ → net` restriction.

## Proof roadmap (HDP pp. 229–231)
- Per-`m` plug (`expectation_sup'_netUnion_le`): `T := ↑(lipschitzNetUnion m)` as a finite subset of `C(unitInterval,ℝ)` with the sup metric; anchor `t₀ := 0` (`zero_mem_lipschitzNet` — check the exact membership export; the stub docstring names it); increments: restrict the `Set.univ` package via `mono_set`; `K = √6/√n`; covering bound: the class-level `exp(12/ε)` export restricted to the net subset (`Metric.coveringNumber_subset_le` at ε/2 or the exported subset form — follow the stub's docstring plan); diam ≤ 1 (all members `[0,1]`-valued ⇒ dist ≤ 1; check the exported diam lemma in LipschitzCovering/Net). Apply `dudley_abs_of_cov_le_exp_div` with `C = 24` (subset loss 12→24 per the docstring formula) and `D = 1`: bound `80·(√6/√n)·√24 = 960/√n`. Note `X_0 = empiricalProcess ... 0 = 0` identically (`empiricalProcess_zero_fun`), so the anchored sup IS the plain sup of absolute values.
- Lift over `m` (`expectation_sup_lipschitzUnitClass_le`): `LipschitzDense.iSup_abs_empiricalProcess_eq_iSup_netUnion`-shaped R3 identity turns `⨆` over the class into `⨆_m` of the finite `sup'`s; monotone convergence (`MeasureTheory.integral_iSup`-style — for a monotone sequence of nonneg integrands with uniformly bounded integrals; grep `lintegral_iSup` / `integral_tendsto_of_monotone` — or work in `∫⁻` and descend) with the constant dominant from `abs_empiricalProcess_le` (≤ 2).
- Headline: R1/R2 identities (scaling `L`, shift to `[0,1]`-valued) reduce the full `L`-Lipschitz class to the unit class; `NNReal` coercion care for `L = 0` (class of constants — empirical process vanishes; the stub docstring notes the degenerate case).

## Fallback (graded)
If exactly one goal resists, it must be `expectation_sup'_netUnion_le` (leave its single `sorry`). The MCT lift and the headline unwrapping MUST close against it.

## Constraints
- New Mathlib imports fine; StatLean imports only from `StatLean.ConcentrationInequalities.*`.
- `private` named helpers allowed; NO sorried `have`s, no `axiom`/`admit`, no new hypotheses.
- Keep all tags/docstrings intact.
- Commit: `git add StatLean/ConcentrationInequalities/EmpiricalProcess/LipschitzLLN.lean && git commit -m "conc/hdp-emp-lln: close Thm 8.2.3 Lipschitz LLN assembly"`.

## DONE means
`lake build StatLean.ConcentrationInequalities.EmpiricalProcess.LipschitzLLN` exits 0; at most the designated fallback sorry remains; report closed sorries + key Mathlib lemmas.