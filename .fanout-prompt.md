# hdp-orlicz-product — Lemmas 2.8.5 & 2.8.6, ψ₂/ψ₁ product calculus [HDP §2.8.2]

You are a Lean 4 proof-closure session on branch `conc/hdp-orlicz-product` of the StatLean project (Lean v4.29.1, pinned Mathlib `5e932f97` pre-built under `.lake/packages/mathlib`). You are inside an srun allocation on FAS-RC: run plain `lake build <Target>`; NEVER `lake update`; never touch `lakefile.lean` / `lake-manifest.json` / `lean-toolchain`. Iterate until GREEN and COMMIT BEFORE ENDING (do not end your turn while a build is still running — wait for it, then commit).

CONTEXT: this worktree has no CLAUDE.md/notes/tools (gitignored). Everything you need is in this prompt + the stub files. Mathlib search: `grep -rn "<name>" .lake/packages/mathlib/Mathlib/` and in-proof `exact?` / `apply?` / `rw?`. Upstream StatLean lemmas in OTHER files are `sorry`-backed statements — cite them as black boxes; only YOUR file must become sorry-free. NOTE: `Orlicz/Attainment.lean` and `Orlicz/Triangle.lean` carry REAL proofs on this branch (merged) — their lemmas are fully usable.

## Touch-set (edit ONLY this file)
`StatLean/ConcentrationInequalities/Orlicz/Product.lean`
Do NOT edit any other file. Do NOT change any statement, hypothesis, constant, or tag. If a statement looks unprovable AS STATED, stop and report precisely why — do not weaken it.

## Build gate
`lake build StatLean.ConcentrationInequalities.Orlicz.Product` — exit 0.

## The goal — close the file's sorries (read it for exact statements)
1. `subExponentialNorm_sq` — Lemma 2.8.5: `‖X²‖_{ψ₁} = ‖X‖²_{ψ₂}` EXACTLY (fallback; see below).
2. `subExponentialNorm_mul_le` — Lemma 2.8.6: `‖XY‖_{ψ₁} ≤ ‖X‖_{ψ₂}·‖Y‖_{ψ₂}`.

## Proof roadmap (HDP p. 45; the design's gauge-set bijection strategy)
- **2.8.5**: the integrands are LITERALLY EQUAL after rewriting: `psiOne (|X ω|²/k²) = exp(|X²ω|/k²) − 1 = exp((|X ω|/k)²) − 1 = psiTwo (|X ω|/k)` (use `sq_abs`, `abs_sq`, `div_pow`, `mul_pow` algebra — mind `X² ω = (X ω)²` shape in the stub). Hence the gauge-set bijection `K ∈ orliczSet ψ₁ (X²) μ ↔ NNReal.sqrt K ∈ orliczSet ψ₂ X μ` (with `K = (√K)²`, `NNReal.sq_sqrt`, positivity transfers). The ℝ≥0∞ `⨅`-squaring: prove `≤` both ways via the ε-extraction lemma `exists_mem_orliczSet_lt_of_orliczNorm_lt` (in `Orlicz/Attainment.lean` — REAL, usable) — for any `c > ‖X‖²_{ψ₂}` pick a ψ₂-gauge `a` with `a² < c'` between, then `a²` is a ψ₁-gauge for `X²` below `c`; symmetric for the reverse. `ENNReal.pow_lt_pow_left`/`sq` monotonicity, `ENNReal.le_of_forall_lt`-style closure (`le_of_forall_gt_iff_le` or `ENNReal.le_of_forall_pos_le_add` — pick what elaborates).
- **2.8.6**: WLOG-free per the batch design: for gauges `a` of `X` (ψ₂) and `b` of `Y` (ψ₂), show `a·b` is a ψ₁-gauge of `XY`: pointwise `|XY|/(ab) ≤ ½((|X|/a)² + (|Y|/b)²)` (AM-GM: `|u·v| ≤ (u²+v²)/2` — `abs_mul`, `nlinarith [sq_nonneg (|X ω|/a − |Y ω|/b)]`), then `exp(w) ≤ ½(exp u² + exp v²)` for `w ≤ ½(u²+v²)` via exp-monotonicity + convexity of exp (`Real.exp_le_exp`, `Real.add_pow_le…` no — direct: `exp(½(s+t)) ≤ ½(exp s + exp t)` is exp-convexity: `Real.exp_sum_div…`? use `Real.inner_le_iff`? simplest: `Real.exp ((s+t)/2) = √(exp s · exp t) ≤ (exp s + exp t)/2` via `Real.sqrt_mul'`, `Real.sqrt_le_sqrt`-AM-GM `Real.sqrt_mul_self`… or `Real.add_sq_le_sq_add_sq`? — the geometric-mean route: `Real.exp_add`, `Real.sq_sqrt`, `two_mul_le_add_sq`-style; `nlinarith [sq_nonneg (Real.exp (s/2) − Real.exp (t/2))]` closes `√(eˢeᵗ) ≤ (eˢ+eᵗ)/2` after squaring). Integrate: `∫⁻ ψ₁(|XY|/(ab)) ≤ ½∫⁻ ψ₂(|X|/a) + ½∫⁻ ψ₂(|Y|/b) ≤ ½·1 + ½·1 = 1` (`lintegral` linearity with AEMeasurable side conditions from the stub hypotheses; `ENNReal.ofReal` algebra: `ofReal_le_ofReal` pointwise then `lintegral_mono_ae`, splitting via `ofReal_add` on the nonneg decomposition — mind `ψ₁ = exp − 1`: `exp(w) − 1 ≤ ½(exp u² − 1) + ½(exp v² − 1)` follows from the same bound since `−1 = ½(−1) + ½(−1)` ✓). Close with the double ε-extraction as in 2.8.5 (`a·b` infimum arithmetic in ℝ≥0∞: `ENNReal.mul_lt_mul`).
- Attainment inputs: `lintegral_exp_sq_le_two_of_subGaussianNorm_le` (Attainment.lean, REAL) converts `norm ≤ a` to the integral condition.

## Fallback (graded)
If exactly one lemma resists, it must be `subExponentialNorm_sq` (the ℝ≥0∞ biInf-squaring gymnastics; leave its single `sorry`). Lemma 2.8.6 MUST close.

## Constraints
- New Mathlib imports fine; StatLean imports only from `StatLean.ConcentrationInequalities.*`.
- `private` named helpers allowed; NO sorried `have`s, no `axiom`/`admit`, no new hypotheses.
- Keep all tags/docstrings intact.
- Commit: `git add StatLean/ConcentrationInequalities/Orlicz/Product.lean && git commit -m "conc/hdp-orlicz-product: close Lem 2.8.5/2.8.6"`.

## DONE means
`lake build StatLean.ConcentrationInequalities.Orlicz.Product` exits 0 — WAIT for the build to finish before ending; at most the designated fallback sorry remains; report closed sorries + key Mathlib lemmas.