# hdp-orlicz-subexp-mgf — Prop 2.8.1 (iii)⇒(iv): restricted-range ψ₁ MGF [HDP §2.8]

You are a Lean 4 proof-closure session on branch `conc/hdp-orlicz-subexp-mgf` of the StatLean project (Lean v4.29.1, pinned Mathlib `5e932f97` pre-built under `.lake/packages/mathlib`). You are inside an srun allocation on FAS-RC: run plain `lake build <Target>`; NEVER `lake update`; never touch `lakefile.lean` / `lake-manifest.json` / `lean-toolchain`. Iterate until GREEN and COMMIT BEFORE ENDING (do not end while a build is in flight — wait, then commit).

CONTEXT: no CLAUDE.md/notes/tools in this worktree. Mathlib search: `grep -rn "<name>" .lake/packages/mathlib/Mathlib/` + `exact?`/`apply?`. Upstream StatLean lemmas may be sorry-backed — cite as black boxes. REAL on this branch: `Orlicz/Attainment.lean` (ψ₁ attainment `lintegral_exp_abs_le_two_of_subExponentialNorm_le`), `Orlicz/Triangle.lean`, `Orlicz/TailToNorm.lean`.

## Touch-set (edit ONLY this file)
`StatLean/ConcentrationInequalities/Orlicz/SubExponentialMGF.lean`
No statement/hypothesis/constant/tag changes. If unprovable AS STATED, stop and report.

## Build gate
`lake build StatLean.ConcentrationInequalities.Orlicz.SubExponentialMGF` — exit 0.

## The goal
Close the file's sorries (read it): the raw restricted-MGF core (`E exp(λX) ≤ exp(4λ²K²)` for `|λ| ≤ 1/(2K)` under `∫⁻ ofReal(exp(|X|/K)) ≤ 2` + mean-zero; book K₄ = 2K), its norm-facing wrapper, the `IsSubExponential` bridge into the existing carrier (see `SubExponential/Defs.lean` — proven area, restricted-range MGF structure), and the integrability helper (fallback).

## Proof roadmap (HDP p. 44, (iii)⇒(iv))
- Taylor with mean-zero: `e^u ≤ 1 + u + u²·e^{|u|}/2`-flavored (or the book's `e^u ≤ 1 + u + (u²/2)e^{|u|}`): for `u = λX`, `|λX| ≤ |X|/(2K)`, so `e^{|λX|} ≤ e^{|X|/(2K)} ≤ (e^{|X|/K})^{1/2}`... elementary route per the file's Proof-formalization notes (READ THEM — the designed chain is recorded there; `ForMathlib/ExpTaylorBounds.lean` stocks the pointwise bricks, sorry-backed citable).
- `E[1 + λX + …] = 1 + 0 + …` (mean-zero kills the linear term — integrability of `X` derives from the exp-moment: `|x|/K ≤ e^{|x|/K}` ⇒ finite first moment; `integrable_pow_of_integrable_exp_mul`-family at pin).
- Cauchy–Schwarz/interpolation for the quadratic term against `E e^{|X|/K} ≤ 2`; land `E e^{λX} ≤ 1 + 4λ²K² ≤ e^{4λ²K²}` (`Real.add_one_le_exp`).
- `IsSubExponential` bridge: its constitutive field is a restricted-range MGF bound (read `SubExponential/Defs.lean` for the exact field shapes: `mgf_le` on `|λ| ≤ 1/α` + `integrable_exp_mul`); instantiate `α := 2K`-shaped per the stub statement.

## Fallback (graded)
Only the integrability helper named in the file (read its docstring; `mgf_le_of_lintegral_exp_abs_le_two` itself MUST close — check which decl the docstring designates as this item's fallback and honor exactly that).

## Constraints
No `axiom`/`admit`; no new hypotheses; keep tags; `private` named helpers OK.
Commit: `git add StatLean/ConcentrationInequalities/Orlicz/SubExponentialMGF.lean && git commit -m "conc/hdp-orlicz-subexp-mgf: close Prop 2.8.1(iv)"`.

## DONE means
Target builds exit 0 (WAIT for it); ≤ 1 designated fallback sorry; report closures + key lemmas.