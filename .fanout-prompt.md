# hdp-orlicz-tail-to-norm — layer-cake engine + B3 reverse bridge [HDP Prop 2.6.1 (i)⇒(iii)]

You are a Lean 4 proof-closure session on branch `conc/hdp-orlicz-tail-to-norm` of the StatLean project (Lean v4.29.1, pinned Mathlib `5e932f97` pre-built under `.lake/packages/mathlib`). You are inside an srun allocation on FAS-RC: run plain `lake build <Target>`; NEVER `lake update`; never touch `lakefile.lean` / `lake-manifest.json` / `lean-toolchain`. Iterate until GREEN.

CONTEXT: this worktree has no CLAUDE.md/notes/tools (gitignored). Everything you need is in this prompt + the stub files. Mathlib search: `grep -rn "<name>" .lake/packages/mathlib/Mathlib/` and in-proof `exact?` / `apply?` / `rw?`. Upstream StatLean lemmas in OTHER files are `sorry`-backed statements — cite them as black boxes; only YOUR file must become sorry-free.

## Touch-set (edit ONLY this file)
`StatLean/ConcentrationInequalities/Orlicz/TailToNorm.lean`
Do NOT edit any other file. Do NOT change any statement, hypothesis, constant, or tag. If a statement looks unprovable AS STATED, stop and report precisely why — do not weaken it.

## Build gate
`lake build StatLean.ConcentrationInequalities.Orlicz.TailToNorm` — exit 0.

## The goal — close the file's sorries (read it for exact statements)
The layer-cake core (gaussian-type tails `μ{t ≤ |X|} ≤ ofReal(2·exp(−t²/K₁²))` ⇒ ψ₂ gauge condition at `K₃ = √3·K₁`), its norm conclusion (`subGaussianNorm X μ ≤ √3·K₁`-shaped), the improper-integral computation (this file's fallback `integral_Ioi_self_mul_exp_neg_mul_sq`), and **B3** `subGaussianNorm_le_of_isSubGaussian : IsSubGaussian X σ2 μ → (mean-zero) → subGaussianNorm X μ ≤ NNReal.sqrt (6·σ2)`.

## Proof roadmap (HDP p. 36 (i)⇒(iii), rerouted via layer-cake per the batch design — avoids the absent Γ-bound)
- FTC identity: `exp(u²/K₃²) − 1 = ∫ t in (0, u], (2t/K₃²)·exp(t²/K₃²)` for `u ≥ 0` (via `intervalIntegral.integral_deriv_eq_sub`-family / `integral_Ioi_of_hasDerivAt_of_tendsto`; derivative `HasDerivAt (fun t => exp(t²/K₃²)) …` by `Real.hasDerivAt_exp` composed with `hasDerivAt_pow` — chain rule `HasDerivAt.comp`).
- Weighted layer-cake (PIN-VERIFIED): `MeasureTheory.lintegral_comp_eq_lintegral_meas_le_mul` — `∫⁻ ofReal(∫_0^{|X ω|} g) dμ = ∫⁻_{Ioi 0} μ{t ≤ |X|}·ofReal(g t)` with `g t = (2t/K₃²)·exp(t²/K₃²)` (side conditions: `g` integrable-on-finite-intervals + nonneg — check the lemma's exact signature by grepping it).
- Plug the tail bound: RHS ≤ `∫_{Ioi 0} 2·exp(−t²/K₁²)·(2t/K₃²)·exp(t²/K₃²) dt`; with `K₃² = 3K₁²`: exponent `−t²/K₁² + t²/(3K₁²) = −(2/3)·t²/K₁²`; evaluate `∫_{Ioi 0} t·exp(−c·t²) dt = 1/(2c)` (the fallback lemma; via `integral_Ioi_of_hasDerivAt_of_tendsto` with antiderivative `−exp(−c t²)/(2c)`, tendsto-to-0 at ∞ via `Real.tendsto_exp_neg_atTop_nhds_zero`-flavored composition). Total: `(4/(3K₁²))·(1/(2·(2/3)/K₁²)) = (4/3)·(3/4) = 1` — the gauge condition `∫⁻ ψ₂(|X|/K₃) ≤ 1` EXACTLY (this is why √3 is the frozen constant). Mind `ENNReal.ofReal` algebra: keep everything in `∫⁻` with `ofReal`-multiplicativity (`ENNReal.ofReal_mul`, nonneg side goals by `positivity`).
- Norm conclusion: gauge condition at `K₃` + `orliczNorm_le_of_mem` (proven, `Orlicz/Defs.lean`).
- **B3**: `IsSubGaussian X σ2 μ` (see `SubGaussian/Defs.lean`, proven area) + `hmean` gives Mathlib `HasSubgaussianMGF X σ2 μ` of X itself (centering = identity under mean-zero, `sub_zero`-congr); its tail `HasSubgaussianMGF.measure_ge_le` (grep exact form: `μ.real {ε ≤ X} ≤ exp(−ε²/(2σ2))`) — apply to `X` and `−X` (`.neg`) for the two-sided `μ{t ≤ |X|} ≤ 2·exp(−t²/(2σ2))`-shaped bound (union bound `{t ≤ |X|} ⊆ {t ≤ X} ∪ {t ≤ −X}`, `measure_union_le`; convert `μ.real` to `μ`-with-`ofReal` — grep `measureReal_def` / `Measure.real`); then the layer-cake core at `K₁ = √(2σ2)` gives `subGaussianNorm ≤ √3·√(2σ2) = √(6σ2)` — final coercion algebra in `NNReal` (`NNReal.sqrt_mul`, `NNReal.coe_sqrt`).

## Fallback (graded)
If exactly one lemma resists, it must be `integral_Ioi_self_mul_exp_neg_mul_sq` (the improper integral; leave its single `sorry`). The layer-cake core, the norm conclusion, and B3 MUST close (they may cite the fallback).

## Constraints
- New Mathlib imports fine; StatLean imports only from `StatLean.ConcentrationInequalities.*`.
- `private` named helpers allowed; NO sorried `have`s, no `axiom`/`admit`, no new hypotheses.
- Keep all tags/docstrings intact.
- Commit: `git add StatLean/ConcentrationInequalities/Orlicz/TailToNorm.lean && git commit -m "conc/hdp-orlicz-tail-to-norm: close layer-cake engine + B3"`.

## DONE means
`lake build StatLean.ConcentrationInequalities.Orlicz.TailToNorm` exits 0; at most the designated fallback sorry remains; report closed sorries + key Mathlib lemmas.