# Fix and close ForMathlib/NoncentralChiSquared.lean (ONE file)

**RUN EVERY BUILD IN THE FOREGROUND. Never background a build, never `srun`/`sbatch` from inside, never `sleep`/poll, never schedule a wakeup. Plain `lake build StatLean.HypothesisTesting.ForMathlib.NoncentralChiSquared` and WAIT for it. If you background a build you will run out of turns with the work unverified — that is the one failure mode we must avoid.**

Lean 4 / Mathlib proof engineer on `StatLean`. Pin `v4.29.1`. (The repo `CLAUDE.md` is gitignored and NOT in this worktree — all rules you need are below. Project rules: never `lake update`; `sorry` is planned debt tied to a named lemma; never launder unproven content into hypotheses; lines ≤ 100 chars.)

You are ON the cluster in a fan-out worktree on branch `ht/gchisq-fix`.

## Scope — edit ONLY this one file
`StatLean/HypothesisTesting/ForMathlib/NoncentralChiSquared.lean`. Touch nothing else. It must keep **Mathlib-only imports** (it is a ForMathlib brick). You MAY add `import Mathlib.*`, `open`/`open scoped`, and `private` helpers. Signatures/docstrings of the two public headline theorems are FROZEN.

## State: the file currently has 8 compile errors + 4 sorries
A prior session wrote a whitening proof of the two bridge lemmas but left it non-compiling. The two headline theorems are:
- `multivariateGaussian_map_inner_inv_eq_chiSquared` — `N(0,S)` pushed through `z ↦ ⟪z, S⁻¹ z⟫` is `chiSquared k`.
- `multivariateGaussian_map_inner_inv_eq_noncentralChiSquared` — `N(μ,S)` pushed through the same map is `noncentralChiSquared k (⟪μ, S⁻¹ μ⟫).toNNReal`.

### Diagnosis of the 8 errors (fix these first, get it compiling-with-sorries)
1. **`unexpected token 'ᴴ'`** — the `ᴴ` (conjugate-transpose) notation is not in scope. Add `open scoped Matrix` near the top, OR replace every `Aᴴ` with `star A` / `Aᵀ` (real matrices: `conjTranspose = transpose`).
2. **`hS.nonneg` → `And.nonneg` does not exist** — `Matrix.PosDef` has no `.nonneg` projection. You need `0 ≤ S` in the star-order or a matrix square root.
3. **`failed to synthesize instance` at the `CFC.sqrt S` lines** — `CFC.sqrt` likely does NOT resolve its typeclass stack for `Matrix (Fin k) (Fin k) ℝ` at this pin.
   **STRONGLY PREFER the matrix-native square root instead of CFC**: from `hS : S.PosDef` take `hS.posSemidef : S.PosSemidef`, then `hS.posSemidef.sqrt` is the PSD square root. Available API (verify names with `exact?`/`loogle`): `Matrix.PosSemidef.posSemidef_sqrt`, `Matrix.PosSemidef.sqrt_mul_self` (or `sq_sqrt` / `sqrt_sq`), `Matrix.PosSemidef.isHermitian_sqrt`, and for invertibility use `Matrix.PosDef` ⇒ `IsUnit S.det`. Rebuild the whitening matrix `A = (sqrt S)⁻¹` on this API. This removes the CFC instance problem entirely.

### The 4 sorries (close after it compiles)
The whitening argument (see the file's docstring): with self-adjoint `A`, `A·A = S⁻¹`, `A·S·Aᵀ = I`:
`(N(μ,S)).map A = N(Aμ, I)` and `⟪z,S⁻¹z⟫ = ‖A z‖²`, so the quadratic form pushes to `‖·‖²` of a standard Gaussian `N(Aμ,I)` = `noncentralChiSquared k ‖Aμ‖²`, with `‖Aμ‖² = ⟪μ,S⁻¹μ⟫`. The central case is `μ=0`. Reuse whatever `multivariateGaussian_map_*` / `chiSquared` / `noncentralChiSquared` lemmas already exist in this file and in `StatLean.MultipleTesting.ForMathlib.ChiSquared` (search with `loogle`/`exact?`). If a genuine Mathlib gap remains, lift it to ONE named `private` lemma with a precise `-- TODO:` and report it — do not fake it.

## Discipline
- Commit after each lemma compiles (`git add -A && git commit -m '...'`).
- **If a headline statement is actually false, STOP, leave it sorried, and report the counterexample.** Honest refusal is the desired outcome, never weaken a statement to make it pass.
- When green: run `#print axioms multivariateGaussian_map_inner_inv_eq_chiSquared` and `#print axioms multivariateGaussian_map_inner_inv_eq_noncentralChiSquared` — expect only `propext, Classical.choice, Quot.sound`. Any `sorryAx` means a sorry is still reachable; report it.

## Report at the end
Final `lake build` status, the file's remaining sorry count and each sorry's named lemma, both `#print axioms` outputs, and any statement you believe false (with counterexample).