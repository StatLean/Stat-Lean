# hdp-bundle-final — VC assembly (Examples/EntropyIntegral/Countable/GC/Generalization) + SymmetrizationIndep [HDP §8.3–8.4 + §6.3]
You are a Lean 4 proof-closure session on branch conc/hdp-bundle-final of the StatLean project (Lean v4.29.1, pinned Mathlib 5e932f97 pre-built under .lake/packages/mathlib). Inside an srun allocation: plain `lake build <Target>`; NEVER `lake update`; never touch lakefile/manifest/toolchain. Iterate until GREEN and COMMIT BEFORE ENDING (wait for the final build, then commit).
CONTEXT: no CLAUDE.md/notes/tools here. Mathlib search: grep over .lake/packages/mathlib + exact?/apply?. REAL on this branch: essentially EVERYTHING (all Orlicz, all Symmetrization incl. Ex 8.11 + support, all Chaining, all EmpiricalProcess, VC/{Defs,SauerShelah,DimensionReduction,HalfSpaces,CoveringByVC spine,LawOfLargeNumbers} + ForMathlib). Statements FROZEN.
## Touch-set (ONLY these six; one at a time, building each)
1. StatLean/ConcentrationInequalities/VC/Examples.lean — gate `...VC.Examples`. Ex 8.3.2 intervals vc=2 (2-point shatter witness; 3-point p<q<r impossibility of the 101 labeling) + Ex 8.3.4 Fin 3 finite class vc=2 (decide-able finite checks — Finset.decide / explicit case analysis).
2. StatLean/ConcentrationInequalities/VC/EntropyIntegral.lean — gate `...VC.EntropyIntegral`. ∫₀^2 √(22·d·log(4/ε)) ≤ 27√d: √(ab) split (Real.sqrt_mul), log bound log(4/ε) ≤ log 4 + log(1/ε), √-subadditivity inline ((√a+√b)² ≥ a+b — CLAUDE-gotcha: Real.sqrt_add_le_sqrt_add_sqrt does NOT exist; prove via nlinarith [sq_nonneg ...]), ∫₀^2 √(log(1/ε)) evaluated/bounded via the ε^{-1/4} domination (log x ≤ 4x^{1/4}-style: Real.log_le_rpow-family or Real.add_one_le_exp manipulations) + integral_rpow; frozen 27 has slack per the docstring formula.
3. StatLean/ConcentrationInequalities/VC/LawOfLargeNumbersCountable.lean — gate `...VC.LawOfLargeNumbersCountable`. integral_biSup_le_of_forall_finset: countable exhaustion (Set.Countable.exists_eq_range / enumeration prefixes as Finsets), monotone convergence (integral_tendsto_of_tendsto_of_monotone with dominant 1 — mirror LipschitzLLN's MCT step, REAL) + le_of_tendsto; vc_lln_countable = instantiate with vc_lln_finset (REAL) via vcDim_mono on finite subfamilies.
4. StatLean/ConcentrationInequalities/VC/GlivenkoCantelli.lean — gate `...VC.GlivenkoCantelli`. vcDim_iicClass = 1 (singleton shatter witness; a < b with labeling {b} unrealizable since b ≤ x forces a ≤ x); right-continuity lemmas (empFrac: finite sum of Iic-indicators, right-continuous in threshold — pointwise eventually-constant on (t, t+min-gap); population: StieltjesFunction fields of ProbabilityTheory.cdf transported via measureReal — or direct: μ.real (Iic s) → μ.real (Iic t) as s ↓ t by measure continuity from above MeasureTheory.tendsto_measure_iInter_atTop-family); iSup_iic_diff_eq_iSup_rat via ForMathlib/SupRatApprox (REAL); glivenko_cantelli = instantiate vc_lln_countable at iicRatClass (countable: range over ℚ) + vcDim ≤ 1 (vcDim_mono into iicClass) + the ℝ→ℚ bridge; 5400·√1 = 5400 arithmetic.
5. StatLean/ConcentrationInequalities/VC/Generalization.lean — gate `...VC.Generalization`. shatters_symmDiff_image (labeling bijection T ↦ T ∆ (Λ ∩ Θ): symmDiff algebra — symmDiff_symmDiff_cancel, inf_symmDiff distributivity; grep Order.SymmDiff); vcDim_symmDiff_image (both inclusions via the involution); excess_risk_le_two_mul_sup (Eq 8.40 chain: R(Sn) − R(S₀) ≤ [R(Sn) − Rn(Sn)] + [Rn(S₀) − R(S₀)] using hERM Rn(Sn) ≤ Rn(S₀); each term ≤ sup via le_ciSup with BddAbove from |·| ≤ 1); vc_generalization = integrate the pointwise bound (integral_mono with the AEStronglyMeasurable hypothesis + integrable dominants) + vc_lln_countable on the symmDiff image class (vcDim equality) + 2·5400 = 10800.
6. StatLean/ConcentrationInequalities/Symmetrization/SymmetrizationIndep.lean — gate `...Symmetrization.SymmetrizationIndep`. Abstract-Ω corollaries of the REAL canonical 6.3.2 via ForMathlib/IndepTransport (REAL) + map_signs_eq_signVec (product-law identification from iIndepFun + per-coordinate map law: Measure.pi uniqueness — grep Measure.pi_eq / eq_pi of marginals+independence, or construct via IndepTransport's designed route per the docstring).
## Fallbacks (≤ ONE per file, docstring-designated only).
## Constraints
No axiom/admit; no new hypotheses; keep tags; private helpers OK.
Commit: git add StatLean/ConcentrationInequalities/ && git commit -m "conc/hdp-bundle-final: close VC assembly + sym-indep"
## DONE means
All six targets exit 0 (WAIT); ≤ 6 designated fallbacks; report per-file closures.