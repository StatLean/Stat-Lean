# Close the differentiation sorries in PointEstimation/ExponentialFamily/Smoothness.lean (ONE file)

**RUN EVERY BUILD IN THE FOREGROUND. Never background a build, never `srun`/`sbatch` from inside, never `sleep`/poll, never schedule a wakeup. Plain `lake build StatLean.PointEstimation.ExponentialFamily.Smoothness` and WAIT for it. Backgrounding a build wastes the whole session with work unverified — do not do it.**

Lean 4 / Mathlib proof engineer on `StatLean`. Pin `v4.29.1`. (The repo `CLAUDE.md` is gitignored and NOT in this worktree — rules you need are below. Project rules: never `lake update`; `sorry` is planned debt tied to a named lemma; never launder unproven content into hypotheses; lines ≤ 100 chars.)

You are ON the cluster in a fan-out worktree on branch `pe/expfam-smoothness`.

## Scope — edit ONLY this one file
`StatLean/PointEstimation/ExponentialFamily/Smoothness.lean`. Touch nothing else — NOT `ExponentialFamily/Defs.lean` (frozen, laptop-only). You MAY add `import Mathlib.*`, `open`, and `private` helpers.

## Target: 3 real sorries (leave the 4th — it's a sanctioned deferral)
This is Thm 5.8 "differentiation under the integral sign for exponential families." Close, in order:
1. `continuousOn_integral_exp_inner` — continuity on `interior (weightedNatSet f)`.
2. `hasFDerivAt_integral_exp_inner` — the Fréchet derivative is `innerSL ℝ (∫ (f·e^{⟪η,T⟫})•T)`.
3. `iteratedDeriv_integral_exp_inner` — the `n`-th directional derivative pulls down `⟪u,T⟫^n`.

**DO NOT attempt `analyticOnNhd_integral_exp_inner` (the 4th sorry).** Its docstring marks it DEFERRAL-ELIGIBLE (needs multivariate power-series machinery absent at this pin; no downstream consumer). Leave it `sorry` with its docstring intact. Report it as a retained named debt.

## How — the proof plans are already written as in-file `-- TODO:` comments; follow them
- The already-**proven** `analyticAt_integral_exp_mul` (same file) is your template: it splits `f = f⁺ − f⁻`, views `∫ f± e^{ηT}` as the `mgf` of the tilted measure `base.withDensity (ofReal ∘ f±)`, and uses `ProbabilityTheory.analyticAt_mgf`. Its private-style helpers `hmgf`, `hintw`, `hsub` (integrability + `integrableExpSet` membership on the weighted set) are exactly what the continuity/derivative dominations need — factor them out to `private` lemmas at file scope and reuse.
- Continuity & FDeriv: Mathlib's dominated-convergence differentiation API — `MeasureTheory.continuousAt_of_dominated` and `MeasureTheory.hasFDerivAt_integral_of_dominated_loc_of_lip` (verify the exact current name with `exact?`/`loogle '"hasFDerivAt_integral_of_dominated"'`). The local `bound` on a ball `B(η,r) ⊆ interior (weightedNatSet f)` is `|f x|·(1+‖T x‖)·e^{⟪η,T x⟫ + r‖T x‖}`; its integrability is the finite-dimensional `2^s` sign-vector envelope `e^{r‖T x‖} ≤ Σ_{ε∈{±1}^s} e^{⟪r·ε, T x⟫}` combined with `∫|f|e^{⟪η±rε,T⟫} < ∞` (each `η ± rε` is in `weightedNatSet f` because `η` is interior). `t·e^{rt} ≤ C_r·e^{2rt}` absorbs the `‖T x‖` factor.
- iteratedDeriv: induction on `n`, each step the same dominated-differentiation with weight `f·⟪u,T⟫^k`.

## PRE-AUTHORIZED statement adjustment (do this if needed, then report it)
The `2^s` sign-vector envelope needs `V` finite-dimensional. The three target theorems currently lack `[FiniteDimensional ℝ V]`. **You ARE authorized to add `[FiniteDimensional ℝ V]` to any of the three** (every consumer instantiates `V = EuclideanSpace ℝ (Fin k)`, so this is the classical setting, not a weakening of the contract). Add it only where actually needed, and list exactly which theorems you changed in your report. Any OTHER statement change is NOT authorized: if a conclusion looks false or needs a different hypothesis, STOP, leave it sorried, and report the counterexample — honest refusal is the desired outcome.

## Discipline
- Commit after each lemma compiles (`git add -A && git commit -m '...'`).
- When green: `#print axioms continuousOn_integral_exp_inner`, `#print axioms hasFDerivAt_integral_exp_inner`, `#print axioms iteratedDeriv_integral_exp_inner` — expect only `propext, Classical.choice, Quot.sound`. Report any `sorryAx`.

## Report at the end
Final `lake build` status, remaining sorry count (should be exactly 1: `analyticOnNhd_integral_exp_inner`), the three `#print axioms` outputs, whether you added `[FiniteDimensional ℝ V]` and to which theorems, and any statement you believe false (with counterexample).