# Close the closeable sorries in PointEstimation/Equivariance/ (5 files)

**RUN EVERY BUILD IN THE FOREGROUND. Never background a build, never `srun`/`sbatch` from inside, never `sleep`/poll, never schedule a wakeup. Plain `lake build <module>` and WAIT for it. Backgrounding a build wastes the whole session with work unverified — do not do it.**

Lean 4 / Mathlib proof engineer on `StatLean`. Pin `v4.29.1`. (The repo `CLAUDE.md` is gitignored and NOT in this worktree — rules you need are below. Project rules: never `lake update`; `sorry` is planned debt tied to a named lemma; never launder unproven content into hypotheses; lines ≤ 100 chars.)

You are ON the cluster in a fan-out worktree on branch `pe/equivariance-close`. This is the equivariant-estimation chapter (location/scale minimum-risk equivariant estimators).

## Scope — edit ONLY these 5 files
`StatLean/PointEstimation/Equivariance/{NoGroup,LocationExamples,ScaleStructure,LocationMRE,ScaleMRE}.lean`. Touch nothing else — NOT any `Defs.lean` (frozen, laptop-only). You MAY add `import Mathlib.*`, import closed project modules, and add `private` helpers. Public signatures/docstrings FROZEN (except the pre-authorized hypothesis additions below).

## Work in this order (EASY FIRST — bank quick wins before the hard debts)
Build + close + **commit after each lemma compiles**, in this order:

1. **`NoGroup.lean`** — `powerSeriesFamily_invariant_affine_core` (~line 87). Power-series family invariance under the affine group (TPE Thm 2.17). Algebraic; the other theorems in the file already build on it.
2. **`LocationExamples.lean`** — the one remaining sorry (Gaussian sample-mean is location-MRE; concrete Gaussian computation — the file already has `pitmanEstimator_gaussDensity`, `integral_id_mul_gaussianPDFReal` helpers proven above it; reuse them).
3. **`LocationMRE.lean` `exists_isLocMRE_of_bounded_loss`** (~line 273) — existence under a bounded loss (compactness / continuity of the conditional-risk minimand).
4. **`ScaleMRE.lean` `exists_isScaleMRE_of_convex`** (~line 307) — scale analogue of the convex-loss existence. (Only route it through the analytic-core lemma below if unavoidable; if it depends on that debt, note the taint and lift it too.)

## KNOWN HARD DEBTS — attempt briefly, then LIFT as a documented `private` sorry (do NOT burn the session)
These three were designed as deferrable. Time-box each; if not closed in a couple of build cycles, leave a precise `-- TODO:` and report it as a retained named debt:
- **`ScaleMRE.lean` `integral_orbitCondKernel_eq_self_of_invariant`** (~line 121) — the analytic core, already marked the file's one lifted `private` sorry. Likely stays a debt.
- **`ScaleStructure.lean`** (~line 240) — the reverse measurability direction: `Measurable u` is not derivable from an off-null-set factorization. **PRE-AUTHORIZED**: if adding a hypothesis `(hu : Measurable u)` (or a `SecondCountable`/standard-Borel side condition) closes it cleanly, add it and report — every consumer supplies measurability. Otherwise lift as documented debt.
- **`LocationMRE.lean` `exists_measurable_condMinimizer_convex`** (~line 174) — measurable selection of the conditional-risk minimizer. **PRE-AUTHORIZED**: if a finiteness/continuity hypothesis (e.g. `[Finite]` on the index, or lower-semicontinuity of the loss) is what unblocks the measurable-argmin, add it and report — it does not change the theorem's mathematical content. Otherwise lift as documented debt.

## Discipline
- Commit after each lemma compiles.
- **If any public statement is actually false, STOP, leave it sorried, report the counterexample.** Honest refusal is the desired outcome; never weaken a statement to pass.
- For every headline you close, `#print axioms <name>` — expect only `propext, Classical.choice, Quot.sound`. Report any `sorryAx` (means it still reaches a sorried lemma).

## Report at the end
Per-file `lake build` status, remaining sorry count per file, which sorries you CLOSED vs LIFTED-as-debt (with the reason + any hypothesis you added), the `#print axioms` outputs for closed headlines, and any statement you believe false (with counterexample).