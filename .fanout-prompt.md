# Close the 2 sorries in PointEstimation/ExponentialFamily/MinimalSufficient.lean (ONE file)

**RUN EVERY BUILD IN THE FOREGROUND. Never background a build, never `srun`/`sbatch` from inside, never `sleep`/poll, never schedule a wakeup. Plain `lake build StatLean.PointEstimation.ExponentialFamily.MinimalSufficient` and WAIT. Backgrounding a build wastes the whole session — do not.**

Lean 4 / Mathlib proof engineer on `StatLean`. Pin `v4.29.1`. (The repo `CLAUDE.md` is gitignored and NOT here — rules below. Project rules: never `lake update`; `sorry` is planned debt; never launder unproven content into hypotheses; lines ≤ 100 chars.)

You are ON the cluster on branch `pe/minsuff`. Closing these completes the last closeable cluster in the PointEstimation area.

## Scope — edit ONLY this one file
`StatLean/PointEstimation/ExponentialFamily/MinimalSufficient.lean`. Touch nothing else — NOT any `Defs.lean` or `Sufficiency/*` (all frozen/closed). You MAY add `import Mathlib.*`, import closed project modules, and add `private` helpers. Public signatures/docstrings FROZEN.

## ⚠️ The in-file TODO comment is STALE — read this first
`isMinimalSufficient_stat`'s TODO says the bridging lemma `Sufficiency.Factorization.isSufficient_of_isFactorizedDensity` "is itself an open `sorry` … so this half cannot currently be discharged." **That is no longer true.** I verified on this branch: `StatLean/PointEstimation/Sufficiency/Factorization.lean` has **0 sorries** and `isSufficient_of_isFactorizedDensity` (~line 65) is **fully proven**. Use it freely — there is no debt-laundering concern. Please also delete/update that stale parenthetical in the comment as part of your edit.

## The 2 sorries
1. **`fullRank_exists_affineSpan`** (~83) — from the classical full-rank condition on the parameter set, produce `s + 1` points lying in no proper affine subspace. Pure finite-dimensional affine geometry: the file already imports `Mathlib.LinearAlgebra.AffineSpace.{Independent,FiniteDimensional}` and `Convex.Intrinsic`. Route: a set spanning the whole space affinely contains an affinely independent spanning family of the right cardinality (`AffineIndependent`, `affineSpan_eq_top`, `finrank`-counting).
2. **`isMinimalSufficient_stat`** (~120) — the natural statistic is minimal sufficient. Two halves, both now available:
   - **Sufficiency.** The density of `E.P η` w.r.t. `E.base` is `exp(⟨η, T x⟩ − A η)` — a function of `T` alone — so build `IsFactorizedDensity` and apply the (proven) `Sufficiency.Factorization.isSufficient_of_isFactorizedDensity`. `ExponentialFamily.{Basic,MGF,NaturalStatistic}` (incl. `P_eq_withDensity`) are closed — reuse them for the density form.
   - **Minimality.** For a competing sufficient `U`, the likelihood ratios `p_{η⁽ʲ⁾}/p_{η⁽⁰⁾} = exp(⟨η⁽ʲ⁾ − η⁽⁰⁾, T⟩ − ΔA)` are `U`-measurable; the affine-span hypothesis (from sorry 1) makes the vector `(⟨η⁽ʲ⁾ − η⁽⁰⁾, T x⟩)ⱼ` determine `T x`, so `T` factors through `U`. Take `Real.log` of the ratios to get the linear functionals, then invert via affine independence.

If a genuine gap remains, lift it to ONE named `private` lemma with a precise `-- TODO:` and report it — never fake a proof or weaken a public statement.

## Discipline
- Commit after each lemma compiles.
- **If a statement is actually false, STOP and report the counterexample.** Honest refusal is desired.
- `#print axioms isMinimalSufficient_stat` and `#print axioms fullRank_exists_affineSpan` — expect only `propext, Classical.choice, Quot.sound`. Report any `sorryAx`.

## Report at the end
Final `lake build` status, remaining sorry count, `#print axioms` outputs, any statement you believe false (with counterexample).