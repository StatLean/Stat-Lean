# Close the LAST 2 sorries in PointEstimation/LinearModel/ (2 files)

**RUN EVERY BUILD IN THE FOREGROUND. Never background a build, never `srun`/`sbatch` from inside, never `sleep`/poll, never schedule a wakeup, never say "the build is running in the background". Plain `lake build <module>` and WAIT for it to print. A previous session lost its whole run by backgrounding — do not repeat that.**

Lean 4 / Mathlib proof engineer on `StatLean`. Pin `v4.29.1`. (Repo `CLAUDE.md` is gitignored and NOT here. Project rules: never `lake update`; `sorry` is planned debt; never launder unproven content into hypotheses; lines ≤ 100 chars.)

You are ON the cluster on branch `pe/lm-final`. **These are the last 2 open sorries in the whole PointEstimation area's closeable work** — closing them completes the area.

## Scope — edit ONLY these 2 files
`StatLean/PointEstimation/LinearModel/Equivariant.lean` and `StatLean/PointEstimation/LinearModel/RandomDesign.lean`. Nothing else — NOT `Canonical.lean` or any `Defs.lean` (frozen/closed). You MAY add `import Mathlib.*`, import closed project modules, add `private` helpers. Public signatures/docstrings FROZEN.

## Sorry 1 — `Equivariant.isCanonicalScaleMRE_residual_pow` (~line 407, sorry ~421)
**Strong hint: its sibling `isCanonicalScaleMRE_residual_variance` (~425) is ALREADY CLOSED in this file.** Read that closed proof first and mirror it — the `_pow` statement is the same argument with a general exponent `k` in place of the variance's fixed power, with the normalising constant supplied by `residualScaleConst k r` (already defined, and `residualScaleConst_one` is proven). The χ² moment machinery you need is in-file and closed: `integrable_pow_chiSquared` (~326), plus `MultipleTesting.ForMathlib.ChiSquared` (imported, 0-sorry). Route: scale-equivariance reduces the risk to a one-dimensional minimisation over the scale factor; the minimiser is the ratio of consecutive χ² moments, which is exactly `residualScaleConst`.

## Sorry 2 — `RandomDesign.not_exists_blue_of_known_design_moment` (~447, sorry ~479)
A **negative** result: no best-linear-unbiased estimator exists in the random-design model when the design moment is known. Standard route: assume a BLUE `δ` exists, then exhibit a competitor that beats it at some parameter point — typically compare two estimators (e.g. the fixed-design LSE at two different design realisations) whose risks cross, so neither uniformly dominates; conclude the "uniformly minimum variance in the linear class" requirement is contradictory. The file's proven `isUMVU_regression_coeff` and `covariance_regression_coeff` (~245/~350) give you the exact variance expressions to make the crossing explicit. `IsBLUERandomDesign` / `IsRandomDesignLinear` (~418/~431) are the definitions to unfold.

If either genuinely cannot be closed, lift it to ONE named `private` lemma with a precise `-- TODO:` explaining the exact obstruction and report it — never fake a proof or weaken a public statement. **If a statement is actually false, STOP and report the counterexample** — honest refusal is the desired outcome.

## Discipline
- Commit after each lemma compiles.
- `#print axioms isCanonicalScaleMRE_residual_pow` and `#print axioms not_exists_blue_of_known_design_moment` — expect only `propext, Classical.choice, Quot.sound`. Report any `sorryAx`.

## Report at the end
Per-file `lake build` status, remaining sorry count per file, which CLOSED vs LIFTED (with the exact obstruction), both `#print axioms` outputs, and any statement you believe false (with counterexample).