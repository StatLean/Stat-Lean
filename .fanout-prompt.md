# Close the 3 sorries in HypothesisTesting/GoodnessOfFit/SmoothTest.lean (ONE file)

**RUN EVERY BUILD IN THE FOREGROUND. Never background a build, never `srun`/`sbatch` from inside, never `sleep`/poll, never schedule a wakeup, never say "the build is running in the background". Plain `lake build StatLean.HypothesisTesting.GoodnessOfFit.SmoothTest` and WAIT for it to print. A previous session lost its whole run by backgrounding — do not repeat that.**

Lean 4 / Mathlib proof engineer on `StatLean`. Pin `v4.29.1`. (Repo `CLAUDE.md` is gitignored and NOT here. Project rules: never `lake update`; `sorry` is planned debt; never launder unproven content into hypotheses; lines ≤ 100 chars.)

You are ON the cluster on branch `ht/smooth`. This is **Neyman's smooth test** of goodness of fit.

## Scope — edit ONLY this one file
`StatLean/HypothesisTesting/GoodnessOfFit/SmoothTest.lean`. Nothing else — in particular **do NOT touch `SmoothTestLargeK.lean`**: its `bentkus_berry_esseen_convex` / `_ball` are **sanctioned planned deferrals** (that Berry–Esseen lemma is stated without proof in the source text), so leave them alone. You MAY add `import Mathlib.*`, import closed project modules, add `private` helpers. Public signatures/docstrings FROZEN.

## The 3 sorries
1. **`smoothStat_weakConverges_chiSquared`** (~128, sorry ~146) — the smooth statistic `‖n^{-1/2} ∑ᵢ ψ(Xᵢ)‖²`-type quadratic form converges weakly to `χ²_k` under `P₀`.
   ⭐ **This is the same pattern that just closed Pearson's χ² in `GoodnessOfFit/ChiSquaredMultinomial.lean` — read that file's proof and mirror it.** Two proven, axiom-clean bricks do the work:
   - `StatLean.AsymptoticStatistics.ForMathlib.MultivariateCLT.tendstoInDistribution_multivariate_clt` (0-sorry) — the standardised sum of the i.i.d. score vectors `ψ(Xᵢ)` converges to a multivariate Gaussian. (`ChiSquaredMultinomial` uses a `clt_finDim`-style canonical i.i.d. transfer; reuse that shape.)
   - `StatLean.HypothesisTesting.ForMathlib.NoncentralChiSquared.multivariateGaussian_map_inner_inv_eq_chiSquared` (0-sorry, axiom-clean) — `N(0,S)` pushed through `z ↦ ⟪z, S⁻¹ z⟫` is `chiSquared k`, for `S.PosDef`.
   Add the imports you need. If `smoothModel`'s `ψ` are orthonormal in `L²(P₀)` the covariance is `I`, so `S = 1` and the quadratic form is just `‖·‖²` — check the actual hypotheses in the signature and instantiate accordingly.
2. **`smoothTest_maximin_upper_bound`** (~158, sorry ~194) — an upper bound on the maximin power over the smooth alternative class.
3. **`smoothTest_asymptotically_maximin`** (~204, sorry ~243) — the smooth test is asymptotically maximin (TSH 16.4.1); combine 1 and 2.

**Taint note:** this file imports `GoodnessOfFit/AsymptoticMaximin.lean`, which carries 2 open sorries. If 2 or 3 route through them, their `#print axioms` will show `sorryAx` — that is acceptable, but **report exactly which theorems are clean and which inherit taint, and from where**. Do not weaken a statement to dodge it.

If a genuine gap remains, lift it to ONE named `private` lemma with a precise `-- TODO:` and report it — never fake a proof. **If a statement is actually false, STOP and report the counterexample** — honest refusal is desired (a sibling session just correctly caught a false statement this way).

## Discipline
- Commit after each lemma compiles.
- `#print axioms` on each theorem you close (`smoothStat_weakConverges_chiSquared` should come out fully clean `[propext, Classical.choice, Quot.sound]` if routed through the two bricks above).

## Report at the end
Final `lake build` status, remaining sorry count, which CLOSED vs LIFTED, per-theorem `#print axioms` (clean vs sorryAx + source), and any statement you believe false (with counterexample).