# dinf-discrete: faithful general-T discrete Dudley (Thm 8.1.4)

TOUCH-SET (only this file):
- StatLean/ConcentrationInequalities/Chaining/DiscreteDudley.lean (2 sorries:
  the general `discrete_dudley` and `discrete_dudley_abs` at the end of the
  file, section "Faithful general forms")

The finite-T proofs `discrete_dudley_of_finite` / `discrete_dudley_abs_of_finite`
in the SAME file are your templates — the general proofs reuse their entire
per-level machinery, with two structural changes: (a) the maximum runs over
the given finite subset `F ⊆ T` while nets/entropy stay on T (nets from
`exists_finset_net_of_cov_ne_top` + `hcov`, DyadicNets.lean — NOT the
hfin-version); (b) the finest-scale a.e. stop (`exists_fine_scale` +
`hclean`) is REPLACED by truncation at an arbitrary level n plus the
residual bound `residual_expectation_le` / `residual_abs_expectation_le`
(Chaining/SubsetChaining.lean — usable even while that session is running:
sorried lemmas typecheck), followed by n → ∞.

Other statements you rely on (frozen; possibly still sorried — fine):
`dudleyLSum`, `sum_window_le_dudleyLSum`, `dudleyLSum_eq_ofReal_of_summable`,
`ofReal_diam_mul_sqrt_log_two_le_four_mul_dudleyLSum` (EntropySum.lean);
`isBounded_of_coveringNumber_ne_top` (DyadicNets.lean).

## Roadmap for `discrete_dudley` (mean-zero)

1. `by_cases hLS : dudleyLSum T = ⊤` — trivial branch (`le_top` after
   rewriting RHS ⊤: `ENNReal.mul_top`, the ofReal(6√3) and K factors are
   nonzero… simpler: `hLS ▸ …`; note `ofReal(6√3) ≠ 0` and if `K = 0`
   handle separately: with K = 0 all increments have ψ₂-norm 0, so all
   coordinates are a.e. equal to X_c and the LHS integral is 0 ≤ RHS —
   mirror the K = 0 / diam-degenerate branches of the finite abs proof; or
   avoid the case split by proving the real core below for every finite
   WINDOW and only then splitting).
   RECOMMENDED clean structure: prove a real-valued core first:
   `hcore : ∀ n : ℕ, ∫ ω, F.sup' hFne (fun t => X t ω) ∂μ ≤
      6*√3*K * (∑ j ∈ Finset.range n, dudleySummand T (κ+1+j)) + √3*(K*ε n)*√(2*log F.card)`
   by mirroring `discrete_dudley_of_finite` (anchor c from the coarse
   singleton net; StepA mean-zero anchoring at c; telescope over range n;
   per-level `expectation_max_finset_le` over close pairs of T-nets exactly
   as in the finite proof — the level machinery does not mention T's
   cardinality once nets exist) + `residual_expectation_le` for the tail
   term at level n (net `N n` has radius `ε n`, prox from the net property).
   Degenerate branches (diam = 0) as in the template.
2. From `hcore`: the window sum is `≤ (dudleyLSum T).toReal` when
   `dudleyLSum T ≠ ⊤` (window ofReal-sum ≤ dudleyLSum via
   `sum_window_le_dudleyLSum`, then `ENNReal.toReal_mono` /
   `ENNReal.le_toReal_iff`-style bridging with `ENNReal.ofReal_sum_of_nonneg`).
   Let n → ∞: the residual `√3*(K*ε n)*√(2 log |F|) → 0`
   (`ε n = 2^{-(κ+n)} → 0`: `tendsto_zpow_atTop_zero`-family or explicit
   geometric; use `ge_of_tendsto'` / `le_of_tendsto` on the constant LHS).
   Conclude `∫ … ≤ 6√3*K*(dudleyLSum T).toReal`, then
   `ENNReal.ofReal_le_ofReal` + `ENNReal.ofReal_toReal hLS` +
   `ENNReal.ofReal_mul` plumbing to land the frozen ℝ≥0∞ statement.
3. The ⊤ branch: `le_top` composed with rewriting `x * ⊤`: since
   `ENNReal.ofReal (6*√3) * K * ⊤` is `⊤` unless `K = 0`; treat `K = 0`
   inside the ⊤ branch via the degenerate argument (all X_t a.e. equal X_c,
   LHS = ofReal 0 = 0 ≤ anything) — the finite template's diam-0 branch has
   the a.e. argument shape (`SubGaussianIncrements.sub_ae_eq_zero`).

## Roadmap for `discrete_dudley_abs`

Mirror `discrete_dudley_abs_of_finite` with the same two changes. The
re-anchoring scale m (finest 𝒩 = 1 scale) is bounded above by any j with
`2·2^{-(κ+j)} < diam T` (`one_lt_coveringNumber_of_two_mul_lt_dist`
forces 𝒩 ≥ 2 there), so `m := (Finset.range (J+1)).filter (𝒩 = 1) |>.max'`
for an explicit diam-based J replaces the finite template's nf-based cap;
everything else (window levels with 𝒩 ≥ 2, the √π·K·diam anchor
reconnection via `integral_abs_le_of_subGaussianNorm_le`, the
20 = 6√3 + 4√π/√log2 arithmetic) transfers verbatim with
`ofReal_diam_mul_sqrt_log_two_le_four_mul_dudleyLSum` in place of the real
absorber, plus `residual_abs_expectation_le` at the truncation level.
K = 0 branch exactly as in the template.

FALLBACK (single named sorry): `discrete_dudley_abs`. The mean-zero
`discrete_dudley` MUST close.

DONE: at most the fallback sorry remains in DiscreteDudley.lean;
`lake build StatLean.ConcentrationInequalities.Chaining.DiscreteDudley` and
the area build error-free.
## Common rules (all dinf sessions)

You are a Lean 4 (v4.29.1, Mathlib pinned) proof-closure session for the StatLean
library, working in a git worktree of the Stat-Lean repo on your own branch. The
statements you must prove are ALREADY STUBBED with `sorry` — they are FROZEN
CONTRACTS. You may NOT change any statement, hypothesis, constant, definition, or
docstring of a stubbed declaration; you may NOT touch files outside your touch-set;
you may NOT use `axiom`, `admit`, or add new hypotheses. If you become convinced a
frozen statement is FALSE or unprovable as stated, leave its `sorry` in place and
write a precise blocker report (what corner breaks, with a counterexample sketch) in
your final commit message — do NOT weaken or alter the statement.

Build discipline: iterate with `lake build StatLean.ConcentrationInequalities.<YourModule>`
(fast); before finishing run `lake build StatLean.ConcentrationInequalities` — it must
compile with NO errors (sorry warnings from files outside your touch-set are expected;
your own files must have 0 sorries except, if truly stuck, your single designated
fallback below). Commit early and often with clear messages; your session auto-commits
and pushes at close.

Mathlib search: you have no network tools. Use `exact?`/`apply?`/`rw?` in-proof, and
`grep -rn "<pattern>" .lake/packages/mathlib/Mathlib/` for names. The repo's own
lemmas: grep under StatLean/.

House gotchas: real `⨆ i ∈ s, f i` over a Finset carries junk `sSup ∅ = 0` from
out-of-set indices — the junk-free finite max is `Finset.sup'`; convert with
`biSup_finset_eq_sup'` (needs the family nonneg, e.g. `fun _ _ => abs_nonneg _`) or
`biSup_finset_eq_sup'_of_sup'_nonneg` (needs `0 ≤ sup'`), and `le_biSup_finset` for
the unconditional member bound (all in Chaining/FinsetMaximal.lean). `simp only` does
not β-reduce after `rw [hSdef]`-style set-unfolds reliably — `dsimp only` first when
rewriting under `(fun x => ...) y` redexes. `Finset.sup'_cons` carries the tail's
Nonempty only on its RHS — supply it explicitly. Never write the two-character
sequence `-` `/` inside a docstring. Keep lines ≤ 100 chars.