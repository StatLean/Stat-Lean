# Close the sorries in HypothesisTesting/Invariance/{Admissibility,BayesAdmissible}.lean (2 files)

**RUN EVERY BUILD IN THE FOREGROUND. Never background a build, never `srun`/`sbatch` from inside, never `sleep`/poll, never schedule a wakeup, never say "the build is running in the background". Plain `lake build <module>` and WAIT for it to print. A previous session lost its whole run by backgrounding — do not repeat that.**

Lean 4 / Mathlib proof engineer on `StatLean`. Pin `v4.29.1`. (Repo `CLAUDE.md` is gitignored and NOT here. Project rules: never `lake update`; `sorry` is planned debt; never launder unproven content into hypotheses; lines ≤ 100 chars.)

You are ON the cluster on branch `ht/admiss-2`. This is **admissibility of tests** (convex acceptance regions in exponential families, and admissibility of Bayes tests).

## Scope — edit ONLY these 2 files, in this order
1. `StatLean/HypothesisTesting/Invariance/Admissibility.lean` (3 sorries) — build `…Invariance.Admissibility` first.
2. `StatLean/HypothesisTesting/Invariance/BayesAdmissible.lean` (4 sorries) — it **imports** Admissibility, so do it second and reuse what you just proved.

Touch nothing else — NOT `Tests/Defs.lean`, `Invariance/Defs.lean`, or the `PointEstimation` files (all frozen/closed). You MAY add `import Mathlib.*`, import closed project modules, add `private` helpers. Public signatures/docstrings FROZEN.

## Part 1 — `Admissibility.lean`
The definitions `IsDAdmissible` (~99) and `IsAlphaAdmissible` (~108) are in-file — unfold them.
1. **`acceptance_subset_of_power_le`** (~148, sorry ~168) — if one test's power is everywhere ≤ another's on the alternative, the acceptance regions are nested (up to null sets). Exponential-family densities are strictly positive, so a.e. domination of the integrals against every `P θ` forces the pointwise a.e. comparison of the critical functions.
2. **`isDAdmissible_of_convex_acceptance`** (~171, sorry ~188) — a test whose acceptance region is **convex** is d-admissible in a (full-rank) exponential family. This is the Birnbaum–Stein convexity criterion: if a competitor dominated it, part 1 nests the acceptance regions; a separating hyperplane for the convex acceptance set gives a natural-parameter direction along which the power comparison is strict, contradicting domination. Mathlib: `geometric_hahn_banach_open`/`_closed_point` for the separation.
3. **`isAlphaAdmissible_of_size_attained`** (~193, sorry ~217) — the α-level version, given the size is attained; reuse 2.

## Part 2 — `BayesAdmissible.lean`
4. **`bayesTest_isDAdmissible`** (~93, sorry ~111) — a Bayes test (likelihood-ratio against a prior mixture) is d-admissible. Standard: a strictly-dominating competitor would have strictly smaller Bayes risk, contradicting the Bayes property. This is a clean risk-comparison argument — no separation needed.
5. **`bayesTest_isAlphaAdmissible`** (~115, sorry ~137) — the α-level analogue; combine 4 with 3.
6. **`bayesTest_admissible_of_subset`** (~142, sorry ~166) — admissibility transfers to a subset of the parameter space (restricting `Θ'` only weakens the "no dominator" requirement).
7. **`exists_gaussian_scale_mixture`** (~182, sorry ~191) — existence of a Gaussian scale mixture with the prescribed second moments; a construction/measure-theoretic existence statement (build the mixing measure explicitly, e.g. a two-point mixture matching `σ2`/`M2`).

If a genuine gap remains, lift it to ONE named `private` lemma per file with a precise `-- TODO:` and report it — never fake a proof. **If a statement is actually false, STOP and report the counterexample** — honest refusal is desired (sibling sessions have correctly caught two false statements this way).

## Discipline
- Commit after each lemma compiles — bank incrementally.
- `#print axioms` on each theorem you close; report any `sorryAx` and its source.

## Report at the end
Per-file `lake build` status, remaining sorry count per file, which CLOSED vs LIFTED (with the exact obstruction), `#print axioms` outputs, and any statement you believe false (with counterexample).