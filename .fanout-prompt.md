# hdp-bundle-chain-support — DyadicNets + PsiTwoMaximal + TailToExpectation [HDP §8.1 support]
You are a Lean 4 proof-closure session on branch conc/hdp-bundle-chain-support of the StatLean project (Lean v4.29.1, pinned Mathlib 5e932f97 pre-built under .lake/packages/mathlib). Inside an srun allocation: plain `lake build <Target>`; NEVER `lake update`; never touch lakefile/manifest/toolchain. Iterate until GREEN and COMMIT BEFORE ENDING (wait for the final build, then commit).
CONTEXT: no CLAUDE.md/notes/tools here. Mathlib search: `grep -rn "<name>" .lake/packages/mathlib/Mathlib/` + exact?/apply?. REAL on this branch: DiscreteDudley/Dudley/DudleyTail/GenericChaining/EntropySum/EntropyIntegrand + all Orlicz bridges (they were PROVEN AGAINST the statements you are closing — do NOT change any statement).
## Touch-set (edit ONLY these three files; close their sorries one file at a time, building each)
1. StatLean/ConcentrationInequalities/Chaining/DyadicNets.lean — build gate `lake build StatLean.ConcentrationInequalities.Chaining.DyadicNets`. Nets from Metric.exists_set_encard_eq_coveringNumber (pin-verified), encard→Finset plumbing (Set.Finite.toFinset, Set.encard_eq_coe_toFinset_card), Int.clog coarse-scale selection (Int.self_le_zpow_clog/Int.zpow_pred_clog_lt_self — grep exact pin names), closest-point projection on finite nonempty nets (Finset.exists_min_image).
2. StatLean/ConcentrationInequalities/Chaining/PsiTwoMaximal.lean — gate `...Chaining.PsiTwoMaximal`. E max of N non-centered ψ₂ vars ≤ CL√log N: B1 tails (REAL) + union bound + TailToExpectation statements (file 3 — its statements are citable even before you close it, but close file 3 FIRST if convenient).
3. StatLean/ConcentrationInequalities/Chaining/TailToExpectation.lean — gate `...Chaining.TailToExpectation`. Gaussian-type tail ⇒ expectation: layer-cake (MeasureTheory.lintegral_eq_lintegral_meas_lt, pin) + gaussian integral evaluation (integral_Ioi_of_hasDerivAt_of_tendsto pattern — mirror Orlicz/TailToNorm.lean which is REAL and solved the same improper integral).
## Fallbacks (graded, at most ONE per file, only the docstring-designated): exists_finset_net / expectation_abs_max_le / integral_le_of_tail_le.
## Constraints
No axiom/admit; no new hypotheses; keep tags; private helpers OK. Statements are FROZEN (downstream real proofs depend on them verbatim).
Commit: git add StatLean/ConcentrationInequalities/Chaining/ && git commit -m "conc/hdp-bundle-chain-support: close nets+maximal+tail-to-expectation"
## DONE means
All three targets build exit 0 (WAIT); ≤ 3 designated fallback sorries total; report per-file closures.