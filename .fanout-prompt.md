# hdp-orlicz-attainment — Luxemburg-inf attainment + norm definiteness [HDP Ex 2.42–2.43]

You are a Lean 4 proof-closure session on branch `conc/hdp-orlicz-attainment` of the StatLean project (Lean v4.29.1, pinned Mathlib `5e932f97` pre-built under `.lake/packages/mathlib` via the shared cache). You are inside an srun allocation on FAS-RC: run plain `lake build <Target>`; NEVER `lake update`; never touch `lakefile.lean` / `lake-manifest.json` / `lean-toolchain`. Iterate until GREEN.

CONTEXT: this worktree has no CLAUDE.md/notes/tools (gitignored). Everything you need is in this prompt + the stub files. Mathlib search: `grep -rn "<name>" .lake/packages/mathlib/Mathlib/` and in-proof `exact?` / `apply?` / `rw?`. Upstream StatLean lemmas in OTHER files may still be `sorry`-backed — cite them as black boxes; only YOUR file must become sorry-free.

## Touch-set (edit ONLY this file)
`StatLean/ConcentrationInequalities/Orlicz/Attainment.lean`
Do NOT edit any other file. Do NOT change any statement, hypothesis, constant, or tag (statements are frozen at the stub gate). If a statement looks unprovable AS STATED, stop and report precisely why — do not weaken it.

## Build gate
`lake build StatLean.ConcentrationInequalities.Orlicz.Attainment` — exit 0, and your file contains at most the designated fallback sorry below.

## The goal — close these sorries
1. `lintegral_le_one_of_orliczNorm_le` — THE attainment lemma (the book's silent WLOG "‖X‖_{ψ₂} ≤ K ⇒ E ψ(|X|/K) ≤ 1", used throughout HDP §2.6–2.9): if `orliczNorm ψ X μ ≤ K` (`0 < K`), then `K ∈`-style gauge condition holds at `K` itself.
2. `lintegral_exp_sq_le_two_of_subGaussianNorm_le` / `subGaussianNorm_le_of_lintegral_exp_sq_le_two` — the ψ₂ instance pair (thresholds: `≤ 1` for `exp(x²)−1` ⟺ `≤ 2` for `exp(x²)`; mind `ENNReal.ofReal_sub`/`one_add` bookkeeping).
3. `lintegral_exp_abs_le_two_of_subExponentialNorm_le` / `subExponentialNorm_le_of_lintegral_exp_abs_le_two` — the ψ₁ instance pair.
4. `orliczNorm_eq_zero_iff` — norm definiteness (fallback; see below).

Definitions live in `Orlicz/Defs.lean` (READ IT FIRST): `orliczSet ψ X μ = {K : ℝ≥0 | 0 < K ∧ ∫⁻ ω, ENNReal.ofReal (ψ (|X ω| / K)) ∂μ ≤ 1}`, `orliczNorm = ⨅ K ∈ orliczSet ψ X μ, (K : ℝ≥0∞)`, `psiTwo x = exp (x^2) − 1`, `psiOne x = exp x − 1`; helper `orliczNorm_le_of_mem` is already proven. Generator facts (monotone/convex/continuous/measurable, `psiTwo_zero` …) are stated in `Orlicz/Generators.lean` — cite them (sorry-backed is fine).

## Proof roadmap (from the batch design)
- Attainment: extract a strictly-decreasing gauge sequence `Kₙ ↓ K` with `Kₙ ∈ orliczSet` from `orliczNorm ≤ K` — ℝ≥0∞ `iInf` extraction: for each `n`, `orliczNorm < K·(1+1/(n+1))` gives a member below it (`iInf₂_lt_iff` / `exists_lt_of_ciInf_lt`-style; in ℝ≥0∞ use `lt_iInf_iff`-negation or `ENNReal.le_of_forall_pos_le_add` patterns; a clean route: prove first `exists_mem_orliczSet_lt_of_orliczNorm_lt : orliczNorm ψ X μ < c → ∃ K ∈ orliczSet ψ X μ, (K:ℝ≥0∞) < c` as a private named helper).
- Then monotone convergence: `ψ(|X ω|/Kₙ) ↑ ψ(|X ω|/K)` pointwise (needs `MonotoneOn ψ (Ici 0)` since `|X|/Kₙ ≤ |X|/Kₙ₊₁`, plus `Continuous ψ` for the limit along `Kₙ → K`); use `MeasureTheory.lintegral_iSup_directed` or `lintegral_tendsto_of_tendsto_of_monotone`; conclude `∫⁻ ψ(|X|/K) ≤ 1` from `∀ n, ≤ 1`.
- Careful: division by `(K : ℝ)` with `K : ℝ≥0`, coercion positivity `(0 < K) → (0:ℝ) < K` is `K.coe_pos.mpr` / `NNReal.coe_pos`.
- ψ₂/ψ₁ instances: rewrite `ENNReal.ofReal (exp u − 1)` vs `ENNReal.ofReal (exp u) − 1` — since `exp u ≥ 1` when `u ≥ 0`, use `ENNReal.ofReal_sub _ zero_le_one` and `∫⁻ (f − 1) ≤ 1 ↔ ∫⁻ f ≤ 2` via `lintegral_sub` (needs finiteness of `∫⁻ 1 = μ univ` — the statements carry `[IsProbabilityMeasure μ]` where needed; check the stub hypotheses and use exactly them).
- Definiteness (⇒): if `orliczNorm = 0`, for every `ε > 0` and every threshold `t > 0`, pick gauge `K < min ε (t/ψ⁻¹-ish)`; Markov (`mul_meas_ge_le_lintegral₀`) on `ψ(|X|/K)` at level `ψ(t/K)` with `ψ(t/K) → ∞` (hypothesis `Tendsto ψ atTop atTop`) forces `μ{t ≤ |X|} = 0`; union over `t = 1/(m+1)` gives `X =ᵐ 0`. (⇐): `X =ᵐ 0` + `ψ 0 ≤ 0` puts every `K > 0` in the gauge set (`lintegral_congr_ae`, `ENNReal.ofReal_le_zero`-style), so the `⨅` is `≤ K` for all `K > 0`, i.e. `= 0` (`le_of_forall_pos_le_add` or `ENNReal.eq_zero_of_forall_le_coe`-style: `iInf` over a set containing arbitrarily small elements).

## Fallback (graded)
If after honest effort exactly one lemma resists, it must be `orliczNorm_eq_zero_iff` (leave its single `sorry`, everything else proven). No other sorry may remain.

## Constraints
- New Mathlib imports are fine; StatLean imports only from `StatLean.ConcentrationInequalities.*`.
- `private` named helpers allowed; NO sorried `have`s, no `axiom`, no `admit`, no new hypotheses on existing statements.
- Keep all `-- USER-INPUT:` / `-- LEAN-ONLY:` tags and docstrings intact.
- Commit: `git add StatLean/ConcentrationInequalities/Orlicz/Attainment.lean && git commit -m "conc/hdp-orlicz-attainment: close attainment + definiteness"`.

## DONE means
`lake build StatLean.ConcentrationInequalities.Orlicz.Attainment` exits 0; at most the designated fallback sorry remains in the file; final report lists closed sorries, any remaining fallback, and key Mathlib lemma names used.