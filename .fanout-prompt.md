# Close the 2 sorries in HypothesisTesting/ForMathlib/LindebergCLT.lean (ONE file)

**RUN EVERY BUILD IN THE FOREGROUND. Never background a build, never `srun`/`sbatch` from inside, never `sleep`/poll, never schedule a wakeup. Plain `lake build StatLean.HypothesisTesting.ForMathlib.LindebergCLT` and WAIT. Backgrounding a build wastes the whole session — do not.**

Lean 4 / Mathlib proof engineer on `StatLean`. Pin `v4.29.1`. (The repo `CLAUDE.md` is gitignored and NOT here — rules below. Project rules: never `lake update`; `sorry` is planned debt; never launder unproven content into hypotheses; lines ≤ 100 chars.)

You are ON the cluster on branch `ht/lindeberg-2`. High value: these 2 sorries taint the bootstrap CLT cores AND the likelihood-trinity χ² headline downstream — closing them axiom-cleans those.

## Scope — edit ONLY this one file
`StatLean/HypothesisTesting/ForMathlib/LindebergCLT.lean`. Keep imports as-is (add `import Mathlib.*` only if needed). Public signatures/docstrings FROZEN. You MAY add `private` helpers.

## The 2 sorries — BOTH are corollaries of the file's already-proven core `lindeberg_clt`
The main Lindeberg–Feller triangular-array CLT (`lindeberg_clt`, with its swapping estimate + 3rd-order remainder bound) is **already proven, 0-sorry, in this file** — treat it and every lemma above these two as a black box.

1. **`weighted_iid_clt`** (~690) — CLT for `∑ᵢ wₙ,ᵢ Yᵢ` normalized. As the docstring says, it is **`lindeberg_clt` applied to the row** `Xₙ,ᵢ := wₙ,ᵢ · Yᵢ / (σ · (∑ⱼ wₙ,ⱼ²)^{1/2})`. The work is discharging `lindeberg_clt`'s hypotheses for this row:
   - row entries independent (from `Y` independent + scaling), centered (from `E Yᵢ = 0`), row variance sums to 1 (by the `σ`-normalization and `hσ2` variance identity),
   - **Lindeberg condition** from **asymptotic negligibility of the weights** (`hneg`): `max_i wₙ,ᵢ² / ∑ⱼ wₙ,ⱼ² → 0`, so each entry's truncated 2nd moment vanishes uniformly. This is the one substantive step — build it as a `private` lemma if long.
2. **`triangular_wlln_of_L1`** (~731) — weak law for a triangular array in the first-absolute-moment form. Convergence **in measure** of the row mean to the row-mean of the limit law, from the L1 + negligibility hypotheses in the signature. Standard truncation: split each entry at a threshold, control the truncated part by Chebyshev (2nd moment of the truncation) and the tail by the L1 bound; `hneg`-type negligibility kills the cross terms. Look for a Mathlib WLLN to reuse (`loogle '"weakLaw"'`, `ProbabilityTheory` L¹/L² laws); if none fits the triangular form, do the truncation directly.

If a genuine gap remains, lift it to ONE named `private` lemma with a precise `-- TODO:` and report it — never fake a proof or weaken a public statement.

## Discipline
- Commit after each lemma compiles.
- **If a statement is actually false, STOP and report the counterexample.** Honest refusal is desired.
- `#print axioms weighted_iid_clt` and `#print axioms triangular_wlln_of_L1` — expect only `propext, Classical.choice, Quot.sound`. Report any `sorryAx`.

## Report at the end
Final `lake build` status, remaining sorry count, `#print axioms` outputs, any statement you believe false (with counterexample).