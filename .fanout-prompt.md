# dinf-integral: faithful general-T Dudley integral forms (Thm 8.1.3, Eqs 8.13/8.14/8.16)

TOUCH-SET (only this file):
- StatLean/ConcentrationInequalities/Chaining/Dudley.lean (5 sorries: general
  `dudley_inequality`, `dudley_inequality_abs`, `dudley_inequality_abs_pair`,
  `dudley_inequality_Ioi` in the "Faithful general forms" section, plus the
  restated `dudley_inequality_countable` at constant 40)

Frozen upstream statements you consume (possibly still sorried in concurrent
sessions — they typecheck): general `discrete_dudley` / `discrete_dudley_abs`
(DiscreteDudley.lean, per-finite-subset, ℝ≥0∞ RHS `dudleyLSum`);
`dudleyLSum_le_two_mul_dudleyLIntegral`, `dudleyLIntegral_Ioi_eq`
(EntropySum.lean). The finite templates `*_of_finite` sit above in this file.

## Roadmaps

1. `dudley_inequality`: pure ℝ≥0∞ calc —
   `ofReal(∫ max_F X) ≤ ofReal(6√3)*K*dudleyLSum T` (general
   `discrete_dudley`) `≤ ofReal(6√3)*K*(2*dudleyLIntegral T D)`
   (`dudleyLSum_le_two_mul_dudleyLIntegral hcov hne hD hD0`, `mul_le_mul'`)
   `= ofReal(12√3)*K*dudleyLIntegral T D` (`ENNReal.ofReal_mul`-style
   constant algebra: `12√3 = 2·(6√3)`, mind associativity/commutation in
   ℝ≥0∞ — all factors finite constants, `ring_nf`-style rearrangement via
   `mul_comm`/`mul_assoc` or `ENNReal.ofReal_mul (by positivity)` to split
   `ofReal (2 * (6√3))`).
2. `dudley_inequality_abs`: same shape from general `discrete_dudley_abs`
   with 20 → 40.
3. `dudley_inequality_abs_pair`: pointwise
   `(F ×ˢ F).sup' _ (fun p => |X p.1 ω − X p.2 ω|) ≤
    F.sup' hFne (fun t => |X t ω − X t₀ ω|) + F.sup' hFne (fun t => |X t ω − X t₀ ω|)`
   with `t₀ := hne.some` (triangle through t₀ ∈ T: `Finset.sup'_le` over
   pairs, `Finset.le_sup'` at each leg, `abs_sub` triangle — mirror the
   `hpt` step of `dudley_inequality_abs_pair_of_finite` in this file, which
   does exactly this on biSup carriers). Integrate: both legs integrable —
   per-member `Integrable (fun ω => |X t ω − X t₀ ω|)` comes from the ψ₂
   bound; mine the per-member integrability out of `integrable_biSup_abs`'s
   proof (Chaining/PsiTwoMaximal.lean) or use that lemma with a singleton
   index; then `integrable_sup'_finset` (Chaining/FinsetMaximal.lean).
   Finish: `ofReal(∫ pair) ≤ ofReal(∫ leg + ∫ leg) = ofReal(2·∫ leg)`
   (`integral_add`), then 2·(general `dudley_inequality_abs`) = 80-form
   (`ENNReal.ofReal_mul`, constant algebra).
4. `dudley_inequality_Ioi`: rewrite the RHS lintegral with
   `dudleyLIntegral_Ioi_eq hcov hne (le_refl…)` — set `D := diam T + 1`
   (`hD : diam ≤ D` by `le_of_lt (lt_add_one _)`… diam honest via
   `isBounded_of_coveringNumber_ne_top`; `hD0 : 0 < D` from
   `Metric.diam_nonneg` + 1), then apply the general `dudley_inequality`.
   Mirror `dudley_inequality_Ioi_of_finite` above.
5. `dudley_inequality_countable` (constant 40, ℝ≥0∞, honest countable sup):
   the OLD 80-proof is in git history — read it for the exhaustion/MCT
   plumbing: `git show HEAD~1:StatLean/ConcentrationInequalities/Chaining/Dudley.lean`
   (the deleted body after the same theorem header). Reuse verbatim its:
   enumeration `f`, exhaustion `S n := insert t₀ ((range n).image f)`,
   `Φ n ω := ⨆ t ∈ ↑(S n), ENNReal.ofReal |X t ω − X t₀ ω|`, monotonicity,
   `lintegral_iSup'`, and the pointwise identification of
   `⨆ t ∈ T, ofReal |…|` with `⨆ n, Φ n ω` — BUT replace the per-n bound:
   instead of applying the finite theorem to T := ↑(S n) (which cost the
   `dudleyLIntegral_subset_le_two` factor 2), apply the NEW general
   `dudley_inequality_abs` with carrier T, `F := S n`
   (`hF : ↑(S n) ⊆ T` = the old `hSsub n`; entropy of T directly), cap D
   from the old `hD`-package (`diam T ≤ D` needs deriving from the
   hypothesis `hD : ∀ s ∈ T, ∀ t ∈ T, dist s t ≤ D` via
   `Metric.diam_le_of_forall_dist_le hD0.le` — the old proof has this line).
   Per-n: `∫⁻ ω, Φ n ω = ofReal (∫ ω, (S n).sup' … )`? — the old proof
   bridged `Φ n` to the real integral with `hΦeq`/`Ssup` machinery on
   biSup; the new general theorem's LHS is `(S n).sup'`-carried: bridge
   biSup-over-coe ↔ sup' via `biSup_finset_eq_sup'` (+ `Finset.iSup_coe`,
   abs-nonneg guard) exactly as the old `Ssup` helper did — keep that
   helper (it is still in the file above the theorem) and land
   `∫⁻ ω, Φ n ω ≤ ENNReal.ofReal (40*K) * dudleyLIntegral T D` per n via
   `ofReal_integral_eq_lintegral_ofReal` (integrability of the finite max:
   as in item 3) + the general abs theorem + `ENNReal.ofReal_mul` plumbing
   (mirror the old `hbound`, dropping its `dudleyLIntegral_subset_le_two`
   step and the factor 2).

FALLBACK (single named sorry): `dudley_inequality_countable`. Items 1–4 MUST
close.

DONE: at most the fallback sorry remains in Dudley.lean; module + area
builds error-free.
## Common rules (all dinf sessions)

You are a Lean 4 (v4.29.1, Mathlib pinned) proof-closure session for the StatLean
library, working in a git worktree of the Stat-Lean repo on your own branch. The
statements you must prove are ALREADY STUBBED with `sorry` — they are FROZEN
CONTRACTS. You may NOT change any statement, hypothesis, constant, definition, or
docstring of a stubbed declaration; you may NOT touch files outside your touch-set;
you may NOT use `axiom`, `admit`, or add new hypotheses. If you become convinced a
frozen statement is FALSE or unprovable as stated, leave its `sorry` in place and
write a precise blocker report (what corner breaks, with a counterexample sketch) in
your final commit message — do NOT weaken or alter the statement.

Build discipline: iterate with `lake build StatLean.ConcentrationInequalities.<YourModule>`
(fast); before finishing run `lake build StatLean.ConcentrationInequalities` — it must
compile with NO errors (sorry warnings from files outside your touch-set are expected;
your own files must have 0 sorries except, if truly stuck, your single designated
fallback below). Commit early and often with clear messages; your session auto-commits
and pushes at close.

Mathlib search: you have no network tools. Use `exact?`/`apply?`/`rw?` in-proof, and
`grep -rn "<pattern>" .lake/packages/mathlib/Mathlib/` for names. The repo's own
lemmas: grep under StatLean/.

House gotchas: real `⨆ i ∈ s, f i` over a Finset carries junk `sSup ∅ = 0` from
out-of-set indices — the junk-free finite max is `Finset.sup'`; convert with
`biSup_finset_eq_sup'` (needs the family nonneg, e.g. `fun _ _ => abs_nonneg _`) or
`biSup_finset_eq_sup'_of_sup'_nonneg` (needs `0 ≤ sup'`), and `le_biSup_finset` for
the unconditional member bound (all in Chaining/FinsetMaximal.lean). `simp only` does
not β-reduce after `rw [hSdef]`-style set-unfolds reliably — `dsimp only` first when
rewriting under `(fun x => ...) y` redexes. `Finset.sup'_cons` carries the tail's
Nonempty only on its RHS — supply it explicitly. Never write the two-character
sequence `-` `/` inside a docstring. Keep lines ≤ 100 chars.