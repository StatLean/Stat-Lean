# hdp-finsetmax-final — prove the two renegotiated FinsetMaximal lemmas + thread the new hypothesis through all call sites [HDP, batch close]
You are a Lean 4 session on branch conc/hdp-finsetmax-final of the StatLean project (Lean v4.29.1, Mathlib 5e932f97 in .lake). srun allocation: plain lake build; NEVER lake update. COMMIT AND PUSH (git push origin conc/hdp-finsetmax-final) after each green milestone. Work directly, verify every step with a build; do not stop until the FULL gate below is green.
SITUATION: prior sessions PROVED the old statements false (junk ℝ-biSup: for i ∉ s the inner sup is sSup ∅ = 0). The laptop renegotiated (commit a9ec05f, on your branch):
- `biSup_finset_eq_sup'` (Chaining/FinsetMaximal.lean) now has hypothesis `(h0 : ∀ i ∈ s, 0 ≤ f i)` — TRUE now: (≤) every biSup branch is ≤ sup' (out-of-set branches are junk-0 ≤ sup' since sup' ≥ f i₀ ≥ 0); (≥) via the sup'-attaining witness (Finset.exists_mem_eq_sup') and le_ciSup with an explicit BddAbove (bound: max (sup') 0).
- `expectation_max_finset_le` conclusion is now `∫ ω, s.sup' hs (fun i => X i ω) ∂μ ≤ √σ2·√(2 log s.card)` — junk-free, exactly mirroring the PROVEN Maximal/FiniteMaximal.expectation_max_le (Fin-indexed): prove by reindexing via the equiv (s.equivFin or Finset.equivFin — grep the pin), transporting IsSubGaussian/centering pointwise, and converting sup' to the Fin-iSup (Finset.sup'_eq... or via the now-proven biSup lemma at the reindexed family — the Fin-indexed ⨆ j, Y j has NO junk since Fin covers the whole type; relate sup' to it by Finset.sup'_univ_eq-style or a two-inequality argument).
## Your job, in order (per-step commits+pushes)
1. Prove both lemmas in StatLean/ConcentrationInequalities/Chaining/FinsetMaximal.lean. Gate: lake build StatLean.ConcentrationInequalities.Chaining.FinsetMaximal (exit 0, 0 sorries in the file).
2. Thread the new `h0` argument through EVERY call site of `biSup_finset_eq_sup'` (grep -rn across StatLean/ConcentrationInequalities — ~30 sites in DiscreteDudley, Dudley, DudleyTail, GenericChaining, LipschitzLLN). At |·|-valued sites the uniform argument is `(fun _ _ => abs_nonneg _)`. At any NON-abs site (e.g. DiscreteDudley ~:120/:207 where the family is the mean-zero `fun t => X t ω`): the rewrite is no longer valid as-was — repair LOCALLY: either (a) the surrounding proof already effectively bounds the (·)⁺ form; insert the honest inequality `(⨆ i ∈ s, f i) ≤ max (s.sup' hs f) 0`-style private helper (prove it — junk-tolerant one-directional bound needing NO nonnegativity: every branch ≤ max(sup',0)) and adjust the chain, or (b) restructure that step through the abs-anchored form the file already has. You may add private helpers; you may NOT change any further public statement.
3. Also update the sole caller of `expectation_max_finset_le` (if any — grep; adjust its sup'-vs-biSup interface with the same tools).
4. FULL GATE: lake build StatLean.ConcentrationInequalities (the whole area umbrella) — exit 0 with ZERO sorry warnings anywhere. This is the batch's final zero; do not stop before it.
## Constraints
No axiom/admit; no new sorries; no further statement changes; keep tags.
Final commit: git add -A StatLean && git commit -m "conc/hdp-finsetmax-final: batch-10 zero" && git push origin conc/hdp-finsetmax-final
## DONE means
Step-4 gate green with 0 sorries; report per-step.