# hdp-orlicz-norm-from-moments — Prop 2.6.1 (ii)⇒(iii) [HDP §2.6]
You are a Lean 4 proof-closure session on branch conc/hdp-orlicz-norm-from-moments of the StatLean project (Lean v4.29.1, pinned Mathlib 5e932f97 pre-built under .lake/packages/mathlib). Inside an srun allocation: plain `lake build <Target>`; NEVER `lake update`; never touch lakefile/manifest/toolchain. Iterate until GREEN and COMMIT BEFORE ENDING (wait for the final build, then commit).

CONTEXT: no CLAUDE.md/notes/tools here. Mathlib search: `grep -rn "<name>" .lake/packages/mathlib/Mathlib/` + exact?/apply?. Upstream StatLean lemmas may be sorry-backed — cite as black boxes. REAL on this branch: all Orlicz bridges (Attainment/Triangle/TailToNorm/SubGaussianMGF/SubExponentialMGF/Product/Bernstein), Symmetrization 6.3.2/6.6.1/6.6.2, Chaining DiscreteDudley/Dudley/GenericChaining.

## Touch-set (edit ONLY): StatLean/ConcentrationInequalities/Orlicz/NormFromMoments.lean
No statement/hypothesis/constant/tag changes. If unprovable AS STATED, stop and report precisely why.

## Build gate
`lake build StatLean.ConcentrationInequalities.Orlicz.NormFromMoments` — exit 0.

## The goal
Close the file's sorries — READ THE FILE: every statement + its docstring Proof-formalization notes record the designed route and the single designated fallback.

## Proof roadmap (HDP Prop 2.6.1 (ii)⇒(iii), K₃ = 2√e·K)
- Expand E e^{(X/(cK))²} = ∑_n E(X/(cK))^{2n}/n! (MeasureTheory.integral_tsum / lintegral_tsum with nonneg terms — tsum swap in ℝ≥0∞ is unconditional: lintegral_tsum pin-verified); moment hypothesis gives E X^{2n} ≤ (K√(2n))^{2n} = K^(2n)·(2n)^n; with n! ≥ (n/e)^n (Stirling.le_factorial_stirling or Nat.factorial-bounds) each term ≤ ((2e)/c²)^n; at c = 2√e the ratio is 1/2 → geometric sum = 2 → gauge condition at 2√e·K (tsum_geometric_two… ENNReal.tsum_geometric).
- Wrapper to subGaussianNorm ≤ 2√e·K via orliczNorm_le_of_mem (Defs, REAL).

## Fallback (graded)
Only the decl the module docstring designates as this work item's named-sorry fallback; everything else MUST close.

## Constraints
No axiom/admit; no new hypotheses; keep tags; private named helpers OK.
Commit: git add StatLean/ConcentrationInequalities/Orlicz/NormFromMoments.lean && git commit -m "conc/hdp-orlicz-norm-from-moments: close"

## DONE means
Target builds exit 0 (WAIT for it); ≤ 1 designated fallback sorry; report closures + key lemmas.