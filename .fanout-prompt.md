# hdp-debt-rademacher-glue — close the single debt rademacher_process_expectation_le [HDP]
You are a Lean 4 single-debt session on branch conc/hdp-debt-rademacher-glue of the StatLean project (Lean v4.29.1, pinned Mathlib 5e932f97 pre-built under .lake/packages/mathlib). Inside an srun allocation: plain lake build; NEVER lake update; never touch lakefile/manifest/toolchain. You have ~3.5h for ONE lemma — iterate to GREEN, then COMMIT (wait for the final build). No sub-agents, no parallel orchestration: work the proof yourself, directly, verifying each integration step with a build.
CONTEXT: no CLAUDE.md/notes/tools here. Mathlib search: grep over .lake/packages/mathlib + exact?/apply?. EVERYTHING in StatLean/ConcentrationInequalities is REAL except 2 sorries; yours is rademacher_process_expectation_le in VC/LawOfLargeNumbers.lean. Statement FROZEN; only the sorry body (+ private helpers in the same file) may change.
## Your debt roadmap (verbatim from the tranche-2 prompt)
## Debt D — VC/LawOfLargeNumbers.lean: rademacher_process_expectation_le
The conditional Dudley glue; ALL inputs REAL in this file's cone: subGaussianIncrements_inner_signVec (same file, REAL), dudley_inequality_abs (40, REAL), VC/CoveringByVC exports incl. card_le_pow_of_entropy_bound (REAL now), VC/EntropyIntegral (27√d, REAL). Assemble per the module docstring's frozen accounting (per-sample bound 2700-flavored: 40 × √6-normalization × 27 ≤ 2700 — read the docstring formula and land its numeral). Mind: T' = projected class ∪ {0} finite (empFrac-based embedding per the file's earlier REAL lemmas), diam ≤ 2, anchor 0. Gate `...VC.LawOfLargeNumbers`.
## DONE means
## Touch-set: StatLean/ConcentrationInequalities/VC/LawOfLargeNumbers.lean ONLY.
## DONE: lake build StatLean.ConcentrationInequalities.VC.LawOfLargeNumbers exits 0 (WAIT for it); 0 sorries in your file; commit: git add StatLean/ConcentrationInequalities/VC/LawOfLargeNumbers.lean && git commit -m 'conc/hdp-debt-rademacher-glue: close rademacher_process_expectation_le'; report the key steps.