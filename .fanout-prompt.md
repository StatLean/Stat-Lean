# hdp-orlicz-moments — Prop 2.6.1 (iii)⇒(ii) moment growth [HDP §2.6]
You are a Lean 4 proof-closure session on branch conc/hdp-orlicz-moments of the StatLean project (Lean v4.29.1, pinned Mathlib 5e932f97 pre-built under .lake/packages/mathlib). Inside an srun allocation: plain `lake build <Target>`; NEVER `lake update`; never touch lakefile/manifest/toolchain. Iterate until GREEN and COMMIT BEFORE ENDING (wait for the final build, then commit).

CONTEXT: no CLAUDE.md/notes/tools here. Mathlib search: `grep -rn "<name>" .lake/packages/mathlib/Mathlib/` + exact?/apply?. Upstream StatLean lemmas may be sorry-backed — cite as black boxes. REAL on this branch: all Orlicz bridges (Attainment/Triangle/TailToNorm/SubGaussianMGF/SubExponentialMGF/Product/Bernstein).

## Touch-set (edit ONLY): StatLean/ConcentrationInequalities/Orlicz/SubGaussianMoments.lean
No statement/hypothesis/constant/tag changes. If unprovable AS STATED, stop and report precisely why.

## Build gate
`lake build StatLean.ConcentrationInequalities.Orlicz.SubGaussianMoments` — exit 0.

## The goal
Close the file's sorries — READ THE FILE: each docstring's Proof-formalization notes record the designed route and the single designated fallback.

## Proof roadmap (HDP Prop 2.6.1 (iii)⇒(ii), K₂ = √3·K per the frozen docstrings)
- Even-moment core: the lintegral bound on the (2n)-th absolute moment from the ψ₂ gauge condition, via the pointwise estimate u^(2n) ≤ n! · exp(u²) (one exp-series term: u^(2n)/n! is a summand of exp(u²) — grep the pin for a "term le exp"/series lemma, e.g. Real.sum_le_exp_of_nonneg or bound by induction + nlinarith); then lintegral monotonicity against the ≤ 2 gauge integral.
- Real-p wrappers via eLpNorm exponent monotonicity on a probability measure (grep eLpNorm_le_eLpNorm_of_exponent_le family) + factorial bound (Nat.factorial_le_pow) landing the √3·K·√p shape of the stubs (ENNReal.ofReal RHS carriers — mind coercions).
- Attainment input: lintegral_exp_sq_le_two_of_subGaussianNorm_le (Orlicz/Attainment.lean — REAL).

## Fallback (graded)
Only the decl the module docstring designates; everything else MUST close.

## Constraints
No axiom/admit; no new hypotheses; keep tags; private named helpers OK.
Commit: git add StatLean/ConcentrationInequalities/Orlicz/SubGaussianMoments.lean && git commit -m "conc/hdp-orlicz-moments: close"

## DONE means
Target builds exit 0 (WAIT for it); ≤ 1 designated fallback sorry; report closures + key lemmas.