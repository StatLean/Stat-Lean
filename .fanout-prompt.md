# dinf-nets: TotallyBounded bridges + ℝ≥0∞ entropy twins + subset-chaining residuals

TOUCH-SET (only these files):
- StatLean/ConcentrationInequalities/Chaining/DyadicNets.lean   (2 sorries)
- StatLean/ConcentrationInequalities/Chaining/EntropySum.lean   (4 sorries)
- StatLean/ConcentrationInequalities/Chaining/SubsetChaining.lean (3 sorries)

GOAL: close all 9 sorries. These are the infrastructure for the faithful
infinite-T Dudley batch; downstream sessions consume the STATEMENTS (already
frozen), so correctness of proofs is all that matters here.

## Roadmaps

1. `coveringNumber_ne_top_of_totallyBounded` (DyadicNets): from
   `Metric.totallyBounded_iff` get a finite EXTERNAL set t with
   `T ⊆ ⋃ y ∈ t, ball y (ε/2)`. Center-swap: for each y with
   `(ball y (ε/2) ∩ T).Nonempty` choose `z y ∈ T` in that ball; the swapped
   finite set `C ⊆ T` is an internal cover at radius ε (triangle:
   `dist x (z y) ≤ dist x y + dist y (z y) < ε/2 + ε/2`). Note the covering
   number's `IsCover` uses `edist ≤ ε` (≤, not <) — `≤` follows from `<`.
   Then `coveringNumber T ε ≤ C.encard < ⊤`: exhibit C in the iInf
   (`iInf₂_le`-chain over the def `Metric.coveringNumber`, or find the
   Mathlib helper bounding coveringNumber by any internal cover — grep
   `coveringNumber_le` in Mathlib/Topology/MetricSpace/CoveringNumbers.lean).
   Mind the real-vs-NNReal radius: StatLean `coveringNumber T ε` unfolds to
   `Metric.coveringNumber (Real.toNNReal ε) T`, and `Metric.IsCover` is
   edist-based; `edist_le_ofReal` (used in this file) bridges.
2. `totallyBounded_of_coveringNumber_ne_top` (DyadicNets): for `ε > 0` use
   the already-proved `exists_finset_net_of_cov_ne_top (hcov (ε/2) _) hne`
   … careful: T.Nonempty is NOT a hypothesis — handle `T = ∅`
   (`totallyBounded_empty`) separately, then use the net at radius ε/2 and
   `Metric.totallyBounded_iff` with open balls of radius ε (dist ≤ ε/2 < ε).
3. `dudleyLSum_eq_ofReal_of_summable` (EntropySum): `ENNReal.ofReal_tsum_of_nonneg`
   (grep the exact name/direction) with `dudleySummand_nonneg` (exists in
   this file or EntropyIntegrand — grep; else prove from `sqrtLogCov_nonneg`
   and `zpow` positivity).
4. `dudleyLSum_le_two_mul_dudleyLIntegral` (EntropySum): DO NOT route through
   the real lemma (real `dudleyIntegral` can junk for infinite T). Native
   ℝ≥0∞ block-tiling, mirroring `sum_window_le_two_mul_dudleyIntegral`
   (this file, real version) step by step: for each k with `2^{-k} ≤ D`
   (others: `sqrtLogCov T (2^{-k}) = 0` beyond the diameter — see
   `sqrtLogCov_eq_zero_of_diam_le` in EntropyIntegrand; diam is honest via
   `isBounded_of_coveringNumber_ne_top` from DyadicNets), the summand obeys
   `ofReal (dudleySummand T k) ≤ 2 * ∫⁻ ε in Ioc (2^{-(k+1)}) (2^{-k}), ofReal (sqrtLogCov T ε)`
   (antitone integrand via `sqrtLogCov_anti` — its `≠ ⊤` hypothesis comes
   from hcov; interval length = 2^{-(k+1)}, so 2^{-k} = 2·length). Then sum
   over any finite window (disjoint Ioc blocks ⊆ Ioc 0 D, `lintegral` is
   additive/monotone over disjoint sets) and pass to the tsum via
   `ENNReal.tsum_eq_iSup_sum` + `iSup_le`.
5. `ofReal_diam_mul_sqrt_log_two_le_four_mul_dudleyLSum` (EntropySum):
   mirror `diam_mul_sqrt_log_two_le_four_mul_dudleySum` (this file): the
   single scale k* with `diam/4 < 2^{-k*} ≤ diam/2` has 𝒩 ≥ 2
   (`one_lt_coveringNumber_of_two_mul_lt_dist`), so
   `dudleySummand T k* ≥ 2^{-k*}·√log 2 ≥ (diam/4)·√log 2`; then
   `ofReal (diam·√log2) ≤ 4 · ofReal (dudleySummand T k*) ≤ 4 · dudleyLSum`
   (single term ≤ tsum: `ENNReal.le_tsum`). Degenerate diam = 0: LHS 0.
6. `dudleyLIntegral_Ioi_eq` (EntropySum): `Ioi 0 = Ioc 0 D ∪ Ioi D`
   (disjoint, measurable); on `Ioi D` the integrand vanishes
   (`sqrtLogCov_eq_zero_of_diam_le` with `diam T ≤ D < ε`; diam honest via
   `isBounded_of_coveringNumber_ne_top`); `lintegral_union` /
   `setLIntegral_union` + `lintegral_zero` on the tail. Mirror the real
   `dudleyIntegral_Ioi_eq` in this file for the set plumbing.
7. `residual_expectation_le` (SubsetChaining): apply
   `expectation_max_finset_le` (Chaining/FinsetMaximal.lean) to the family
   `fun t ω => X t ω - X (netProj N t) ω` indexed by F with proxy
   `σ2 := 3 * (K * Real.toNNReal ε)^2`. Ingredients: per t ∈ F (so t ∈ T
   via hF), `netProj N t ∈ T` (`netProj_mem_of_subset`, this file);
   center: `∫ (X t - X (netProj N t)) = integral_sub` of hmean's; ψ₂→proxy:
   `SubGaussianIncrements.isSubGaussian_sub` gives proxy `3(K·nndist)²`,
   then enlarge `nndist (netProj N t) t ≤ toNNReal ε`
   (`dist_netProj_le (hprox t _)`, `Real.toNNReal_le_toNNReal`) — mirror
   `hSG` in DiscreteDudley.lean's `discrete_dudley_of_finite` (the
   `isSubGaussian_mono` step is a 5-line private lemma there; re-derive it
   locally: `⟨h.integrable_exp_mul, fun s => (h.mgf_le s).trans (by gcongr ...)⟩`
   — copy the pattern). Finish: `√σ2 = √3·(K·ε)` for ε ≥ 0
   (`Real.sqrt_mul`, `Real.sqrt_sq`, `Real.coe_toNNReal`), matching the
   frozen RHS.
8. `residual_abs_expectation_le` (SubsetChaining): apply
   `expectation_abs_max_le` (Chaining/PsiTwoMaximal.lean) with
   `L := K * Real.toNNReal ε` (`hL : 0 < L` from hK, hε via
   `Real.toNNReal_pos`); ψ₂ bound per member: `hinc (netProj N t) _ t _`
   then `≤ K·edist ≤ (L : ℝ≥0∞)` — mirror `hSGpair` in
   DiscreteDudley.lean's abs proof (edist/ofReal plumbing). The helper's
   conclusion is on `⨆ i ∈ F, |·|` (biSup); convert to the frozen
   `F.sup'` LHS with `biSup_finset_eq_sup' hFne _ (fun _ _ => abs_nonneg _)`
   (rewrite under the integral with `integral_congr` / funext, or transport
   the inequality pointwise). `Real.coe_toNNReal ε hε.le` aligns `(L:ℝ)`
   with `K*ε`.
9. `residual_tail_le` (SubsetChaining): apply `tail_abs_max_le`
   (PsiTwoMaximal) with the same `L := K * Real.toNNReal ε`; its event is
   `{ω | δ < ⨆ i ∈ F, |·|}` — identical to the frozen `F.sup'` event after
   the same biSup↔sup' conversion (prove set equality via `Set.ext` +
   the conversion at each ω, or `measure_congr`/rewrite). RHS aligns via
   `Real.coe_toNNReal`.

FALLBACK (single named sorry allowed if truly stuck): `residual_tail_le`.
Everything else must close.

DONE: 0 sorries in the three touch-set files (except at most the fallback);
`lake build StatLean.ConcentrationInequalities` error-free.
## Common rules (all dinf sessions)

You are a Lean 4 (v4.29.1, Mathlib pinned) proof-closure session for the StatLean
library, working in a git worktree of the Stat-Lean repo on your own branch. The
statements you must prove are ALREADY STUBBED with `sorry` — they are FROZEN
CONTRACTS. You may NOT change any statement, hypothesis, constant, definition, or
docstring of a stubbed declaration; you may NOT touch files outside your touch-set;
you may NOT use `axiom`, `admit`, or add new hypotheses. If you become convinced a
frozen statement is FALSE or unprovable as stated, leave its `sorry` in place and
write a precise blocker report (what corner breaks, with a counterexample sketch) in
your final commit message — do NOT weaken or alter the statement.

Build discipline: iterate with `lake build StatLean.ConcentrationInequalities.<YourModule>`
(fast); before finishing run `lake build StatLean.ConcentrationInequalities` — it must
compile with NO errors (sorry warnings from files outside your touch-set are expected;
your own files must have 0 sorries except, if truly stuck, your single designated
fallback below). Commit early and often with clear messages; your session auto-commits
and pushes at close.

Mathlib search: you have no network tools. Use `exact?`/`apply?`/`rw?` in-proof, and
`grep -rn "<pattern>" .lake/packages/mathlib/Mathlib/` for names. The repo's own
lemmas: grep under StatLean/.

House gotchas: real `⨆ i ∈ s, f i` over a Finset carries junk `sSup ∅ = 0` from
out-of-set indices — the junk-free finite max is `Finset.sup'`; convert with
`biSup_finset_eq_sup'` (needs the family nonneg, e.g. `fun _ _ => abs_nonneg _`) or
`biSup_finset_eq_sup'_of_sup'_nonneg` (needs `0 ≤ sup'`), and `le_biSup_finset` for
the unconditional member bound (all in Chaining/FinsetMaximal.lean). `simp only` does
not β-reduce after `rw [hSdef]`-style set-unfolds reliably — `dsimp only` first when
rewriting under `(fun x => ...) y` redexes. `Finset.sup'_cons` carries the tail's
Nonempty only on its RHS — supply it explicitly. Never write the two-character
sequence `-` `/` inside a docstring. Keep lines ≤ 100 chars.