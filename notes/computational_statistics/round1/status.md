# ComputationalStatistics Round 1 — status

Milestone doc: [outline.md](outline.md) has the scope, dropped-topic table, and
lane plan. Source of truth = `lake build` sorry inventory, not this table.

## State: ALL LANES CLOSED (2026-08-11) — full-library gate + axiom audit pending

All four lanes returned with **every target closed** (46/46 declarations,
0 sorries in the area); lane reports archived in `lane-report-{core,measure,
resampling,partitioning}.md` beside this file.

| Lane | Result |
|---|---|
| A `comp/r1-core` | 12/12 closed. Pin discovery: `ProbabilityTheory.variance_sum_pi` gives Var of coordinate sums directly. |
| B `comp/r1-measure` | ImportanceSampling 5/5 closed first pass. The three rejection-sampling identities were **refuted as first frozen** (nothing excluded `q ≡ ∞`; machine-checked witnesses retained in-file as `private` lemmas) → laptop statement repair added the book hypothesis `hq1 : ∫⁻ q dν = 1` (Gentle's `g_Y` *is* a density) → follow-up session closed all three, axiom-clean. |
| C `comp/r1-resampling` | 17/17 closed, incl. the counts bridge `map_categoricalCounts_pi`: the fiber-cardinality fact `#{a | counts a = v} = Nat.multinomial univ v` has NO Mathlib bridge and was proved from scratch (induction peeling the first coordinate + Pascal-style recurrence via `Nat.multinomial_spec`). |
| D `comp/r1-partitioning` | 12/12 closed (jackknife algebra axiom-clean immediately; measure-theoretic targets tainted only by cross-lane bricks, resolved by the merge). |

## Statement repairs (book-vs-Lean ledger)

1. **RejectionSampling** (`rejectionSampling_{restrict_map,acceptProb,conditionalLaw}`):
   added `hq1 : ∫⁻ z, q z ∂ν = 1` (USER-INPUT; ECS §2.1 — the proposal is a
   *density*). Refutation witness: `𝓧 = Unit`, `ν = δ`, `p ≡ 1`, `q ≡ ∞`,
   `c = 1` — acceptance region is `uniform01`-null, so acceptance probability
   `0 ≠ c⁻¹`. Witnesses retained as `private` lemmas in the file.
2. `empiricalMeasure_apply` restated with `Set.indicator` count (the
   `Finset.filter` form needs a `DecidablePred` instance that cannot be
   synthesized for set membership).

No other statement, hypothesis, or definition changed from the stub commit.

## Verification gate checklist — ALL PASSED

- [x] Per-lane build gates green; per-lane `#print axioms` self-reports clean.
- [x] Lane diffs ⊆ touch-sets; no forbidden surfaces touched; signatures frozen
      (checked by diff-grep over declaration lines).
- [x] Merged to `comp/round1` (lanes A–D), area sorry-free by grep.
- [x] **Fresh full-library build** of the merged tip (job 38067731):
      `Build completed successfully (9192 jobs)`, exit 0, 0 sorries.
- [x] **Independent `#print axioms` sweep** over all **62** public declarations
      (job 39465602, generated by grepping every `theorem`/`instance` in the
      area): all 62 depend on exactly `[propext, Classical.choice, Quot.sound]`;
      0 `sorryAx`, 0 errors.
- [x] Lint: the two `commandStart` "extra space" warnings (2 of only 4
      library-wide) fixed by hoisting the implicit `g` binder onto the
      declaration line — the house-compliant shape keeps ≥1 binder on the
      `theorem` line when the first hypothesis carries a tag comment. Area
      rebuild after the fix: 2643 jobs green, 0 such warnings.

## Known slack (candidates for Round 2, not defects)

* `holdout_integrated` / `kFold_unbiased` / `loo_unbiased` carry
  `hloss : ∀ s, Integrable (ℓ (A s)) D`, which is strictly stronger than the
  a.e.-`s` integrability Fubini would yield from `hint`. Tagged LEAN-ONLY and
  caller-suppliable (bounded loss); tightening needs an `integral_congr_ae`
  pass.
* `MultinomialMoments.memLp_two_pi` (private helper) takes `hq0`/`hq1` that the
  elaborated proof does not consume (2 of 554 such warnings library-wide).
* Deferred by design: geometric law of the rejection-sampling trial count,
  Koksma–Hlawka / QMC, SMC recursion + Feynman–Kac, higher-order jackknife,
  self-normalized IS asymptotics.

## Disposition

`comp/round1` stays a branch for user review: **not merged to `main`, not
pushed to GitHub `origin`** (per §9/§10 of the charter). Website registration
for the new area is deliberately deferred to a separate website batch.
