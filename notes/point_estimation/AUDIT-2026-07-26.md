# PointEstimation — full audit, 2026-07-26

Performed after removing the two defective statements (see
`defective-statements.tex`). Branch `pe/batch11`; area build **GREEN, 3152 jobs,
`sorry-uses-in-build-output: 0`**.

## Verdict

**PointEstimation is complete and sound.** Every theorem in the area is proved; the two
statements that could not be proved were removed because they are *defective as printed*,
not because a proof was missing, and each removal is documented at the site with the repair
that would restore it.

## 1. Soundness — CLEAN

| check | result |
|---|---|
| `sorry` | **0** (3 grep hits are the English word in prose) |
| `admit` | **0** (2 hits are "admit" in bibliographic prose) |
| `axiom` declarations | **0** |
| `native_decide` / `unsafe` / `implemented_by` | **0** |
| `#print axioms` over headline theorems | **69/69 resolved report exactly `[propext, Classical.choice, Quot.sound]`; zero `sorryAx` anywhere** |

The axiom sweep is the load-bearing check: a sorry-count alone cannot detect `sorryAx`
inherited through a dependency. Nothing in the area carries it.

## 2. Inventory

57 files, ~17 000 lines; 191 public theorems/lemmas, 152 private, 99 defs/structures.

## 3. Hypothesis discipline — 186/191 tagged

Every hypothesis of a book-facing theorem carries `USER-INPUT` or `LEAN-ONLY`
(487 + 133 = 620 tags). Five multi-hypothesis theorems carry none; four of those are in
`ForMathlib/` (`MeasurableArgmin`, `ScaleStructure`) where the distinction is meaningless
because the file is theorem-agnostic Mathlib-candidate material, and one is
`InformationInequality/Basic.memLp_score`. **Minor; worth tagging for uniformity.**

## 4. Citations — 57/57

Every file carries a `**Reference.**` paragraph and a `Bibliographic comments.` paragraph;
53/57 additionally carry a machine-greppable `TPE2 §x.y` tag. The convention is respected:
`**Reference.**` names Lehmann–Casella with chapter/section/theorem numbers, while
`Bibliographic comments.` cites the primary literature and deliberately does not name the
textbook.

## 5. Layering — CLEAN (with one sanctioned deviation)

No `ForMathlib/` file imports a concept or assembly file: the one-way DAG holds inside the
area. Cross-area imports:

* `AsymptoticStatistics.ForMathlib.PiWithDensity`, `.Contiguity` — into another area's
  `ForMathlib`, which the charter permits;
* `AsymptoticStatistics.ParametricFamily.{Defs,FisherInformation}` and
  `Bayesian.Sufficiency.Defs` — these are other areas' **concept** layers, which the strict
  charter rule discourages. **This is a sanctioned deviation**: the Batch-11 plan explicitly
  directed reuse of the existing `ParametricFamily`/score/Fisher-information and Bayesian
  `IsSufficient` machinery rather than rebuilding it. Recorded here so it is a decision on
  the record rather than drift.

## 6. Naming — CLEAN

No declaration name encodes a book number (no `thm5_12`, `lemma_1_4`, …), as required.

## 7. Style — ONE REAL FINDING

**243 code lines exceed the 100-column convention** (343 lines total over 100 cols, of which
~100 are prose inside docstrings). Concentrated in `InformationInequality/DensityRegularity`,
`InformationInequality/Additivity` and the larger assembly files. Cosmetic, but it is a
stated convention and it is violated at scale.

## 8. Defects found and fixed during this audit

1. **`LinearModel/RandomDesign.lean` — orphaned docstring broke the build.** Removing the
   `DesignSample` abbrev left its docstring attached to nothing; Lean failed with
   `unexpected token 'variable'; expected 'lemma'`. Caught only by building — the removal
   looked correct on inspection. Fixed.
2. **`Equivariance/ScaleMRE.lean` — stale docstring.** The analytic core still described
   itself as "the one lifted `private` sorry of this file" after that sorry had been closed.
   Fixed.

Both are instances of the same lesson enforced on the cluster sessions all campaign: *a
change that looks right is not verified until it builds*.

## 9. Removals (this session)

* `InformationInequality/Attainment.lean`: `expFamily_of_cramer_rao_attained` and its private
  core removed (FALSE as written). The file keeps its two true results,
  `cramer_rao_attained_of_affine` and `score_ae_eq_of_cramer_rao_attained` — the latter is
  exactly the Cauchy–Schwarz equality step a repaired converse would consume.
* `LinearModel/RandomDesign.lean`: `not_exists_blue_of_known_design_moment` removed
  (hypothesis strictly too weak), together with the scaffolding used by it alone
  (`IsRandomDesignLinear`, `randomDesignModel`, `IsBLUERandomDesign`, `DesignSample`). All
  fixed-design results retained.

Zero external consumers of anything removed (verified by library-wide grep before deletion).

## Recommended follow-ups (none blocking)

1. Wrap the 243 over-length code lines.
2. Tag the five untagged theorems.
3. Decide whether to restore either removed statement in repaired form —
   `g'(θ) ≠ 0` for the attainment converse, and "design information not a.s. constant" for
   Shaffer. Both are editorial decisions about the contract with the source; the mathematics
   is then routine and classical respectively.
